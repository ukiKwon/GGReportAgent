# ── 내부망 개발 (dev) — Oracle ──────────────────────────────────────────────

server.port=8080

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.JndiDataSourceAutoConfiguration
spring.datasource.jndi-name=
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.url=jdbc:oracle:thin:@//<DEV-DB-HOST>:1521/<DEV-DB-SERVICE>
spring.datasource.username=<DEV-DB-USER>
spring.datasource.password=<DEV-DB-PASSWORD>

# ⚠️ uploader 는 dev/stg/prod 에 mybatis.* 를 빠뜨려 두었다(uploader/README §13-①).
#    같은 실수를 하지 않도록 모든 축에 동일하게 적는다.
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.kbstar.kgi.ggreport.web.domain
mybatis.configuration.map-underscore-to-camel-case=true

ggreport.output-root=/app/ggreport

logging.level.com.kbstar.kgi.ggreport=DEBUG
