# ── 내부망 운영 (prod) — Oracle, WebLogic JNDI ──────────────────────────────
# 이 축에는 DB 비밀번호가 없다. WebLogic 콘솔에서 DataSource 를 만들 때 거기 넣고,
# 앱은 이름만 참조한다(WEB-INF/web.xml 의 resource-ref 와 같은 이름).
# 설정 파일 유출로 DB 자격증명이 새는 경로가 원천적으로 없는 구성이다.

spring.datasource.jndi-name=java:comp/env/jdbc/ggreportDS

mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.kbstar.kgi.ggreport.web.domain
mybatis.configuration.map-underscore-to-camel-case=true

ggreport.output-root=/app/ggreport

logging.level.com.kbstar.kgi.ggreport=INFO
