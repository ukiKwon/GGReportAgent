# Product

<!-- impeccable:product-schema 1 -->

이 문서는 `dashboard/`(금고은행 입찰 현황 히트맵)를 다룬다. 저장소의 나머지
(corpus/·plan/ 등 조사·제안 문서 체계)는 별개 산출물이며 이 문서의 대상이 아니다.

## Platform

web

## Users

- KB(은행) 영업·기획 담당자. 이중 용도(사용자 확정 2026-08-13): ⓐ 관계자에게
  보여주는 **시연·데모**(첫인상·설득력)이자 ⓑ 영업팀·전산팀·예산팀이 실제로 쓰는
  **실무 도구**(훑어보기·작업 효율). 리디자인은 두 목적을 모두 만족해야 한다.
- 역할 체계는 3개 소속(영업팀·전산팀·예산팀) × 직책(팀장·부장·디자이너 등)로
  고정돼 있다 (`index.html` #me-team, `js/roles.js`).

## Product Purpose

전국 지자체·공공기관의 금고은행 약정 만료(입찰 임박) 시점을 지도 히트맵으로
탐지하고, 서버 모드에서는 탐지→참여결정→제안물 제작→결재까지의 업무 흐름을
잇는다. **성공의 기준은 "임박한 금고 입찰을 전국 지도에서 한눈에 찾는 것"**
(사용자 확정 2026-08-13 — 지도 탭이 최우선 화면).

## Positioning

- 데이터가 전부 실측이다: eminwon(지자체 공고 시스템) 전수 조사로 게재제호·
  지정공고 원문을 확인해 수집(129+ 레코드, 시·군·구 단위). 모든 레코드에
  `sources` 1개 이상 필수(조작 금지 — edit 모달에 명문화).
- 전국 시·도 → 시·군·구 폴리곤 드릴다운(자체 제작 geo/*.js), 임박도 5단계
  색 구분(6개월↓/1년↓/2년↓/2년+/미상).

## Operating Context

- **폐쇄망**에서 동작. 배포는 파일 복사 수준.
- 이중 모드: ⓐ `file://`로 index.html만 열어도 지도·지역별 탭 동작(폴백),
  ⓑ FastAPI(`backend/main.py`) 서버 모드에서 워크플로·대화(LLM)·지식(RAG)·
  작업함·결재함·권한관리 탭이 추가 노출(`app.applyServerModeUI`).
- LLM·임베딩은 로컬 Ollama(bge-m3 등) — 외부 API 없음.

## Capabilities and Constraints

리디자인 시 반드시 보존(사용자 확정 2026-08-13):

1. **폐쇄망·오프라인**: CDN·웹폰트 등 외부 요청 금지. 벤더링된
   `vendor/d3.v7.min.js`만 예외(로드 실패 폴백 있음).
2. **바닐라 JS 유지**: 프레임워크·빌드 도구 도입 금지. `<script>` 직접 로드 구조.
3. **기존 테스트 통과**: `node --test dashboard/test/*.test.js` (기준 232+ passed).
   테스트가 DOM id·전역 변수(`window.geo*`, `institutions`)·데이터 스키마에
   결합돼 있다 — id/구조 변경 전 테스트 확인 필수.
4. 데이터 정본은 `dashboard/data/institutions.js`(+파생 CSV). 지도 색·강조는
   사용자 설정 가능(테마 모달 → `render.applyTheme`가 CSS 변수 덮어씀) —
   이 설정 체계는 기능이므로 유지.

명시적 미확정: **다크 테마 기조는 구속 조건이 아니다**(사용자가 보존 항목에서
제외함) — 시각 세계 교체 시 밝은/다른 테마도 선택지에 있다.

## Evidence on Hand

- `dashboard/data/institutions.js` — 실측 레코드(지역·기관·lastBid·term·sources).
- `dashboard/geo/*.js` — 전국+13개 시·도 폴리곤(경남 배선 완료, 레코드 조사는 미완).
- `handoff/NEXT.md` §⑨~㉔ — 수집 절차·판정 기준 기록.
- 스크린샷·로고·브랜드 자산은 없다. KB 실명 브랜딩을 화면에 넣는 결정은
  내려진 바 없다 — 날조 금지.

## Product Principles

1. **실측이 전부** — 출처 없는 데이터·과장된 수치를 화면에 만들지 않는다.
2. **오프라인 자립** — 어떤 개선도 외부 의존을 새로 만들지 않는다.
3. **임박도가 곧 우선순위** — 색·정렬·배지 등 모든 시각 신호는 "얼마나
   급한가"를 먼저 전달한다.
4. **지도 우선, 나머지는 지원** — 지도 탭의 밀도·가독성이 다른 탭의 화려함보다
   중요하다.
5. **데모에서도 거짓 없음** — 시연용 연출(데모 계정 전환 등)은 있되, 데이터
   자체는 실무와 동일 정본을 쓴다.
