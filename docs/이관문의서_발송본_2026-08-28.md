# 문의서 발송본 (2026-08-28) — ⛔ **발송하지 않았다 (2026-08-30)**

> ⛔ **상태: 미발송.** 2026-08-30에 **발송하지 않기로 됐다** — 사용자가 곧 내부
> 담당자여서, 메일을 보내 회신을 기다릴 일이 아니라 **직접 물어보면 답이 나온다.**
> 실제로 그날 **10건 중 5건**(1·2·4·5·7)이 그렇게 답변됐고, 답은
> `docs/이관문의서_WebLogic_확인사항.md`의 "답변" 칸에 채워져 있다.
>
> - **이 문서는 지우지 않고 남긴다.** 남은 미회신(**~~1-1~~ 1-2 · 3 · 5-1 · 6-1 · 6-2**
>   — *2026-08-31 갱신*)을
>   결국 다른 부서에 물어야 하게 되면 **그대로 복사해 쓸 수 있다.** 준비는 끝나 있다
>   (`[수신자]`·`[회신 희망일]`·`[연락처]`만 채우면 발송 가능).
> - ⚠️ **다만 본문이 2026-08-28 시점이라 지금과 어긋난다.** 그대로 보내지 말 것 —
>   **1·2·4·5·7번은 이미 답이 나왔고**, 특히 **1·2번(사내 공용 LLM API·임베딩)은
>   전제가 부정돼 질문 자체가 성립하지 않는다.** 보내게 되면 아래 **"미발송 이후에
>   이 문서를 쓰려면"** 절을 먼저 볼 것.
> - **아래 "회신을 받은 뒤에 할 일" 매핑표도 절반은 이미 소비됐다** — 그 절 머리의
>   2026-08-30 주석 참조.

첨부 원본: `docs/이관문의서_WebLogic_확인사항.md` (10개 항목, 답변 칸 포함)

발송 형태는 **두 가지**를 준비했다. 상황에 맞는 쪽을 골라 그대로 복사해 쓰면 된다.

- **A. 통합 1통** — 수신처가 한 곳(정보화 총괄 등)이거나, 어느 부서 소관인지
  모를 때. 첨부 1건으로 끝난다.
- **B. 부서별 3통** — 담당 부서를 이미 알 때. **회신이 빨라진다** — 각 부서가
  자기 항목만 보면 되고, 2·3번(설계를 가르는 항목)이 서로 다른 부서라
  통합본으로 보내면 한쪽 회신이 다른 쪽을 기다리다 늦어진다.

⚠️ 아래 본문의 `[ ]` 표시 부분(수신자·부서명·회신 희망일·연락처)은 **보내기 전에
채워야 한다.** 회신 희망일은 비워두지 말 것 — 기한 없는 문의는 뒤로 밀린다.

---

## A. 통합 1통

**제목**: [협조요청] 입찰 워크플로우 시스템 행내 이관 — 인프라·DB·사내 API 확인 10건

**본문**:

```
[수신 부서/담당자]님, 안녕하십니까.

「입찰 워크플로우 지원 시스템」을 행내 표준 환경(WebLogic + Oracle + Java 8)으로
이관하는 작업을 진행하고 있습니다.

설계는 마쳤고 이관 개발도 상당 부분 끝냈습니다만(WAR 골격·DB 스키마·데이터 계층·
결재 흐름·문서 산출 완료), 남은 기능이 아래 확인 사항에 막혀 더 진행되지 않는
상태입니다. 첨부한 확인 요청서의 10개 항목에 회신을 부탁드립니다.

특히 아래 두 항목은 답에 따라 설계가 통째로 바뀌는 부분이라, 나머지보다 먼저
회신해 주시면 그동안 나머지 작업을 이어갈 수 있습니다.

  · 2번 — 사내 API의 임베딩(embedding) 엔드포인트 제공 여부
           (없으면 의미 검색을 포기하고 전문검색 단독으로 가야 하며,
            현 시스템보다 검색 품질이 낮아집니다. 사전 합의가 필요합니다)
  · 3번 — Oracle Text 사용 가능 여부
           (가능/불가에 따라 한글 전문검색 구현이 두 갈래로 갈립니다)

각 항목의 "답변" 칸을 채워 회신해 주시면 됩니다. 확실치 않은 항목은 "미상"으로
두시고 담당 부서만 알려주셔도 진행에 도움이 됩니다.

회신은 [YYYY-MM-DD]까지 부탁드리겠습니다. 문항 취지에 대한 문의는 언제든
연락 주십시오.

감사합니다.

[성명 / 부서 / 연락처]
```

**첨부**: 이관문의서_WebLogic_확인사항.md (또는 PDF/HWP 변환본)

---

## B. 부서별 3통

각 통의 첨부는 **원본 전체를 그대로 보내되**, 본문에서 볼 항목을 지정한다
(다른 부서 항목까지 보이면 소관 판단이 오히려 빨라진다).

### B-1. AI/정보화 부서 — 1·2번

**제목**: [협조요청] 사내 공용 LLM API 호출 규격 및 임베딩 엔드포인트 확인 (2건)

```
[부서/담당자]님, 안녕하십니까.

「입찰 워크플로우 지원 시스템」의 행내 이관 작업 중, 사내 공용 LLM API 연동
구현이 규격 확인에 막혀 있어 문의드립니다. 첨부 확인 요청서의 1번·2번 항목입니다.

  · 1번 — 호출 규격이 OpenAI 호환인지 (엔드포인트 형식, 인증 방식,
           JSON 스키마 구조화 출력 지원 여부, 사용 가능 모델·컨텍스트 길이)
  · 2번 — 임베딩(embedding) 엔드포인트 제공 여부 (모델명·차원 수·호출 규격)

2번은 답에 따라 설계가 바뀝니다. 임베딩 API가 없으면 현재의 "전문검색 + 의미검색"
하이브리드에서 의미검색을 빼야 하고, 검색 품질이 현 시스템보다 낮아집니다.
이 경우 품질 저하를 감수할지 사전에 합의가 필요해 미리 여쭙습니다.

회신은 [YYYY-MM-DD]까지 부탁드리겠습니다.

[성명 / 부서 / 연락처]
```

### B-2. DBA — 3·4번

**제목**: [협조요청] Oracle Text 사용 가능 여부 및 DB 버전 확인 (2건)

```
[부서/담당자]님, 안녕하십니까.

「입찰 워크플로우 지원 시스템」의 행내 이관 작업 중, 한글 전문검색 구현 방식을
정하지 못해 문의드립니다. 첨부 확인 요청서의 3번·4번 항목입니다.

  · 3번 — Oracle Text 사용 가능 여부
           ⓐ 설치·라이선스 사용 가능 여부
           ⓑ 한글 형태소 렉서(KOREAN_MORPH_LEXER) 사용 가능 여부
           ⓒ 인덱스 생성 권한을 애플리케이션 계정에 부여 가능한지
  · 4번 — Oracle 버전 (11g / 19c / 23ai)

3번은 가능 여부에 따라 구현이 두 갈래로 갈리는 항목이라(Oracle Text CONTEXT 인덱스
vs. 애플리케이션 자체 색인), 이 회신 없이는 해당 기능을 착수할 수 없습니다.
4번은 DDL 확정 전에 못 박아 두면 재작업이 없습니다.

회신은 [YYYY-MM-DD]까지 부탁드리겠습니다.

[성명 / 부서 / 연락처]
```

### B-3. 인프라/미들웨어 — 5·5-1·6·6-1·6-2·7번

**제목**: [협조요청] WebLogic 환경, 공통 API 게이트웨이 연동 규격·등록 절차 확인 (6건)

```
[부서/담당자]님, 안녕하십니까.

「입찰 워크플로우 지원 시스템」을 행내 표준 환경(WebLogic + Oracle + Java 8)으로
이관하고 있습니다. 배포 환경과 공통 API 게이트웨이(경유지) 연동에 대해 확인이
필요해 문의드립니다. 첨부 확인 요청서의 5번·5-1·6번·6-1·6-2·7번 항목입니다.

  · 5번   — WebLogic 버전 (12.2.1.x / 14.1.1 / 기타)
  · 5-1번 — 애플리케이션의 WorkManager(CommonJ) 사용 가능 여부
            (JNDI 조회 이름, 전용 WorkManager 별도 정의 필요 여부, 시험 환경)
  · 6번   — 경유지의 OAuth 인증 규격 (서비스 간 호출 방식, 토큰 수명·갱신 절차,
            게이트웨이가 인증을 완결하는지 뒤쪽 서비스도 재검증해야 하는지)
  · 6-1번 — 신규 서비스의 API 통합관리시스템 등록 절차
            (신청 창구·양식, 필요 정보, 소요 기간, 환경별 등록 여부)
  · 6-2번 — 게이트웨이 공개 API가 지켜야 할 표준 규격 문서
  · 7번   — 경유지의 응답 버퍼링·유휴 타임아웃 설정값과 변경 요청 창구
            (대화 화면이 스트리밍 응답을 씁니다)

5-1번은 오래 걸리는 작업을 요청 스레드 밖에서 돌리기 위해 여쭙습니다. WAS에서
애플리케이션이 스레드를 직접 만들 수 없어 WorkManager로 구현했는데, 저희 개발
환경에는 WebLogic이 없어 이 경로만은 행내 환경에서만 검증이 가능합니다.
6-1번은 등록이 끝나야 연동 시험을 시작할 수 있어 일정 산정에 직접 영향을 줍니다.
6-2번은 현재 저희 API가 시험용 형태라 규격에 맞춘 변경 개발이 필요한데, 규격을
먼저 받아야 재작업 없이 한 번에 맞출 수 있습니다.
7번은 대화 화면의 스트리밍 응답 때문에 여쭙는 항목입니다 — 앞단에서 응답을 버퍼링하면
표시가 멈추고, 유휴 타임아웃이 짧으면 답변 도중 연결이 끊깁니다(저희 검증 환경에서
타임아웃 60초에서 응답이 잘리는 것을 확인했고, 300초로 늘리고 버퍼링을 끄는 것으로
해결했습니다).

회신은 [YYYY-MM-DD]까지 부탁드리겠습니다.

[성명 / 부서 / 연락처]
```

---

## 미발송 이후에 이 문서를 쓰려면 (2026-08-30 신설)

남은 5건을 결국 다른 부서에 물어야 하게 되면, **위 본문을 그대로 쓰지 말고 아래대로
줄여서** 쓴다.

| 통 | 종전 항목 | 지금 물을 것 |
|---|---|---|
| **B-1. AI/정보화** | 1·2번 | ⛔ **보내지 않는다.** "사내 공용 LLM API가 있다"는 전제가 부정돼 **두 문항 모두 성립하지 않는다** |
| **B-2. DBA** | 3·4번 | **3번만.** 4번(Oracle 버전)은 **11g로 답이 나왔다** |
| **B-3. 인프라** | 5·5-1·6·6-1·6-2·7번 | **5-1·6-1·6-2만.** 5번은 **`12.2.1.4`로 확정 회신**(2026-08-31, 종료), 6번은 LLM 목적 소멸, 7번은 "앞단 웹서버 없음"으로 사실상 해소 |
| **신설 — 인프라/서버 담당** | — | ~~**1-1(서버 사양)**~~ → **1-2(별도 추론 서버)**: *2026-08-31 갱신.* 1-1은 회신됐고 답이 **Solaris/SPARC = 자체 호스팅 불가**였다. 질문이 **"x86 리눅스 서버를 받을 수 있는가"** 로 바뀌었다 — 아래 절 참조. ⚠️ **지금 가장 급한 항목**이고 위 3통 어디에도 본문이 없다 |

**우선순위는 ~~1-1~~ 1-2 → 3 → 5-1 → 6-1·6-2.** **1-2와 3만 받으면 이관이 다시 굴러간다.**
*(5번은 2026-08-31에 종료됐다.)*

### 1-2 문항에 담을 것 (2026-08-31 신설, 아직 본문 미작성)

**협상 사다리** — 어디까지 받느냐로 살아나는 기능이 달라진다:

| 단계 | 사양 | 살아나는 것 |
|---|---|---|
| **1 (최소)** 임베딩만 | x86_64 4코어 · RAM 8GB · 디스크 20GB | **벡터 검색(Task 3.3) 유지** — 코퍼스 벡터는 반입하므로 **질의 임베딩 1건**만 돌리면 된다 |
| **2 (권장)** + 생성, CPU | x86_64 **16코어(AVX2)** · RAM 32GB · 디스크 100GB | + 배점표 추출·문서 요약·검증(**배치**). 8코어 249초 실측 기준 1건당 2분대 |
| **3 (이상적)** + GPU | NVIDIA **VRAM 24GB**급 · CPU 8코어 · RAM 32GB | + **대화 탭까지** (1건당 수 초) |

**공통 필수 조건**: OS는 **Linux**(Ubuntu 22.04 LTS / RHEL 8·9) — ⚠️ Solaris·SPARC면
다시 원점이다. **VM·컨테이너로 충분**하고, **기존 x86 리눅스 서버의 여유분에 얹는 것**도
방법이다(신규 도입보다 문턱이 낮다).

**하드웨어 말고 함께 물을 것 3가지**:
1. **WebLogic 서버 → 추론 서버 HTTP 호출이 방화벽에서 열리는가** ⚠️ 여기서 **문의 6
   (경유지 OAuth)이 되살아날 수 있다** — 별도 서버가 생기면 "서비스 간 호출"이 된다
2. **모델 파일(5~10GB) 폐쇄망 반입 경로** (라이브러리 반입 절차와 같은 길일 수 있다)
3. **실행파일 설치 권한** — 1-1의 ⓔ가 **아직 미답**으로 남아 여기로 넘어왔다

## 회신을 받은 뒤에 할 일 (다음 세션용)

⚠️ **2026-08-30 — 이 표의 절반은 이미 소비됐다.** 1·2·4·5·7번이 답을 받았고,
**예상이 절반만 맞았다** — "판단이 아니라 기계적 반영"이라고 적어 뒀으나, **1·2·6번은
가정을 확정한 게 아니라 전제 자체를 부정**해서 매핑표대로 반영할 수가 없었다.
남은 행(**~~1-1~~ 1-2 · 3 · 5-1 · 6-1·6-2** — *2026-08-31 갱신*)에 대해서는 여전히 유효하다.

회신이 오면 **문의서의 "답변" 칸을 채워 커밋**하고, 아래 매핑대로 계획의 잠정
지점을 확정한다.

| 회신 | 확정되는 것 | 상태 |
|---|---|---|
| **1-2번 (별도 추론 서버)** | **Task 4.4 구현 가능 여부 + Task 3.3(벡터 검색) 존폐 + LLM 기능 범위(ⓐ전부/ⓑ배치만/ⓒ없이)** | 🔴 **미회신 · 최우선**(2026-08-31 신설, **문항 미작성**) |
| ~~1-1번 (서버 사양)~~ | ~~Task 4.4를 구현할 수 있는지~~ | ✅ **회신 2026-08-31 — Solaris/SPARC · 4코어 · 56GB · 20GB ⇒ 자체 호스팅 불가.** 질문이 1-2로 바뀌었다 |
| 3번 (Oracle Text) | 단계 3 검색 — Oracle Text CONTEXT vs Java 인메모리 색인 | 🔴 **미회신** — 유일하게 구조가 갈린다 |
| 5-1번 (WorkManager) | Task 4.2 배경 실행 — JNDI 이름 확정, 폴백 경로 탈출 검증 | 🔴 **미회신** |
| 6-1·6-2 (등록·규격) | 연동 시험 일정, API 표준 규격 맞춤 변경 개발 범위 | 🔴 **미회신** — LLM과 무관하게 유효 |
| ~~2번 (임베딩)~~ | ~~하이브리드 유지 vs 전문검색 단독~~ | ✅ **해소** — 자체 호스팅으로 `bge-m3` 확보 ⇒ **Task 3.3 존치** |
| ~~1번 (LLM 규격)~~ | ~~어댑터 두께~~ | ✅ **전제 부정** — 사내 공용 LLM API 없음 ⇒ **자체 호스팅**. 대기 대상이 1-1로 옮겨갔다 |
| ~~6번 (경유지 OAuth)~~ | ~~Task 4.4 토큰 획득·갱신 구현 범위~~ | 🟡 **LLM 목적 소멸** — 토큰 계층 **보류**(인터페이스는 존치). ⓒ 재검증만 6-1과 함께 남는다 |
| ~~7번 (버퍼링·타임아웃)~~ | ~~Task 4.3 — 경유지 설정 변경 요청서~~ | 🟡 **앞단 웹서버 없음(web-was 일체형)** ⇒ 요청서는 **조건부**로 내려갔다 |
| ~~4번 (Oracle 버전)~~ | ~~DDL 확정~~ | ✅ **11g** — **DDL 리뷰 1회 발동**(12c 전용 문법 7곳, `NEXT.md` 항목 9) |
| ~~5번 (WebLogic 버전)~~ | ~~`weblogic.xml` 설정값~~ | ✅ **회신 2026-08-31 — `12.2.1.4`**(Servlet 3.1). 가정대로였고 **바뀌는 것 없음. 종료** |

상세는 `docs/superpowers/plans/2026-08-10-weblogic-java-migration.md`의 매핑표.
