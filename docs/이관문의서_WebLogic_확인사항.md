# 시스템 이관 사전 확인 요청서 — 인프라·DB·사내 API 9건

- **요청 배경**: 저희가 운영 중인 「입찰 워크플로우 지원 시스템」(현재 Python/FastAPI
  기반)을 **행내 표준 환경(WebLogic + Oracle + Java 8)** 으로 이관하기 위한 설계를
  진행 중입니다. 아래 9개 항목의 답에 따라 설계가 갈리는 부분이 있어,
  **개발 착수 전에** 확인을 부탁드립니다.
- **회신 방법**: 각 항목의 "답변" 칸을 채워 회신해 주시면 됩니다. 확실치 않은
  항목은 "미상"으로 두고 담당 부서만 알려주셔도 진행에 도움이 됩니다.
- **작성일**: 2026-08-10 (개정 2026-08-20, **재개정 2026-08-25**)
- **2026-08-25 재개정 요지**: ⓐ 종전 6번(전자정부 표준프레임워크 관련 문항)을
  **삭제**했습니다 — 이관 대상이 행내 표준 플랫폼이라 해당 사항이 없습니다.
  ⓑ 그 자리에 **경유지(공통 API 게이트웨이) OAuth 규격** 문항을 새로 넣었습니다.
  ⓒ 7번은 "앞단 웹서버를 두는 구성입니까"라는 유무 확인이었으나, 경유지를 반드시
  거치는 구조임이 확인되어 **설정값과 변경 창구를 여쭙는 문항**으로 바꿨습니다.
  ⓓ 이 시스템이 **신규 서비스**임이 확정되어 **6-1(서비스 등록 절차)** 과
  **6-2(API 표준 규격)** 두 문항을 추가했습니다 — 현재 API가 시험용 형태라
  규격에 맞춘 변경 개발이 필요합니다. 문항 수 **7개 → 9개**.

---

## [사내 공용 LLM API 관련 — 담당: AI/정보화 부서]

### 1. 사내 공용 LLM API의 호출 규격이 OpenAI 호환입니까?

- **여쭙는 이유**: 시스템이 문서 요약·검증에 LLM을 호출합니다. OpenAI 호환
  (`POST /v1/chat/completions`, JSON 스키마 구조화 출력 지원)이면 어댑터를 얇게
  구현할 수 있고, 독자 규격이면 요청/응답 변환 계층을 추가로 설계해야 합니다.
- **구체 확인 사항**:
  - ⓐ 엔드포인트 URL 형식 (예: `https://llm.내부도메인/v1/chat/completions`)
  - ⓑ 인증 방식 (Bearer 토큰 / API Key 헤더 / 사내 SSO 연동 등)
  - ⓒ 구조화 출력(JSON schema 강제) 지원 여부
  - ⓓ 사용 가능한 모델 이름 목록과 컨텍스트 길이
- **답변**:

### 2. 사내 API가 임베딩(embedding) 엔드포인트를 제공합니까?

- **여쭙는 이유**: 현 시스템의 문서 검색은 "전문검색 + 의미(벡터) 검색"
  하이브리드입니다. 임베딩 API가 **없으면 의미 검색을 포기하고 전문검색
  단독**이 되며, 검색 품질이 현 시스템보다 낮아집니다 — 이 경우 품질 저하를
  감수할지 사전 합의가 필요해 미리 여쭙습니다.
- **구체 확인 사항**: 임베딩 엔드포인트 유무, 있다면 모델명·차원 수·호출 규격.
- **답변**:

## [Oracle DB 관련 — 담당: DBA]

### 3. Oracle Text를 사용할 수 있습니까?

- **여쭙는 이유**: 한글 전문검색 구현이 두 갈래로 갈립니다 —
  Oracle Text `CONTEXT` 인덱스(표준적, 운영 이관 깔끔) vs. Java 인메모리
  색인(Oracle Text 불가 시 폴백). **설계가 통째로 갈리는 항목**이라 가장
  먼저 확인이 필요합니다.
- **구체 확인 사항**:
  - ⓐ Oracle Text 설치·라이선스 사용 가능 여부
  - ⓑ 한글 형태소 렉서(`KOREAN_MORPH_LEXER`) 사용 가능 여부
  - ⓒ 인덱스 생성 권한을 애플리케이션 계정에 줄 수 있는지
- **답변**:

### 4. Oracle 버전은 무엇입니까? (11g / 19c / 23ai)

- **여쭙는 이유**: 11g면 `IDENTITY` 컬럼과 일부 JSON 함수를 쓸 수 없습니다.
  현 스키마는 애플리케이션이 PK를 생성하는 방식이라 영향이 크지는 않지만,
  DDL 작성 전에 버전을 못 박아야 재작업이 없습니다.
- **답변**:

## [WAS/인프라 관련 — 담당: 인프라/미들웨어]

### 5. WebLogic 버전은 무엇입니까? (12.2.1.x / 14.1.1 / 기타)

- **여쭙는 이유**: 12.2.1.x는 Servlet 3.1, 14.1.1은 Servlet 4.0입니다. 실시간
  진행 표시(SSE)는 둘 다 가능하지만 `weblogic.xml` 설정과 프록시 타임아웃
  값이 버전에 따라 달라집니다.
- **답변**:

### 6. 공통 API 게이트웨이(경유지)의 OAuth 인증 규격을 알려주실 수 있습니까?

- **여쭙는 이유**: 시스템이 사내 LLM API 등 다른 서비스를 호출할 때 공통
  게이트웨이를 경유하며 OAuth 인증을 거치는 것으로 알고 있습니다. 토큰 획득·
  갱신 처리를 애플리케이션에 구현해야 하는데, 규격에 따라 구현 범위가 달라집니다.
- **구체 확인 사항**:
  - ⓐ 서비스 간 호출에 쓰는 인증 방식 (Client Credentials 등 서비스 계정 발급
    방식인지, 사용자 토큰을 위임하는 방식인지)
  - ⓑ 토큰 수명과 갱신 절차, 발급 엔드포인트
  - ⓒ **게이트웨이가 인증을 완결해 주는지, 뒤쪽 서비스도 토큰을 다시 검증해야
    하는지** — 재검증이 필요하면 저희 쪽에 인증 처리를 새로 구현해야 합니다
- **답변**:

### 6-1. 신규 서비스를 API 통합관리시스템에 등록하는 절차를 알려주십시오.

- **여쭙는 이유**: 이 시스템은 **신규 서비스**이므로 통합관리시스템에 서비스
  등록(IP:Port 포함)이 필요합니다. 등록이 완료되어야 연동 시험을 시작할 수 있어
  **일정 산정에 직접 영향**을 줍니다.
- **구체 확인 사항**: 신청 창구·양식, 등록에 필요한 정보 목록, 소요 기간,
  개발/스테이징/운영 환경별로 각각 등록해야 하는지 여부.
- **답변**:

### 6-2. 게이트웨이로 공개하는 API가 지켜야 할 표준 규격 문서를 받을 수 있습니까?

- **여쭙는 이유**: 현재 저희 API는 **시험용(mock-up) 형태로 만들어져 있어 통합관리
  시스템 규격에 맞게 변경 개발이 필요합니다.** 규격을 먼저 받아야 재작업 없이
  한 번에 맞출 수 있습니다.
- **구체 확인 사항**: 요청/응답 형식(공통 헤더·응답 봉투 구조), 오류 코드 체계,
  URL·파라미터 명명 규칙, 페이징·대용량 응답 처리 규약, 첨부/바이너리 전송 방식,
  API 문서화(스웨거 등) 제출 요건.
- **답변**:

### 7. 경유지의 응답 버퍼링·유휴 타임아웃 설정값과, 변경을 요청할 창구를 알려주십시오.

- **여쭙는 이유**: 시스템의 대화 화면과 작업 진행 표시가 SSE(Server-Sent Events)로
  응답을 조금씩 흘려보냅니다. 앞단 프록시가 **응답을 버퍼링하면 실시간 표시가
  멈추고**, **유휴 타임아웃이 짧으면 작업 도중 연결이 끊깁니다**(참고로 저희
  검증 환경에서는 타임아웃 60초 설정에서 응답이 잘리는 것을 확인했고, 300초로
  늘리고 버퍼링을 끄는 것으로 해결했습니다).
- **구체 확인 사항**: 현재 버퍼링·타임아웃 설정값, 특정 경로에 한해 조정이
  가능한지, 조정을 요청하는 창구와 절차.
- **답변**:

---

특히 **2번(임베딩)과 3번(Oracle Text)은 설계 자체를 바꾸는 항목**입니다 — 이 둘만이라도
먼저 회신 주시면, 나머지를 기다리는 동안 구현 계획을 진행할 수 있습니다.
