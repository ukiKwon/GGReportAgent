# WebLogic/Java 이관 설계 — Python(FastAPI+LangGraph) → 당행 표준 Java 플랫폼

작성일: 2026-08-05 · **개정 2026-08-25** · **개정 2026-08-26** · **개정 2026-08-28** ·
**개정 2026-08-30** · **개정 2026-08-31**

> 🔴 **2026-08-31 개정 — 하루 만에 LLM 전제가 또 바뀌었다. 이번엔 하드웨어다.**
>
> 8/30 개정이 "사내 LLM API가 없으니 **우리가 직접 올린다**"로 방향을 틀었는데,
> **1-1(서버 사양) 회신으로 그 길도 막혔다.**
>
> | 받은 값 | |
> |---|---|
> | **OS·아키텍처** | **Oracle Solaris** · `isainfo -kv` → **64-bit `sparcv9`** |
> | CPU | 물리 **4코어** / 8 virtual processor |
> | RAM | **57344 MB (56 GB)** |
> | 디스크 여유 | **20 GB** · GPU **없음** · 실행파일 설치 허용 **미답** |
>
> 🔴 **SPARC/Solaris에는 LLM 추론 런타임을 올릴 현실적 경로가 없다.** ⓐ Ollama·
> llama.cpp가 **Solaris 빌드를 배포하지 않고**, ⓑ SPARC는 **빅엔디언**인데 모델 파일
> 포맷(GGUF)이 리틀엔디언 전제이며(빅엔디언 지원은 s390x용만 있고 SPARC는 미검증),
> ⓒ **SPARC VIS용 SIMD 백엔드가 없어** 스칼라 폴백이면 4코어로 1건당 수십 분~시간대다.
> ⚠️ **RAM 56GB는 넉넉하지만 소용이 없다 — 막는 것은 용량이 아니라 아키텍처다.**
>
> - 🔴 **문의 2의 최악 시나리오(의미 검색 포기)가 되살아났다.** 8/30에 "자체 호스팅
>   하면 임베딩도 올리면 된다"로 닫았던 근거가 사라졌다 — §6-A의 벡터 검색 존치
>   판정을 **조건부로 내렸다.**
> - 🟢 **다만 완전히 잃지는 않는다.** 코퍼스 벡터는 **색인 시점에 계산해 BLOB으로
>   저장**되므로(`agent/retrieval/indexer.py:67`) **외부망 PC에서 미리 계산해 반입**할
>   수 있다. 런타임에 남는 것은 **질의 임베딩 1건**뿐이다 — 이 하나만 해결하면
>   하이브리드 검색이 유지된다.
> - **Java 재작업은 이번에도 0이다.** 어댑터 경계(§3 원칙 3) 덕에 바뀌는 것은 여전히
>   "Task 4.4가 무엇을 구현하느냐"뿐이다.
> - ⇒ **§7의 1-1이 "서버 사양"에서 "별도 추론 서버를 받을 수 있는가"(1-2)로 바뀐다.**
>   ⚠️ 별도 서버가 생기면 **"서비스 간 호출"이 되므로 문의 6(경유지 OAuth)이
>   되살아날 수 있다** — §6-C의 토큰 계층을 지우지 않은 이유가 하나 더 늘었다.
>
> ✅ **함께 확정된 것 — WebLogic `12.2.1.4`**(문의 5). 가정하던 12.2.1.x가 그대로
> 맞았다. **Servlet 3.1 확정**이고 §7-5·Task 1.2·Task 4.3에서 바뀌는 것은 없다.

> 🔴 **2026-08-30 개정 — LLM 전제가 뒤집혔다. §2·§3·§6-C·§7이 그에 맞춰 고쳐졌다.**
>
> 발송을 기다리던 문의서에 **내부 담당자(사용자 본인)가 직접 답해** 10건 중 5건이
> 해소됐다(`docs/이관문의서_WebLogic_확인사항.md`의 "답변" 칸). 그중 하나가 이 설계의
> 전제였다.
>
> 1. 🔴 **사내 공용 LLM API가 없다**(문의 1·2). 폐쇄망이라 외부 호출도 불가하고,
>    사내에 공용으로 열린 LLM 엔드포인트도 없다. ⇒ **모델을 우리가 서버에 직접 올리고
>    애플리케이션이 `localhost`로 부른다.** 네트워크가 서버 밖으로 나가지 않는다.
>    - **Java 재작업은 0이다** — `LlmClient`·`TokenProvider`는 인터페이스만 동결돼 있고
>      구현은 `NotYetMigratedLlmClient`가 일부러 실패시킨다. 바뀌는 것은 "Task 4.4가
>      앞으로 무엇을 구현하느냐"뿐이다.
>    - 🟢 **문의 2의 최악 시나리오(의미 검색 포기)는 회피된다** — 임베딩도 자체 호스팅
>      하면 되고, 현 Python이 쓰는 `bge-m3`(1.2GB·1024차원)를 그대로 올리면 같은 벡터가
>      나온다. **§6-A의 벡터 검색 존치**(구현계획 Task 3.3).
>    - 🔴 **문의 6(경유지 OAuth)은 LLM 목적으로는 무의미**해졌다. 나가는 호출이 없기
>      때문이다. `TokenProvider`는 **지우지 않는다** — 6번 ⓒ("우리 시스템이 공개되는
>      쪽의 토큰 재검증")는 남아 있고, 그것이 "필요"로 오면 **인증이 통째로 새로 생기는
>      별도 과제**다(현재 이 시스템에는 인증이 전혀 없다).
>    - ⚠️ **2026-08-25 기록과 정면으로 충돌한다** — 그때는 "경유지를 거쳐 사내 LLM API를
>      호출하는 구조임이 확인됨"이었다. **오늘 회신을 기준으로 삼되 이 불일치 자체가
>      재확인 대상이다.** 뒤집히면 1·2·6번이 되살아나므로 아래 §2·§3·§6-C의 개정 전
>      서술을 지우지 않고 취소선·주석으로 남겼다.
> 2. **문의 4 — Oracle 11g 확정.** ⚠️ `VARCHAR2` 상한이 **4000바이트**다(12c의 확장
>    32767 불가). §5-(B)의 CLOB 결정이 **선택이 아니라 필수**가 됐다(이미 그렇게 돼
>    있어 설계 변경은 없다). DDL·SQL의 12c 전용 문법 수정은 별건이다(`NEXT.md` 항목 9).
> 3. **문의 7 — 앞단 웹서버가 없다(web-was 일체형).** 사용자 유입 경로의 프록시
>    버퍼링·유휴 타임아웃 걱정이 사라진다. 단 **"경유지"와는 층위가 다를 수 있다** —
>    경유지는 *나갈 때*, 앞단 웹서버는 *들어올 때*다.
> 4. **문의 5 — WebLogic 12 버전대(소버전 미확인).** 기존 가정(12.2.1.x / Servlet 3.1)
>    유지. 비차단.
>
> **새로 생긴 최우선 미회신은 1-1(서버 사양)** 이다 — 별도 추론 서버 없이 **WebLogic
> 서버 한 대에 모델을 얹어야** 하므로, RAM·코어·설치 허용 여부에 따라 **아예 못 올릴
> 수도 있다.** §7 표에 신설했다.
> *(→ **2026-08-31에 회신됐고, 실제로 "못 올린다"였다.** 문서 상단 2026-08-31 주석 참조.)*

> ⚠️ **2026-08-28 개정 — 원본을 "SSE"로 규정한 것은 사실오류였다. §3 이관 대응표를 고쳤다.**
>
> 종전 §3 표가 `chat.py`·`tasks.py`의 스트리밍을 **SSE**로 적고 대응물을 Spring
> `SseEmitter`로 지정했다. **원본은 SSE가 아니다** — 코드에 그렇게 못 박혀 있다
> (`server/routers/chat.py:88`: *"SSE가 아니다 — EventSource는 GET만 되는데 이
> 엔드포인트는 POST라 애초에 쓸 수 없고, 본문도 `data:` 프레이밍이 없는 평문이다"*).
> 실제로는 **POST + `text/plain; charset=utf-8` 청크 스트림**이고, 프런트는
> `EventSource`가 아니라 `fetch` + `body.getReader()`로 읽는다(`frontend/js/chat.js:144`).
>
> - **왜 중요한가**: `SseEmitter`는 `text/event-stream` + `data:` 프레이밍을 붙인다.
>   그대로 만들면 채팅 말풍선에 `data:` 접두사가 그대로 쌓인다. **"화면은 손대지
>   않는다"(§3 원칙 1)가 이관의 전제**라 프레이밍을 바꾸는 선택지는 없다.
> - **바뀐 것은 대응물뿐이다** — `SseEmitter` → **`StreamingResponseBody`**.
>   `async-supported=true`가 필요한 것은 양쪽 다 같으므로 §7-5·§7-7의 **질문은
>   그대로 유효**하다(청크 스트리밍도 버퍼링·유휴 타임아웃에 똑같이 깨진다).
> - **곁가지**: 스트리밍 소비처는 **채팅 하나뿐**이다. 작업 진행 표시는 스트리밍이
>   아니라 **폴링**이다(`frontend/js/workflow.js`의 `progress_pct`). `tasks.py`의
>   스트리밍 엔드포인트는 프런트 호출부가 없는 **API 전용**이다.

> ⚠️ **2026-08-25 개정 — 전제 2건이 바뀌었다. 이 문서를 읽기 전에 먼저 볼 것.**
>
> 1. **eGovFrame(전자정부 표준프레임워크) 제약을 폐기했다.** 2026-08-05 브레인스토밍에서
>    "eGovFrame 4.x 준수 필수"를 확정 제약으로 받아 적었고, 그 한 줄이 문서 제목·
>    Spring Boot 배제·§7 확인항목까지 끌고 갔다. **잘못 받은 확정이었다** — eGovFrame은
>    행안부가 정한 **공공기관 정보화사업 전용** 프레임워크이고, 이 시스템의 이관
>    목적지는 **당행(은행) 내부 표준 플랫폼**이다. 적용 대상이 아니다. 제안 대상이
>    구청·지자체라는 점에서 혼선이 있었던 것으로 보인다.
>    → **되살아나는 조건**: 이 시스템을 지자체·공공기관에 **납품**하기로 하면 그때는
>    다시 유효하다. 그 경우 이 문서의 §2·§3·§7을 개정 전 상태로 되돌려야 한다.
> 2. **경유지(회사 공통 API 게이트웨이) + OAuth가 확정 사실로 들어왔다.** 서비스 간
>    호출은 전부 이 경유지를 지나고 OAuth 인증을 거친다. 개정 전에는 §7-7이 앞단
>    프록시 유무를 *가정*으로 물었으나, 이제 **있다는 것이 확정**이다 — SSE 버퍼링·
>    타임아웃은 가설이 아니라 확정 리스크이고, LLM 호출에는 토큰 처리가 필요하다(§6-C).

> ⚠️ **2026-08-26 개정 — 실행 환경이 5축으로 확정됐다(사용자 확정).**
>
> 종전에 §8이 "개발용 Oracle 인스턴스를 전제한다"고만 적어 둔 자리가 실제 환경으로
> 구체화됐다. `uploader/config-envs/`가 **이미 이 5축을 그대로** 갖고 있어, §8 단계 1의
> 골격 재사용(구현계획 Task 1.1) 근거가 하나 더 붙었다.
>
> | 망 | 프로파일 | DB | 접속 |
> |---|---|---|---|
> | 내부망 로컬 | `local` | **Oracle** | `jdbc:oracle:thin:@//localhost:1521/ORCL` |
> | 내부망 개발 | `dev` | **Oracle** | thin (호스트·서비스명 미정) |
> | 내부망 스테이징 | `stg` | **Oracle** | thin (호스트·서비스명 미정) |
> | 내부망 운영 | `prod` | **Oracle** | **JNDI** `java:comp/env/jdbc/uploaderDS` |
> | **외부망 로컬(만)** | `out-local` | **MySQL** | `jdbc:mysql://localhost:3306/uploaderdb` |
>
> 1. **내부망은 로컬까지 전 구간 Oracle이다.** §8의 "H2 대체 금지"는 그대로 유효하고,
>    그 전제가 "개발용 Oracle을 구해야 한다"에서 **"이미 있다"** 로 바뀌었다.
> 2. **외부망 로컬만 MySQL이다.** 스키마 **정본은 Oracle이고 MySQL은 미러**다 — DDL을
>    두 벌 유지해야 하며 정본이 바뀌면 미러도 함께 바꾼다. ⚠️ §5의 빈 문자열·CLOB
>    차이는 MySQL에서 드러나지 않으므로 **MySQL 통과는 Oracle 정합성의 근거가 되지
>    않는다** — H2를 금지한 것과 정확히 같은 이유다.
> 3. **Maven 중앙 저장소에 닿을 수 없다.** 폐쇄망이므로 필수 라이브러리는 **수동 반입**
>    이다. 외부망 로컬에서 오프라인 리포지토리를 말아 반입하는 절차가 구현계획
>    Task 1.1의 산출물(`dependencies.txt`)이다. **반입 절차 자체는 이미 마련돼 있고
>    까다롭지 않다**(사용자 확인) — 따라서 이관 일정의 리스크 항목이 아니고, 문의서에
>    별도 문항을 넣지 않는다.
> 4. **JDK 1.8은 내·외부망 공통으로 사용 가능하다.**
>
> **함께 개정된 것 — §5-(A)의 결정이 실행 불가능해 바뀌었다.** Task 1.3에서 DDL을
> 실제로 쓰다 발견했다. `tasks.draft_content`를 `NOT NULL`로 유지하면 최초 INSERT가
> 반드시 실패한다(Oracle이 `''`를 NULL로 바꾸므로 "명시값을 넣는다"로 빠져나올 수 없다).
> **NULL 허용 + Mapper의 `null → ""` 정규화**로 바꿨다. 상세는 §5-(A).
>
> **검증이 어디서 이뤄지는지도 명시했다** — §8의 개정판 표. 외부망 로컬은 IntelliJ +
> MySQL로 화면·로직·REST 계약을 돌려 보는 자리이고, **Mapper·DDL 정합성과 Oracle Text는
> 내부망 Oracle에서만 판정한다.** 이 구도는 사용자가 확인한 실제 작업 계획이다.

## 1. 목적과 결론

지금 이 저장소의 실동작 시스템(FastAPI 백엔드 + LangGraph 에이전트 + 순수 HTML/JS
대시보드)을 **WebLogic 기반 Web/WAS 구성으로, 주 언어를 Java + HTML로 이관**하는 것이
목표다.

**결론: 가능하다.** 화면은 사실상 그대로 재사용하고, 백엔드는 전면 재작성한다.
재작성 대상은 프로덕션 Python **6,288줄**(테스트 6,833줄 / 63파일은 JUnit으로 별도
재작성)이며, 이 중 라이브러리 등가물이 없어 직접 만들어야 하는 부분은
**오케스트레이터 상태·재개(HITL)** 와 **한글 전문검색** 두 군데다.

## 2. 확정된 제약 (사용자 확인 완료)

| 항목 | 값 | 영향 |
|---|---|---|
| 언어 | **Java only** — 폐쇄망 서버에 Python 런타임 불가 | 사이드카 방식 배제. RAG 색인·임베딩 호출·PPTX 생성까지 전부 Java |
| JDK | **1.8** | Spring Boot 3·Lucene 9·java.net.http 사용 불가. 라이브러리 상한이 전부 구세대로 고정 |
| 프레임워크 | **Spring Boot 2.7.x → WAR 배포** (JDK 8을 지원하는 마지막 Boot 계열) | `SpringBootServletInitializer`로 WAR화해 WebLogic에 올린다. **개정 2026-08-25** — 종전의 "eGovFrame 4.x 필수 / Boot 배제"를 폐기했다(문서 상단 개정 주석) |
| 영속화 | **Oracle + MyBatis** | SQLite 3파일(registry / corpus_index / LangGraph 체크포인트) 전부 Oracle 스키마로 통합. **개정 2026-08-26** — 내부망은 로컬~운영 전 구간 Oracle이고, **외부망 로컬(`out-local`)만 MySQL 미러**다(문서 상단 5축 표) |
| WAS | **WebLogic `12.2.1.4`** (✅ 확정 2026-08-31, 문의 5) · **OS: Oracle Solaris / SPARC** | 컨테이너 관리 스레드·JNDI DataSource 전제. 앱이 스레드를 직접 만들면 안 됨. **Servlet 3.1** — 가정하던 값 그대로라 `weblogic.xml`·비동기 스트리밍에 변경 없음. ⚠️ **OS·아키텍처가 새 제약이다** — Solaris/SPARC(빅엔디언)라 **네이티브 바이너리를 전제하는 것은 무엇도 올릴 수 없다**(LLM 런타임이 여기 걸렸다). 순수 JVM으로 도는 것만 안전하다 |
| 망 | **폐쇄망** | 의존성 전량 사전 반입(사내 Nexus 또는 오프라인 리포지토리). **개정 2026-08-26** — **Maven 중앙 저장소 접근 불가가 확정 사실**이다. 빌드 머신에서 `mvn`이 새 의존성을 받아 올 수 없으므로 라이브러리 추가에는 반입 절차가 따른다. 단 **그 절차는 이미 있고 까다롭지 않다**(사용자 확인) — 일정 리스크가 아니라 "빌드 중 자동 취득이 안 된다"는 사실로만 다룬다 |
| **서비스 간 통신** | ~~회사 공통 API 게이트웨이(경유지) 경유 + OAuth 필수~~ → **개정 2026-08-30: 나가는 호출 자체가 없다** | 2026-08-25에 "목적지로 직접 부르는 경로가 없다"로 신설했으나, **유일한 바깥 목적지였던 사내 LLM API가 없어졌다**(문의 1). 앱에서 나가는 사내 호출이 없으므로 경유지·OAuth는 **LLM 목적으로는 무의미**하다. 남는 것은 반대 방향뿐 — **우리 시스템을 게이트웨이에 공개**할 때의 등록 절차·표준 규격(문의 6-1·6-2)과 들어오는 토큰 재검증 여부(문의 6-ⓒ). 후자가 "필요"면 **인증이 통째로 새로 생기는 별도 과제**다 |
| LLM | 🔴 **미정 — 올릴 곳이 없다** (개정 2026-08-31) | ~~당행 사내 공용 LLM API(경유지 경유)~~(8/30 부정) → ~~자체 호스팅, `localhost` 호출~~(8/31 부정). **WebLogic 서버가 Solaris/SPARC라 추론 런타임을 올릴 수 없다**(문서 상단 2026-08-31 주석). ⇒ **별도 추론 서버(x86 리눅스)를 받을 수 있는지가 §7의 최우선 확인항목(1-2)** 이고, 그 답이 **LLM 기능의 존폐 전체**를 정한다. 못 받으면 기능 범위를 ⓐ전부/ⓑ배치만/ⓒLLM 없이 중에서 다시 고른다 |
| 임베딩 | 🟡 **부분적으로 살릴 수 있다** (개정 2026-08-31) | 현 Python과 같은 `bge-m3`(1024차원). **코퍼스 벡터는 색인 시점 계산분을 외부망에서 미리 만들어 반입**할 수 있어(§6-A) 런타임 요구는 **질의 임베딩 1건**으로 줄어든다. 그 하나를 어디서 도느냐가 벡터 검색(Task 3.3)의 존폐다 |
| 브라우저 | **크롬/엣지 최신** | ES6 모듈·`fetch`·`EventSource`·d3 v7 그대로 사용 가능 → **프런트 재작성 없음** |

## 3. 목표 아키텍처

```
[크롬/엣지]
   │ HTTP
   ▼
WebLogic ── ggreport.war (단일 WAR)
   ├─ /            정적 리소스: dashboard/index.html, js/*.js, geo/*.json, vendor/d3
   ├─ /api/**      Spring MVC @RestController (기존 11 라우터와 1:1)
   │                  └ Service ── MyBatis Mapper ──▶ Oracle (JNDI DataSource)
   ├─ Orchestrator 상태머신 ── CommonJ WorkManager 스레드
   │                  └ 실행상태·체크포인트 ──▶ Oracle (ORCH_RUN / ORCH_STEP)
   └─ LLM/Embed 어댑터 ──┐
                         │ HTTP  ❓ 목적지 미정 (2026-08-31)
                         ▼
              [??? — 추론 런타임을 올릴 곳이 없다]
                  ⓐ 별도 x86 리눅스 서버   ← 요청 대기 (문의 1-2)
                  ⓑ ~~같은 서버(localhost)~~ ← ⛔ Solaris/SPARC라 불가
                  ⓒ 없음 → LLM 기능 제외 · 전문검색 단독
```

**개정 2026-08-30 — 나가는 화살표가 사라졌다.** 종전 그림은
`LLM/Embed 어댑터 ──HTTP+OAuth──▶ [경유지] ──▶ 당행 사내 공용 LLM API` 였다.
**그 사내 API가 없다**(문의 1·2 회신) — 모델을 우리 서버에 직접 올리므로 호출이
`localhost`에서 끝나고, **네트워크가 서버 밖으로 나가지 않는다.**

🔴 **재개정 2026-08-31 — 그 `localhost`도 사라졌다.** WebLogic 서버가 **Solaris/SPARC**
라 추론 런타임을 올릴 수 없다(문서 상단 주석). 화살표의 **목적지가 미정**이고, 그것을
정하는 것이 문의 1-2다.

- ⓐ **별도 x86 리눅스 서버**를 받으면 화살표가 되살아난다 — 단 이번엔 **서버 밖으로
  나가는 호출**이라 ⚠️ **경유지·OAuth가 다시 끼어들 수 있다**(§6-C 토큰 계층이 보류
  상태로 살아 있는 이유). 협상 사다리: **임베딩만**(4코어·8GB) → **생성까지**
  (16코어·32GB) → **GPU**(VRAM 24GB, 대화 탭까지).
- ⓒ **못 받으면** 이 화살표가 통째로 없어진다 — LLM 노드가 빠지고 검색은 전문검색
  단독이 된다. 그때는 §6-A의 **Oracle Text(문의 3)가 유일한 검색 수단**이다.

~~⚠️ **대신 새 제약이 생겼다 — 모델과 WAS가 한 서버를 나눠 쓴다.**~~ *(8/31 무효 —
같은 서버에 올릴 수 없게 됐다.)* 다만 **ⓐ에서 사양이 빠듯하면 같은 문제가 그대로
따라온다**: CPU 추론은 1건당 수 분간 코어를 전부 먹으므로 **동시 실행 1건 제한·코어
상한**이 어댑터가 아니라 **배치 정책**으로 필요하다. 8코어 실측 **249초/건**이 기준선이다.

~~**경유지는 우회로가 없다**(2026-08-25 확정). 앱에서 나가는 모든 사내 호출은 이
게이트웨이를 지나며 OAuth를 거친다.~~ — **2026-08-30 무효화**(나가는 호출이 없다).
들어오는 방향은 별개다: 2026-08-30 회신이 **앞단 웹서버 없음(web-was 일체형)** 이라고
했으므로 브라우저→WebLogic 사이의 프록시 버퍼링 걱정도 사라진다. 단 경유지가 그
경로에 끼어 있는지는 아직 확인 대상이다(§7-7).

핵심 원칙 세 가지:

1. **화면은 손대지 않는다.** `dashboard/`(2,986줄 + d3 vendor)는 빌드 단계가 없는 순수
   정적 자산이라 WAR의 정적 리소스로 그대로 들어간다. 브라우저가 현대 버전이므로
   폴리필도, JSP 재작성도 필요 없다. 유일한 변경은 API 베이스 경로(`/api` 컨텍스트)다.
2. **REST 계약을 먼저 동결한다.** 프런트가 안 바뀌므로 기존 응답 JSON이 그대로 계약이다.
   Java 쪽은 이 계약을 만족시키기만 하면 되고, 이것이 이관 검증의 기준선이 된다.
3. **LLM 어댑터 경계를 유지한다.** 현재 `agent/llm.py`는 모델·엔드포인트·키를 전부
   환경변수로 받는 63줄짜리 어댑터다(폐쇄망을 이미 전제한 설계). Java에서도 같은 경계를
   유지해 엔드포인트 규격이 바뀌어도 한 클래스만 고치면 되게 한다.
   *2026-08-30 — 이 원칙의 값어치가 오히려 커졌다.* 상대가 "사내 공용 API"에서
   **우리가 고르는 로컬 런타임**으로 바뀌었는데도 **Java 코드 재작업이 0**인 이유가
   이 경계다. 런타임을 바꿔 끼우는 것도 같은 자리에서 끝난다.

## 4. 컴포넌트 매핑

| 현재 (Python) | 이관 후 (Java / JDK 1.8) | 비고 |
|---|---|---|
| FastAPI `APIRouter` 11개 (`server/routers/`) | `@RestController` 11개 | 기계적. 경로·응답 형태 동일 유지 |
| Pydantic `models.py` | POJO + Jackson 2.x + Bean Validation(hibernate-validator 6.2.x) | Pydantic의 자동 검증·직렬화가 두 갈래로 나뉜다 |
| `sqlite3` 직접 호출 모듈 12개 (리포지토리 4 + 서비스·유틸 8) | MyBatis 3.5.x Mapper 인터페이스 + XML | 원시 SQL이 대부분이라 이식성은 좋다 |
| `registry.db` **7테이블** | Oracle **7테이블** | PK가 전부 앱 생성 TEXT라 시퀀스 불필요 (§5 함정 참고). **개정 2026-08-26** — 종전 "6테이블"은 오기다. `server/db.py` 실측 결과 `institutions`·`bid_cases`·`tasks`·`messages`·`notifications`·`role_menus`·`chat_messages` 7개이며, 계획 I에서 뒤늦게 추가된 `role_menus`가 빠져 있었다. 유니크 인덱스 `idx_bid_cases_notice`도 함께 옮긴다 |
| `corpus_index.db` FTS5(trigram) | **Oracle Text CONTEXT 인덱스** (폴백: 인메모리 색인) | §6-A |
| `vectors` BLOB + numpy 코사인 | Oracle BLOB + Java `float[]` 브루트포스 코사인 | 코퍼스가 5.6MB/413파일이라 전량 메모리 적재로 충분. 벡터DB 불필요 |
| LangGraph `StateGraph`/`interrupt()`/`SqliteSaver` | **직접 구현한 상태머신 + Oracle 체크포인트** | §6-B. 그래프 자체는 129줄이지만 재개 의미론이 본질 |
| `threading.Thread` 백그라운드 실행 (`orchestrator_service.py`, `reindex_service.py`) | **CommonJ WorkManager** (`commonj.work.WorkManager`, JNDI 조회) | WAS에서 앱이 raw thread를 만드는 것은 금지 사항 |
| `StreamingResponse` 평문 청크 스트림 (`chat.py`, `tasks.py`) — **SSE가 아니다** | Servlet 3.1 `AsyncContext` + Spring **`StreamingResponseBody`** | **개정 2026-08-28**(문서 상단). POST + `text/plain; charset=utf-8`, `data:` 프레이밍 없음. `SseEmitter`를 쓰면 프레이밍이 붙어 화면이 깨진다. `web.xml`/필터에 `async-supported=true` 필수인 것은 동일 (§7) |
| `pypdf` | **PDFBox 2.0.x** | RFP PDF 텍스트 추출 |
| `python-pptx` | **Apache POI 5.2.x (XSLF)** | 산출물 PPTX 생성. 서식 재현 검증 필요 |
| `langchain_openai.ChatOpenAI` | **Apache HttpClient 4.5 + Jackson 직접 호출** | §6-C — LangChain4j를 쓰지 않는다 |
| `pytest` 63파일 | JUnit 4.13 + Mockito + Spring MockMvc | §8 |

## 5. Oracle 전환에서 실제로 깨지는 지점

SQLite → Oracle은 "타입만 바꾸면 되는" 작업이 아니다. 현재 스키마를 읽어 확인한 구체적
충돌 두 가지를 설계에 반영한다.

**(A) 빈 문자열이 NULL이 된다.** Oracle은 `''`를 NULL로 취급한다. 현재 스키마에는
`tasks.draft_content TEXT NOT NULL DEFAULT ''`가 있고, `NOT NULL` 컬럼에 `DEFAULT ''`는
Oracle에서 그대로 옮기면 제약 위반이 된다. 나아가 애플리케이션 의미도 달라진다 —
Python 코드가 `draft_content == ""`(아직 안 씀)로 판단하던 자리에서 Java는 `null`을
받는다.

~~**결정: 이런 컬럼은 Oracle에서 `NOT NULL`을 유지하되 DB 기본값을 두지 않고,
INSERT 시 애플리케이션이 항상 명시값을 넣는다.**~~
⚠️ **개정 2026-08-26 — 이 결정은 실행 불가능하다.** Task 1.3에서 DDL을 실제로 쓰다
발견했다. `NOT NULL`을 유지하면 **최초 INSERT가 반드시 실패한다** — 작업은 "아직
아무것도 안 쓴" 상태로 생성되는데, 앱이 빈 문자열을 *명시적으로* 넣어도 Oracle이 그것을
NULL로 바꾸므로 제약에 걸린다. "명시값을 넣는다"로는 빠져나올 수 없다. `''`가 아닌
센티널(공백 한 칸 등)을 넣는 방법은 데이터에 거짓을 심는 것이라 택하지 않는다.

**개정된 결정: 이런 컬럼은 `NULL` 허용으로 두고, 읽을 때 Mapper 레벨에서 `null → ""`로
정규화**해 프런트가 받는 JSON을 현재와 동일하게 유지한다. 정규화는 원래 결정에도 이미
들어 있던 항목이라 바뀌는 것은 **DB 쪽 제약 하나뿐**이다 — "아직 안 씀"의 표현이 `''`
에서 `NULL`로 바뀌고, 그 차이는 Mapper 안에서 끝난다.

- 해당 컬럼: `tasks.draft_content` (현재 스키마에서 `NOT NULL DEFAULT ''`인 유일한 컬럼).
- (`participation_decision DEFAULT '[]'`처럼 빈 문자열이 **아닌** 기본값은 문제없다.
  Oracle에서 그대로 `DEFAULT '[]'`로 옮긴다.)
- ⚠️ **외부망 로컬(MySQL)에서도 똑같이 `NULL` 허용으로 둔다.** MySQL은 `''`를 담을 수
  있지만, "아직 안 씀"의 표현이 두 DB에서 갈리면 Mapper를 한 벌로 못 쓴다. 그리고
  **이 결함은 MySQL에서 드러나지 않으므로**(빈 문자열이 그대로 저장된다) 정규화가
  빠져 있어도 외부망 로컬에서는 아무 증상이 없다 — 내부망 Oracle에서 처음 터진다.
- 구현: `kgi-ggreport-web/src/main/resources/db/` (정본 `oracle/001_schema.sql`).

**(B) 긴 텍스트는 CLOB이어야 한다.** `draft_content`, `messages.content`,
`chat_messages.content`, 그리고 색인 청크 본문은 4000바이트를 넘길 수 있어 `VARCHAR2`로
못 받는다. **결정: 이들은 CLOB.** 그 대가로 (1) `WHERE clob_col = ?` 같은 등호 비교가
안 되므로 그런 쿼리가 있으면 해시 컬럼을 따로 두고, (2) MyBatis에 `jdbcType=CLOB`을
명시하고, (3) `ORDER BY`/`DISTINCT` 대상에서 CLOB을 뺀다.

⚠️ **개정 2026-08-30 — 이 결정이 선택에서 필수로 바뀌었다.** 문의 4 회신으로 **Oracle
11g가 확정**됐다. 11g의 `VARCHAR2` 상한은 **4000바이트**이고 12c의 확장 상한(32767,
`MAX_STRING_SIZE=EXTENDED`)을 쓸 수 없다 — "길면 CLOB"이 아니라 **넘치면 방법이 없다.**
설계가 이미 CLOB으로 정해 둬서 **바꿀 것은 없다.** 다만 DDL·SQL 쪽에 남아 있는 12c 전용
문법(`GENERATED BY DEFAULT AS IDENTITY`, `FETCH FIRST`)은 별건으로 고친다 —
위치는 `NEXT.md` 항목 9에 파일·줄 단위로 특정돼 있다.

**(C) 날짜는 문자열로 남긴다.** 현재 모든 시각 컬럼이 ISO 문자열(`TEXT`)이고 프런트도
문자열로 받는다. `DATE`/`TIMESTAMP`로 승격하면 타임존·포맷 차이로 화면 표시가 바뀐다.
**결정: `VARCHAR2`로 1:1 이관.** 날짜 타입 승격은 이관 완료 후 별건으로 다룬다.

**(D) 마이그레이션 로직 대체.** `server/db.py`의 `MIGRATIONS` 딕셔너리(뒤늦게 추가된
컬럼을 기동 시 `ALTER TABLE`로 메우는 방식)는 Oracle에서 그대로 쓸 수 없고, 그럴 필요도
없다. **결정: 최종 스키마 DDL 한 벌을 정본으로 만들고, 이후 변경은 번호가 붙은 DDL
스크립트로 관리한다**(폐쇄망 DBA 반영 절차와도 맞는다).

## 6. 등가물이 없어 직접 만드는 세 가지

### A. 한글 전문검색 — FTS5 trigram 대체

현재는 SQLite FTS5의 `tokenize='trigram'`, 즉 3글자 부분문자열 색인이다. 한글에서
형태소 분석 없이 부분일치를 얻는 실용적 선택이었다. Oracle에는 등가물이 없다.

- **1안(권장): Oracle Text `CONTEXT` 인덱스.** 당행 Oracle 환경에서 가장 표준적이고
  운영 이관이 깔끔하다. 한글은 `KOREAN_MORPH_LEXER`, 부분일치가 필요하면 `WORDLIST`에
  substring 색인을 켠다. 단 **형태소 색인은 trigram과 검색 결과가 달라진다** — 이건
  버그가 아니라 의도된 교체이므로, 골든 쿼리 세트로 차이를 문서화하고 수용 여부를 받는다.
- **2안(폴백): Java 인메모리 색인.** Oracle Text를 쓸 수 없을 때. 코퍼스가 5.6MB뿐이라
  기동 시 전량 적재해 trigram 색인을 Java로 재현하면 **현재 동작을 가장 정확히 보존**한다.
  단 WebLogic 다중 인스턴스면 인스턴스마다 중복 적재되고, 반입 즉시 반영이 아니라 재적재
  트리거가 필요하다.
- Lucene은 배제한다. JDK 1.8이면 Lucene 8.x 계열에 묶이고, 인덱스 파일이 WAS 파일시스템에
  생겨 이중화·백업 대상이 Oracle 밖으로 새어나간다.

**벡터 검색은 조건부로 유지한다** — 🟡 *2026-08-30 확정 → 2026-08-31 조건부로 하향.*
문의 2가 "임베딩 API가 없으면 의미 검색을 포기한다"를 걱정했고, 8/30에는 자체 호스팅으로
닫혔다가 **8/31에 SPARC 때문에 다시 열렸다.** 현 Python이 쓰는
**`bge-m3`(1.2GB·1024차원)를 그대로** 써야 같은 벡터가 나오고 골든 비교가 성립한다.

🟢 **요구가 생각보다 작다 — 런타임에 필요한 것은 질의 임베딩 1건뿐이다.**
`agent/retrieval/indexer.py:67`이 보여주듯 **코퍼스 청크 벡터는 색인 시점에 계산해
BLOB으로 저장**된다. 즉 **무거운 쪽(전량 임베딩)은 외부망 PC에서 미리 돌려 DB에 넣어
반입**할 수 있고, 운영 서버는 **검색할 때 짧은 질의 하나만** 임베딩하면 된다.

| 문의 1-2 결과 | 벡터 검색 |
|---|---|
| ⓐ 별도 추론 서버 확보 | ✅ **그대로 유지** — 지금과 같은 품질 |
| ⓑ 질의 임베딩만 Java로 자체 구현 | 🟡 **가능은 하다** — 단 bge-m3 encoder 이식 비용·속도가 미검증이라 **실측 전에는 계획에 넣지 않는다** |
| ⓒ 아무것도 못 받음 | ❌ **포기 — 전문검색 단독.** 원래 예고한 대로 **품질 저하 사전 합의**가 필요하고, §6-A 1안(Oracle Text)의 중요도가 올라간다 |

청크 벡터를 Oracle BLOB(리틀엔디언 float32)로 옮기고 Java에서
`float[]` 코사인 유사도를 전수 계산한다. 수천 청크 × 1024차원이면 수십MB·수십ms 규모다.
하이브리드 랭킹(BM25 + 코사인) 합산식은 현재 `agent/retrieval/search.py`의 것을 그대로 옮긴다.

### B. 오케스트레이터 — LangGraph `interrupt()` 대체

LangGraph 사용량 자체는 얇다(`agent/orchestrator/graph.py` 129줄). 어려운 건 라이브러리가
공짜로 주던 **의미론**이다: 노드 실행 중 `interrupt()`로 멈추고, 상태를 체크포인트에
저장하고, 사람이 결재한 뒤 `Command(resume=...)`로 **멈춘 지점부터** 다시 흐른다.

**결정: 명시적 상태머신으로 직접 구현한다.**

- `ORCH_RUN` — 실행 1건(입찰건·현재 노드·상태·생성시각).
- `ORCH_STEP` — 노드별 입출력과 결과 스냅샷(= 체크포인트). 상태 직렬화는 Jackson JSON.
- 노드는 Java `enum` + 각 노드 1클래스(`rfp_extract`, `rfp_analysis`, `role_router`,
  `institution_match`, `content_writer`, `verification`, `pptx_builder` — 현재 7개와 1:1).
- 라우팅은 노드가 "다음 노드 이름"을 반환하는 방식으로 단순화한다. 현재 쓰는
  `Command(goto=[Send(...)])` 팬아웃(팀별 병렬 작성)은 **팬아웃 노드가 자식 STEP 여러 건을
  만들고 WorkManager에 각각 제출, 전부 완료되면 조인 노드로 진행**하는 형태로 재현한다.
- `interrupt()`는 "노드가 `PENDING_APPROVAL`을 반환하고 리턴" + "결재 API가 해당 RUN을
  다시 큐에 넣음"으로 대체한다. **재개 지점이 DB에 명시적으로 남는 게 오히려 장점**이다
  (지금은 LangGraph 체크포인트 내부에 있어 운영자가 못 본다).
- PII 마스킹(`orchestrator/pii.py` 42줄)은 정규식이라 그대로 이식된다.

### C. LLM 호출 — LangChain4j를 쓰지 않는 이유

실제로 쓰는 기능은 두 개뿐이다: **구조화 출력**(`structured_llm`, 스키마에 맞는 JSON을
받는다)과 **2단 폴백**(1순위 모델 실패 시 2순위 재시도). 이걸 위해 프레임워크를 들이면
JDK 1.8 지원 때문에 구버전에 묶이고, 폐쇄망 의존성 반입 목록만 길어진다.

**결정: Apache HttpClient 4.5 + Jackson으로 직접 호출한다.** 구조화 출력은 "JSON 스키마를
프롬프트에 포함 → 응답에서 JSON 블록 추출 → Jackson 역직렬화 → 실패 시 재시도" 로 재현한다
(현재 langchain의 `with_structured_output`도 사내 API가 tool-calling을 지원하지 않으면
결국 이 경로다). 폴백은 어댑터 안의 try/catch 2단으로 유지하고, **어느 모델이 실제로
답했는지 응답에 실어 보낸다** (`messages.model` 컬럼이 이미 그 용도로 존재한다).

**🔴 개정 2026-08-30 — 이 절의 토큰 계층은 보류됐다(삭제가 아니다).** 아래 2026-08-25
서술은 "경유지를 지나 사내 LLM API를 부른다"를 전제로 쓰였는데, **그 사내 API가
없다**(문의 1). 모델을 우리 서버에 올려 `localhost`로 부르므로 **나가는 호출이 없고,
따라서 OAuth 토큰도 없다.**

- **`TokenProvider` 인터페이스는 지우지 않는다.** 문의 6-ⓒ(우리 시스템이 게이트웨이로
  **공개될 때** 들어오는 토큰을 재검증해야 하는가)가 아직 열려 있고, "필요"로 판명되면
  인증이 통째로 새로 생긴다 — 다만 그것은 **어댑터가 아니라 입구 쪽 별도 과제**다.
- **2026-08-25 기록과의 충돌은 재확인 대상**이다. 뒤집히면 아래가 그대로 되살아나므로
  지우지 않고 남긴다.
- **오늘의 어댑터가 실제로 할 일**은 아래 본문의 **구조화 출력 + 2단 폴백 + 응답
  모델명 기록**뿐이다. 로컬 런타임이 OpenAI 호환 엔드포인트를 제공하므로 얇게 간다.
- **대신 새로 들어오는 것**: 모델이 WAS와 한 서버를 쓰므로(§3) **동시 실행 1건 제한과
  타임아웃**이 어댑터의 실질 요구사항이 된다. CPU 추론은 1건당 수 분이 걸린다.

~~**⚠️ 2026-08-25 추가 — 이 어댑터는 경유지를 지나며 OAuth 토큰이 필요하다.**~~ 개정 전
설계는 엔드포인트를 직접 부르는 전제라 **토큰 처리 자리가 아예 없었다.** 현재
`agent/llm.py`(63줄)에도 없다 — 파이썬 쪽은 로컬 Ollama와 무인증 엔드포인트만 상대해
왔기 때문이다. Java 어댑터에는 다음이 새로 들어간다(**2026-08-30 보류**).

- **토큰 획득·캐시·갱신**: 만료 전 선제 재발급 + 401 수신 시 1회 재시도. 토큰은
  메모리에만 두고 **로그·`messages` 기록·예외 메시지에 절대 싣지 않는다**(현재
  `server/agent_adapter.py`의 실패 안내문이 엔드포인트 정보를 그대로 노출하므로,
  이관 시 같은 실수를 하지 않도록 여기 적어 둔다).
- **폴백과의 상호작용**: 2단 폴백의 1순위 실패가 **인증 실패(401/403)인지 모델
  부재(404)인지 구분**해야 한다. 인증 실패는 2순위로 넘어가도 똑같이 실패하므로
  폴백을 태우지 말고 즉시 사람에게 알린다 — 안 그러면 토큰 만료가 "모델 2개가 다
  죽었다"로 보고된다.
- ~~**경유지 규격이 §7 확인항목**(토큰 발급 방식·수명·서비스 계정 여부)이며, 회신
  전에는 **어댑터 인터페이스만 동결하고 구현은 비워 둔다.**~~
  **2026-08-30 — 회신이 왔고 "해당 없음"이었다.** 인터페이스 동결은 이미 끝났으므로
  (`LlmClient`·`TokenProvider`, 구현은 `NotYetMigratedLlmClient`가 일부러 실패) 어댑터를
  막는 것은 이제 경유지 규격이 아니라 ~~문의 1-1(서버 사양)~~ **문의 1-2(별도 추론
  서버 확보)** 다 — 모델을 올릴 곳이 정해져야 무엇을 구현할지가 정해진다.
  ⚠️ **2026-08-31 — 별도 서버로 가면 토큰 계층이 되살아날 수 있다.** `localhost`가
  아니라 **서버 간 호출**이 되어 경유지·OAuth가 다시 끼어들 수 있기 때문이다.
  이 절을 보류로만 두고 지우지 않은 판단이 이틀 만에 두 번 값을 했다.

## 7. 설계 전 확인이 필요한 항목 (기관/DBA 문의)

⚠️ **2026-08-25 개정 — 번호를 문의서와 통일했다.** 종전에는 이 표와
`docs/이관문의서_WebLogic_확인사항.md`의 **4·5·6이 서로 달라서**(문의서가 담당
부서별로 묶느라 순서를 바꿨다) 2026-08-19 점검에서 실제로 매핑 오류가 발견됐었다.
회신은 문의서 번호로 오므로 **이 표가 문의서 순서를 따르도록 맞췄다.** 이제 두 문서와
구현계획의 번호가 전부 같다 — 변환표가 더 이상 필요 없다.

✅ **2026-08-30 — 발송하지 않았다. 내부 담당자(사용자)가 직접 답해 5건이 해소됐다.**
문서 상단 2026-08-30 개정 주석과 문의서의 "답변" 칸을 함께 볼 것. 아래 표의 **상태** 열이
그 결과이며, **1·2·6번은 답이 가정을 확정한 게 아니라 전제 자체를 부정**했다.

| # | 담당 | 확인할 것 | 상태 (2026-08-30) | 답에 따라 바뀌는 것 |
|---|---|---|---|---|
| **1-2** | 인프라 | **[신설 2026-08-31] 별도 추론 서버(x86 리눅스)를 받을 수 있는가** — 사다리: 임베딩만(4코어·8GB) → 생성까지(16코어·32GB) → GPU(VRAM 24GB). 함께 물을 것: **WebLogic→추론서버 방화벽 개방 · 모델 파일(5~10GB) 반입 경로 · 실행파일 설치 권한** | 🔴 **미회신 — 최우선**<br>*(문항 미작성)* | **LLM 기능의 존폐 전체 + 벡터 검색(Task 3.3)의 존폐.** ⚠️ 서버가 생기면 "서비스 간 호출"이 되어 **문의 6(경유지 OAuth)이 되살아날 수 있다** |
| ~~1-1~~ | 인프라 | ~~**[신설] 이 시스템이 올라갈 서버 사양** — RAM · 코어 수 · GPU 유무 · 디스크 여유 · 실행파일 설치 허용 여부~~ | ✅ **회신 2026-08-31 — 자체 호스팅 불가 판정** | **Oracle Solaris / `sparcv9`** · 물리 4코어(8 vCPU) · RAM 56GB · 디스크 여유 20GB · GPU 없음. **아키텍처 때문에 막혔다**(문서 상단 주석) — RAM은 넉넉하나 소용없다. ⓔ 설치 허용은 **여전히 미답**이라 1-2로 넘겼다 |
| 1 | AI/정보화 | ~~사내 공용 LLM API가 **OpenAI 호환**인가, 인증 헤더 방식은?~~ | ✅ **회신 — 전제 부정** | **사내 공용 LLM API가 없다.** 폐쇄망이라 외부 호출도 불가 ⇒ **자체 호스팅**(§2·§3). 규격은 우리가 정하는 값이 되고, 로컬 런타임이 OpenAI 호환을 제공하므로 어댑터는 계획대로 얇다 |
| 2 | AI/정보화 | ~~사내 API가 **임베딩 엔드포인트를 제공하는가**~~ | ✅ **회신 — 걱정 해소** | 사내 임베딩 API도 없지만 **최악(의미 검색 포기)은 일어나지 않는다** — `bge-m3`(1024차원)를 함께 올리면 지금과 같은 벡터가 나온다. **벡터 검색 존치, 품질 저하 합의 불필요** |
| 3 | DBA | **Oracle Text** 사용 가능 여부(설치·라이선스·`KOREAN_MORPH_LEXER`) | 🔴 **미회신** | §6-A 1안/2안 분기. **여전히 유일하게 설계 구조가 갈리는 항목** |
| 4 | DBA | Oracle 버전 (11g / 19c / 23ai) | ✅ **회신 — 11g** | `IDENTITY`·일부 JSON 함수 사용 불가(앱 생성 PK라 영향은 작다). ⚠️ `VARCHAR2` 상한 **4000바이트** ⇒ §5-(B) CLOB이 **필수**가 됐다. 12c 전용 문법 수정은 별건(`NEXT.md` 항목 9) |
| 5 | 인프라 | **WebLogic 버전** (12.2.1.x = Servlet 3.1 / 14.1.1 = Servlet 4.0) | ✅ **회신 2026-08-31 — `12.2.1.4`** | **Servlet 3.1 확정.** 가정하던 값 그대로라 `weblogic.xml`·비동기 스트리밍(Task 1.2·4.3)에 **바뀌는 것이 없다. 이 항목 종료** |
| 5-1 | 인프라 | **[신설 2026-08-28] CommonJ WorkManager** JNDI 조회 이름 · 전용 정의 필요 여부 · 시험 환경 | 🔴 **미회신** | 구현·폴백은 이미 있다. **폴백인 채 운영에 오르면 WAS 스레드 풀 고갈 장애** |
| 6 | 인프라 | ~~**[신설] 경유지 OAuth 토큰 발급 규격**~~ — 단 ⓒ(**뒤쪽 서비스가 토큰을 재검증해야 하는가**)는 남는다 | 🟡 **회신 — LLM 목적으로는 해당 없음** | 나가는 호출이 사라져 §6-C의 토큰 계층은 **보류**. `TokenProvider`는 지우지 않는다. ⓒ가 "필요"로 오면 **인증이 통째로 새로 생기는 별도 과제** |
| 6-1 | 인프라 | **[신설 2026-08-25] 신규 서비스의 API 통합관리시스템 등록 절차** | 🔴 **미회신** | 등록이 끝나야 연동 시험을 시작한다 — 일정에 직접 영향 |
| 6-2 | 인프라 | **[신설 2026-08-25] 게이트웨이 공개 API 표준 규격 문서** | 🔴 **미회신** | 현 API가 시험용 형태라 규격 맞춤 변경 개발 분량이 여기서 정해진다 |
| 7 | 인프라 | **[개정] 경유지의 스트리밍 대응** — 응답 버퍼링·유휴 타임아웃 설정과 변경 요청 창구 | 🟡 **회신 — 앞단 웹서버 없음(web-was 일체형)** | 사용자 유입 경로의 버퍼링·타임아웃 걱정은 **사라진다**. ⚠️ 다만 층위가 다를 수 있다(경유지=나갈 때 / 앞단 웹서버=들어올 때) — **경유지가 스트리밍 경로에 끼어 있는지**만 확인하면 닫힌다 |

**개정으로 사라진 항목**: 종전 5번 "eGovFrame 표준 템플릿의 `web.xml`/공통 필터가
`async-supported`를 켜두는가". eGovFrame 폐기로 **`web.xml`을 우리가 소유**하게 되어
물을 필요가 없어졌다 — 우리 필터 체인에 `async-supported=true`를 직접 명시하면 끝이다.
그 자리에 위 6번(경유지 OAuth)이 들어갔다.

**7번의 성격이 두 번 바뀌었다.** 처음에는 "앞단에 웹서버를 두는 구성인가"라는 유무
확인이었고, 2026-08-25에 경유지 확정으로 "설정값과 변경 창구"로 내려왔다. **2026-08-30
회신은 앞단 웹서버가 아예 없다(web-was 일체형)고 답했다** — 사용자 유입 경로의 걱정이
사라진다. 남은 것은 경유지가 그 경로에 끼어 있는지 한 가지뿐이다.

~~**여전히 2·3번만 설계 자체를 바꾼다.**~~ ~~**개정 2026-08-30 — 이제 3번 하나다.**~~
🔴 **재개정 2026-08-31 — 2번이 다시 열렸다.** 8/30에 자체 호스팅으로 닫았던 근거가
SPARC 때문에 사라져서, **2번(임베딩)은 이제 1-2에 종속된 항목**이 됐다.

**막고 있는 것은 3번과 1-2 둘**이고 성격이 다르다 — **3번은 설계 구조**가 갈리고
(§6-A 1안/2안), **1-2는 기능 범위**가 갈린다(LLM 존폐 + 벡터 검색 존폐).
⚠️ **둘이 서로 영향을 준다**: 1-2가 ⓒ(못 받음)로 오면 **전문검색이 유일한 검색
수단**이 되므로 3번의 답이 검색 품질 전체를 결정하게 된다.

**1·6번은 여전히 Java 재작업 0이다** — 어댑터 경계(§3 원칙 3) 덕에 바뀌는 것은
"무엇을 구현하느냐"뿐이고, 이번 SPARC 건에서도 그대로였다.

## 8. 단계 분할과 검증 방법

각 단계는 그 자체로 화면에서 확인 가능한 상태로 끝난다.

1. **골격** — WAR 구조, `web.xml`, JNDI DataSource, 스키마 DDL, 정적 화면 서빙.
   *끝난 모습:* 대시보드가 뜨고 데이터는 비어 있다.
2. **조회 REST** — institutions / bidcases / documents / notifications / accounts.
   MyBatis Mapper + 시드 데이터 이관. *끝난 모습:* 지도·목록·상세가 현재와 같이 보인다.
3. **검색** — 청크·벡터 Oracle 이관, Oracle Text(또는 폴백) + 하이브리드 랭킹.
   *끝난 모습:* 지식 탭이 동작한다.
4. **오케스트레이터** — 상태머신 + WorkManager + 채팅 스트리밍 + 결재 재개.
   *끝난 모습:* 워크플로 탭에서 실행·중단·결재·재개가 된다.
5. **산출물** — PDFBox RFP 파싱, POI PPTX 생성, 검증·일관성 체크.
   *끝난 모습:* PPTX가 떨어지고 검증 결과가 표시된다.

**검증은 "같은 입력 → 같은 출력" 회귀 비교로 한다.** 이관 전에 현재 Python 시스템에서
주요 API 응답과 산출물을 **골든 파일로 떠 두고**(단계 0 작업), Java 쪽 결과를 그것과
비교한다. 프런트가 안 바뀌므로 이 비교가 통과하면 화면도 통과한다. 의도적으로 달라지는
곳(§6-A 검색 랭킹)은 골든 파일에 차이를 기록하고 수용 근거를 남긴다.

JUnit 테스트는 63개 pytest 파일을 1:1로 옮기지 않는다. **REST 계약 테스트(MockMvc) +
Mapper 테스트 + 오케스트레이터 상태전이 테스트**에 집중하고, Oracle 의존 테스트는 H2의
Oracle 호환 모드로 대체하지 않는다(§5의 빈 문자열·CLOB 차이가 바로 그 지점에서 갈리므로,
H2로 통과한 테스트는 오히려 잘못된 안심을 준다).

**개정 2026-08-26 — 어디서 무엇을 검증하는가** (문서 상단 5축 표). 종전의 "개발용 Oracle
인스턴스를 전제한다"는 이제 가정이 아니라 사실이다 — **내부망은 로컬(`local`)부터 이미
Oracle이다.**

| | 외부망 로컬 (`out-local`, MySQL) | 내부망 (`local`/`dev`/`stg`, Oracle) |
|---|---|---|
| REST 계약(MockMvc) + 골든 비교 | ✅ 여기서 대부분 돈다 | ✅ |
| 오케스트레이터 상태전이 | ✅ | ✅ |
| **Mapper·DDL 정합성** | ❌ **근거 없음** | ✅ **여기서만 유효** |
| Oracle Text 전문검색(§6-A) | ❌ 등가물 없음 | ✅ |

⚠️ **MySQL 통과는 Oracle 정합성의 근거가 되지 않는다** — H2를 금지한 것과 정확히 같은
이유다(§5의 빈 문자열→NULL, CLOB 정렬 제약이 MySQL에서는 드러나지 않는다). 외부망 로컬은
**로직·계약을 빨리 돌려 보는 자리**이지 DB 계층의 합격 판정 자리가 아니다. 스키마
**정본은 Oracle DDL**이고 MySQL DDL은 그로부터 파생된 **미러**다(구현계획 Task 1.3).

## 9. 하지 않는 것 (YAGNI)

- **벡터DB 도입 안 함** — 코퍼스 5.6MB 규모에서 순수 낭비다.
- **LangChain4j / langgraph4j 도입 안 함** — §6-C.
- **Lucene 도입 안 함** — §6-A.
- **화면 프레임워크 도입 안 함** — 현재 화면이 잘 돌고 브라우저 제약도 없다.
- **날짜 타입 승격, PII 집계 구조 개선(NEXT.md 항목 7) 같은 개선은 이관에 섞지 않는다** —
  이관은 "동작 동일"이 유일한 성공 기준이어야 회귀 비교가 성립한다.
- **WAS 교체(WebSphere) 검토 — 지금은 안 한다.** 아래 별도 절.

### WAS 교체 검토 (WebSphere) — 🟡 **보류, 이관 완료 후 판단** (2026-08-31 신설)

- **출처**: **인프라 담당자가 "한번 고려해 보라"고 제안**했다(2026-08-31). 우리가
  꺼낸 안이 아니고, **확정된 요구도 아니다.**
- **결정: WebLogic으로 먼저 끝까지 돌려 보고 그때 판단한다**(사용자 확정).
  **지금 검토하면 변수가 둘이 된다** — 이관의 유일한 성공 기준이 "동작 동일"이고
  그 판정이 골든 비교인데, WAS까지 같이 흔들면 **차이가 이관 때문인지 WAS 때문인지
  구분되지 않는다.** 기준선을 먼저 세우는 것이 순서다.
- **다시 조사하지 않아도 되도록 영향 범위를 남긴다** — 결론은 **"이관 관점에서는
  놀랄 만큼 안 다르다"** 이다.

  | 영역 | 영향 |
  |---|---|
  | Servlet/JSP 표준 · WAR 구조 · `SpringBootServletInitializer` | 🟢 **그대로** — 손댈 것 없다 |
  | JNDI DataSource | 🟢 **이름만 바뀐다**, 코드 동일 |
  | Oracle 11g · JDK 8 · MyBatis · PDFBox/POI | 🟢 **무관** |
  | 배포서술자 | 🟡 `weblogic.xml` → **`ibm-web-ext.xml` / `ibm-web-bnd.xml`** (Task 1.2 재작성). **클래스로더 우선순위**(`prefer-application-packages` 대응)가 가장 성가시다 |
  | **CommonJ WorkManager** | 🟡 **유일하게 실코드가 바뀌는 곳** — WebLogic `commonj.work.WorkManager` ↔ WebSphere **`com.ibm.websphere.asynchbeans.WorkManager`**. API는 유사하나 패키지·JNDI 이름이 다르다 (Task 4.2에 어댑터 한 겹) |
  | 비동기 스트리밍(Task 4.3) | 🟢 Servlet 3.1 표준이라 동일 |

- **규모 감각**: **Task 1.2 다시 쓰기 + Task 4.2 어댑터 한 겹**, 며칠 규모다.
  eGovFrame 건처럼 스택 전체가 흔들리는 종류가 **아니다.**
- ⚠️ **오해하지 말 것 — WAS를 바꿔도 지금 막힌 것은 안 풀린다.** 2026-08-31에 LLM을
  막은 것은 WAS가 아니라 **OS·아키텍처(Solaris/SPARC)** 다. **WebSphere로 바꿔도 같은
  서버면 똑같이 막힌다.** 두 사안은 무관하므로 섞어서 판단하지 말 것.
- **되살아나는 조건**: 이관이 끝나 골든 비교가 통과한 뒤, 그때도 인프라 쪽 요구가
  유지되면 위 표를 출발점으로 착수한다.
