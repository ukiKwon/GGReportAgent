# 전국 지자체 금고 입찰 일정 — 기본 데이터 설계 (2026-08-09)

## ① 무엇을 왜 만드는가

대시보드의 전국 지도가 v1에서 **서울·경기 두 곳만 활성**이고 나머지 15개 시·도는
빗금('준비중')이었다. 그 지도에 실제로 칠할 **전국 광역 17개 시·도의 금고 입찰 일정**을
기본(default) 데이터로 넣는다. 사용자 표현으로 "기틀을 마련하는 작업"이며, 이후에는
같은 방식의 요청 없이 이 데이터가 계속 쓰인다.

**1차 범위는 광역 17곳으로 한정한다.** 기초 226곳까지 한 번에 하면 대부분이 근거 없는
추정치가 되어 "지도는 화려한데 못 믿는 데이터"가 된다. 광역은 보도가 충분해 근거를
붙일 수 있고, 기초 지자체 주기를 추정할 때의 기준선이 된다.

## ② 조사에서 확인된 사실 (설계의 전제)

### 법·제도

- 근거: **지방회계법 제38조 · 같은 법 시행령 제48조 · 행정안전부 예규
  「지방자치단체 금고지정 기준」(예규 제71호)**.
- ⚠️ **약정기간은 "4년 이내에서 각 지자체 조례·규칙으로 정한다"** — *무조건 4년이 아니다.*
  실제로 **경상남도는 3년**(2026-01-01~2028-12-31)이다. 전국 일괄 4년 가정은 금지한다.
- **"금고의 약정기간은 부득이한 경우를 제외하고 회계연도를 나누지 아니한다"** → 약정은
  대체로 1/1 시작 ~ 12/31 종료. ⚠️ 다만 **경기도는 4/1~3/31**로 이 원칙의 예외다.
- 대구광역시 조례에는 **"약정 만료 6개월 전까지 공고, 만료 90일 전까지 지정 완료"**
  절차 기한이 있다. 날짜를 특정해 주지는 않지만 공고 시점의 상한을 준다.

### 공고가 실제로 게시되는 곳

- **나라장터(G2B)는 이 건에서 신뢰할 수 없는 소스다.** 금고는 지방계약법상 '계약'이
  아니라 지방회계법상 **'지정'**이라 각 지자체 **고시·공고란**에 게시된다. 조사 과정의
  검색에서 나라장터 결과는 **0건**이었다.
- 통합 공개 데이터셋도 없다. 공공데이터포털의
  `행정안전부_지방재정365_금고지정현황`은 **11행짜리 집계표**라 자치단체별 일정이 없다.
- ⇒ 확인 경로는 ⓐ보도 ⓑ지자체 보도자료·공고 ⓒ자치법규(조례)의 세 겹이다.

### 행정구역 변동 (2026)

- **2026-07-01 전남광주통합특별시 출범** — 광주광역시와 전라남도가 통합됐다.
  통합 첫 금고는 1금고 NH농협은행 · 2금고 광주은행이며 **운영기간이 6개월**
  (2026-07-01~2026-12-31)이고, **차기 금고 공개모집이 2026년 10월경 예정**이다.
  지도(`geo/korea.js`)는 여전히 29(광주)·46(전남)을 별개 폴리곤으로 갖고 있다.

## ③ 데이터 채우기 규칙 (사용자 확정)

지어내지 않는 것이 이 데이터셋의 존재 이유다. 규칙은 사용자와 합의해 아래로 고정했다.

| 필드 | 넣는 값 | 요건 |
|---|---|---|
| `lastBid` | 직전 회차의 **확인된 절차일** | 공고일을 특정할 수 있으면 공고일, 없으면 지정(선정)일 |
| `term` | 그 지자체의 **실제** 약정주기(년) | ⓐ 과거 2회 이상 회차로 주기 확인, 또는 ⓑ 그 지자체 조례의 약정기간 조항 |
| `contractEnd` | 지금 공고가 **실제로 뜬 건만** | 복수의 독립 매체 또는 공식 발표로 날짜 특정 |
| `confirmed` | 위 `contractEnd`가 있을 때만 `true` | 그 외 전부 `false` |
| — | 위를 못 채우면 **날짜 전부 비움** | `name`/`type`/`region`만 남긴다 |

- **월/일은 직전 회차 값을 그대로 재사용한다.** 이건 화면이 이미 하고 있다 —
  `logic.effectiveBid`가 `logic.addYears(lastBid, term)`을 쓰는데 이 함수는
  **월/일을 보존하고 연도만 더한다.** 즉 코드 변경 없이 규칙이 성립하고, 결과는
  `(추측)`으로 표시된다. 내가 날짜를 만드는 게 아니라 앱의 공개된 규칙이 유도한다.
- 날짜를 비운 레코드는 `logic.recordGlyph`가 `?`를 붙이고 지도에서 회색으로 남는다.
  **"모른다"가 화면에 드러나는 것이 이 설계의 의도된 결과**다.
- 아는 사실(약정 만료 연도, 금고 은행, 규모 등)은 날짜를 못 채워도 `sources`에
  문장으로 남긴다 — 다음 조사가 처음부터 다시 하지 않게.

### ⚠️ 승인된 규칙에서 벗어난 지점 1건

사용자와 합의한 원문은 `lastBid` = **직전 "공고일"** 이었다. 그러나 실제 조사에서
**공고일의 월/일까지 특정된 곳은 서울·인천 둘뿐**이고, 나머지는 보도가 **지정(선정)일**
또는 **제안서 접수 마감일**만 전한다. 이 경우 날짜를 통째로 비우면 그 시·도는 지도에서
아무 신호도 못 준다.

그래서 **공고일이 없으면 지정일/접수마감일을 쓰되, 어느 쪽인지를 `sources` 첫 항목에
`기준일=…` 로 반드시 밝힌다.** 어느 값도 지어내지 않았고 전부 출처가 있다. 공고→지정은
통상 1~2개월 차이라 "언제쯤 움직이나"의 실용적 정밀도는 유지된다.
**이 편차는 사용자에게 보고했고, 원치 않으면 해당 행을 비우면 된다.**

## ④ 산출물

| 파일 | 역할 |
|---|---|
| `dashboard/data/institutions.js` | **정본.** 서버·업로드 없이 `file://`로도 뜨는 기본 데이터 |
| `dashboard/data/institutions_시도금고.csv` | 같은 내용의 12열 CSV. `collector/SCHEMA.md`의 반입 형식과 같은 열이라 수집기가 그대로 이어받는다 |
| `dashboard/test/institutions_data.test.js` | 데이터 무결성 회귀 테스트 11건 |

**정본을 서버 registry로 두지 않은 이유**: `serverdata.js`의 `CREATE_MAP`은 6개 필드만
보내고 registry에는 `sources`·`confirmed` 컬럼이 **아예 없다.** 이번 작업은 근거가 곧
가치이므로 거기 넣으면 출처 링크가 통째로 사라진다. 번들·CSV는 12필드를 전부 담는다.

CSV는 정본에서 파생시킨다(손으로 이중 관리하지 않는다). 회귀 테스트가 CSV 왕복
보존을 검증하므로, 정본을 고치면 같은 변환으로 CSV를 다시 뽑으면 된다.

## ⑤ 화면 변경 (`dashboard/js/render.js`)

1. `state.activeRegions`를 `['11','41']` → **17개 시·도 전부**로 확대.
   비활성 지역은 빗금 처리되고 **랭킹 집계에서도 빠지므로**, 데이터만 넣고 여기를
   안 고치면 화면에 아무것도 안 나타난다.
2. `REGION_NAME`에 **17개 이름을 전부** 채움. 없으면 breadcrumb·지역 카드·관심 핀·
   전체목록이 지역명 대신 `26` 같은 **코드를 그대로 노출**한다.
3. **`hasSubGeo(code)` 신설 — 면 색칠과 드릴인을 분리.**
   v1은 활성 지역이 서울·경기뿐이라 "색칠 대상"과 "드릴인 가능"이 우연히 일치했다.
   전국 확대 후에는 17곳이 전부 색칠되지만 구/시군 GeoJSON은 여전히
   `geo/{seoul,gyeonggi}.js` 둘뿐이라, 조건을 묶어 두면 나머지 15곳이
   **눌리는데 오류 배너만 뜨는 막다른 길**이 된다. 지도 클릭·커서와 지역 카드
   클릭을 `hasSubGeo` 기준으로 바꾸고, 카드 문구도 `구/시군 경계 준비중`으로 표시.

**지도 렌더링 로직(`render.js`의 좌표·투영·색 계산)은 건드리지 않았다** — 활성 지역
집합, 이름 표, 클릭 게이트만 바꿨다.

## ⑥ 결과 (2026-08-09 기준)

- **날짜 확보 9곳**: 서울 · 부산 · 인천 · 대전 · 울산 · 경기 · 강원 · 경남 · 제주
- **날짜 미확보 8곳**(지역만 표시, 회색 `?`): 대구 · 광주 · 세종 · 충북 · 충남 ·
  전북 · 전남 · 경북
- 임박도: **인천 red**(2026-08-06 공고, 진행 중) · **울산 yellow**(2027-09-25 추측) ·
  나머지 7곳 blue · 8곳 gray

## ⑦ 남은 일

1. **미확보 8곳의 공고일 특정** — 각 지자체 고시·공고란을 직접 훑어야 한다.
   그중 **세종·충북·경북·전남광주는 2026년 중 공모**로 확인돼 우선순위가 높다.
2. **전남광주통합특별시 처리 결정** — 지도는 29·46을 별개로 갖고 있는데 실체는 하나다.
   현재는 두 레코드가 같은 통합 일정을 각각 들고 있고, 그 사실을 `sources`에 적어 뒀다.
   폴리곤 병합 여부는 사용자 결정 사항.
3. **기초 226곳 확대** — 이 문서의 규칙을 그대로 쓰되, 광역에서 확인된 주기가
   기초 추정의 기준선이 된다. 별도 스펙·별도 세션.

---

## ⑧ 2단계 — 서울 25개 자치구 (2026-08-09 추가)

사용자 지시 순서는 **서울 → 경기 → 부산**이다. 이 절은 그중 서울분이다.

### 25개 구 공통 사실 (개별 공고일 없이도 확실)

- **약정 2023-01-01 ~ 2026-12-31(4년)이 25개 구 공통**이고 **올해 말 일제히 만료**된다.
- **2026년 하반기 순차 입찰** 예정. 총 16조 규모.
- 2022년 회차 진행 패턴: **8월 공고 → 9월 중순 접수 → 10월 초 선정**
  (10월 말까지 23개 구가 선정 완료).
- 현 금고: **우리 14 · 신한 6 · 국민 5** — 구별 배정은 `institutions.js`의 `SEOUL_GU` 표에 있다.

### 개별 공고일 확보 현황 — 2/25

| 구 | 값 | 근거 |
|---|---|---|
| 서대문구 | `lastBid` 2022-10-06 | 구청 **금고지정 결과 공고 원문** |
| 송파구 | `lastBid` 2022-09-01 | **구의회 회의록**(2022-08-29) — 접수 9/1~9/7, 9월 말 지정 |
| 성동구 | 날짜 없음(주기만) | 2018년 회차 **계획 공고 원문** → 2018·2022 두 회차로 4년 주기 실증 |
| 나머지 22곳 | 날짜 없음 | 미확보 |

### ⚠️ 조사 경로에 대해 알아낸 것 (다음 세션이 같은 벽에 부딪히지 않게)

**웹 검색으로는 자치구 공고일을 얻을 수 없다.** 실측 결과:

| 시도한 경로 | 결과 |
|---|---|
| 구 이름 + 금고 키워드 일반 검색 (5개 구) | **0건** — 구청 공고가 검색엔진에 거의 색인되지 않는다 |
| `allowed_domains`로 구청 도메인 한정 검색 (7개 구) | **0건** — 게시판 목록 페이지만 잡히고 공고 본문은 안 잡힌다 |
| 검색에 우연히 걸린 공고 URL을 WebFetch | **성공** — 서대문·성동 원문을 실제로 열었다. 경로는 있으나 **URL 발견이 병목**이다 |
| **구의회 회의록** 검색 | **성공(송파)** — 금고 일정이 소관 상임위 회의록에 자주 나온다. 가장 수율 높은 벡터 |
| eminwon 목록 API에 제목 파라미터(`not_ancmt_sj=금고`) 직접 호출 | **HTTP 500** — 세션·토큰이 필요해 무상태 GET으로는 안 된다 |

**⇒ 다음에 이 작업을 이어받으면 웹 검색부터 다시 하지 말 것.** 실효성 있는 순서는
① 구의회 회의록(`council.{구}.go.kr`) 검색 → ② 구청 고시공고 게시판을 브라우저 자동화로
직접 조작(검색어 입력 + 목록 페이징). ②는 `claude-in-chrome`으로 가능하나 구마다 게시판
구조가 달라 구당 수 분이 든다.

**지금이 시점상 중요하다** — 2022년 패턴대로면 **2026년 8~9월에 공고가 실제로 뜬다.**
지금 훑으면 과거 공고일 추정이 아니라 **진행 중인 실제 공고(확정)** 를 잡을 수 있다.

### 남은 일 (⑦의 항목에 추가)

4. **서울 22개 구의 공고일 확보** — 위 방법 ①②로. 2026년 공고가 뜨는 시기라 우선순위 높음.
5. **경기 31개 시군** — `geo/gyeonggi.js`가 이미 드릴인을 지원한다(단 수원·성남·안양·안산·
   고양·용인은 일반구 단위로 쪼개져 있어 시 단위 레코드를 어느 폴리곤에 붙일지 결정 필요).
6. **부산 16개 구·군** — ⚠️ **`geo/busan.js`가 없다.** 데이터를 넣어도 드릴인 화면은
   생기지 않고 전국 지도의 부산 면 색·랭킹·전체목록에만 반영된다. 경계 GeoJSON 확보가
   선행되어야 완전해진다.

---

## ⑨ 조사 경로 확립 — eminwon 고시공고 직접 질의 (2026-08-10)

§⑧에서 "웹 검색으로는 못 얻는다"고 적었던 문제가 **풀렸다.** 서울 자치구 대부분이
쓰는 eminwon 고시공고 게시판에 **제목 검색 파라미터가 있고, 브라우저에서 같은 출처로
fetch하면 목록이 그대로 나온다.**

### 방법 (재현 절차)

1. 대상 구의 eminwon 호스트로 **navigate** 한다(같은 출처를 만들기 위해서다 —
   다른 출처에서 fetch하면 CORS로 막힌다).
2. 그 페이지에서 아래를 실행한다.

```js
const p = new URLSearchParams({
  context:'NTIS', jndinm:'OfrNotAncmtEJB',
  method:'selectListOfrNotAncmt',            // ← 목록. 상세는 selectOfrNotAncmt
  methodnm:'selectListOfrNotAncmtHomepage',
  homepage_pbs_yn:'Y', subCheck:'Y', not_ancmt_se_code:'01,02,03,04,05,06,07',
  not_ancmt_sj:'금고', s_gubun:'글제목',      // ← 제목 검색어
  ofr_pageSize:'50', pageIndex:'1', initValue:'Y', title:'고시공고',
  countYn:'Y', dep_se:'%'
});
await fetch('/emwp/gov/mogaha/ntis/web/ofr/action/OfrAction.do?' + p, {credentials:'include'});
```

- ⚠️ **`method` 이름을 틀리면 HTTP 500이 난다**(§⑧에서 막혔던 원인). 정확한 이름은
  상세 페이지의 인라인 스크립트에 `method.value = 'selectListOfrNotAncmt'`로 들어 있다.
- ⚠️ **URL로 직접 navigate하면 검색어가 먹지 않는다**(`initValue=Y`가 초기화한다).
  반드시 navigate → fetch 2단계로 해야 한다.

### 확인된 eminwon 호스트 (2026-08-10 실측, DNS 기준)

| 구 | 호스트 | 구 | 호스트 |
|---|---|---|---|
| 종로 | `jongno.eminwon.seoul.kr` | 양천 | `eminwon.yangcheon.go.kr` |
| 중구 | `eminwon.junggu.seoul.kr` | 강서 | `eminwon.gangseo.seoul.kr` |
| 용산 | `eminwon.yongsan.go.kr` | 구로 | `eminwon.guro.go.kr` |
| 성동 | `eminwon.sd.go.kr` | 금천 | `eminwon.geumcheon.go.kr` |
| 광진 | `gwangjin.eminwon.seoul.kr` | 영등포 | `eminwon.ydp.go.kr` |
| 동대문 | `eminwon.ddm.go.kr` | 동작 | `dongjak.eminwon.seoul.kr` |
| 중랑 | `eminwon.jungnang.go.kr` | 관악 | `eminwon.gwanak.go.kr` |
| 강북 | `eminwon.gangbuk.go.kr` | 서초 | `eminwon.seocho.go.kr` |
| 도봉 | `dobong.eminwon.seoul.kr` | 강남 | `gangnam.eminwon.seoul.kr` |
| 은평 | `eminwon.ep.go.kr` | 송파 | `songpa.eminwon.seoul.kr` |
| 서대문 | `eminwon.sdm.go.kr` | 강동 | `eminwon.gangdong.go.kr` |
| 마포 | `eminwon.mapo.go.kr` | | |

**노원·성북은 eminwon 호스트를 못 찾았다** — 자체 CMS이거나 다른 이름이다. 별도 조사 필요.

### 남은 두 개의 벽

1. **브라우저 확장의 도메인 권한.** `eminwon.junggu.seoul.kr`·`gwangjin.eminwon.seoul.kr`은
   *Navigation to this domain is not allowed*로 막혔다. **사용자가 확장 프로그램에서
   해당 도메인을 허용해야** 진행된다. 허용된 곳(강서·강남·종로·용산)은 즉시 동작했다.
2. **오래된 공고가 목록에 없는 구가 있다.** 종로·용산은 2026년 금리 공고 1건만 나오고
   `[이전 자료 보기]` 링크가 따로 있다. 그 구들은 아카이브 조회를 한 단계 더 타야 한다.

### 이 방법으로 확보한 것 (2026-08-10)

- **강남구 — `contractEnd` 2026-07-30 확정.** 「서울특별시 강남구 금고지정 계획 공고」
  (공고 제2026-1843호). **2026년 회차가 이미 시작됐다는 첫 실물 증거다.**
- **강서구 — `lastBid` 2022-10-18**(지정 결과공고). 같은 게시판에서 2014-11-05 ·
  2018-10-24 지정공고도 확인돼 **4년 주기가 3회차로 실증**됐다.

⇒ 서울 자치구 공고일 확보 **4/25**(강남·강서·서대문·송파). 나머지는 위 두 벽만 걷히면
같은 절차로 기계적으로 채울 수 있다.

---

## ⑩ 서울 22개 구 전수 조사 완료 (2026-08-10)

§⑨의 방법을 사용자 승인(도메인 확장 권한 허용) 후 남은 22개 구 전부에 적용했다.
**23/25구 조회 완료** — 노원·성북 2곳만 경로 문제로 미완.

### 결과 — 날짜 확보 9/25 (2026년 진행 중 공고 3건 포함)

| 구 | 값 | 근거 |
|---|---|---|
| **강남** | `contractEnd` 2026-07-30 확정 | 금고지정 계획 공고 제2026-1843호 |
| **동대문** | `contractEnd` 2026-07-30 확정 | 금고지정 계획 공고. 2022년 회차(계획 08-18→결과 10-27, **70일 소요**)도 같은 게시판에서 확인 |
| 강서 | `lastBid` 2022-10-18 | 2014·2018·2022 **3회차** 확인 — 4년 주기 최다 실증 |
| 서대문 | `lastBid` 2022-10-06 | 결과 공고 원문 |
| 송파 | `lastBid` 2022-09-01 | 구의회 회의록 |
| 금천 | `lastBid` 2022-10-20 | 2018·2022 2회차 확인 |
| 강북 | `lastBid` 2022-10-07 | 결과 공고 |
| 성동 | 날짜 없음(주기만) | 2018년 계획 공고(§⑧에서 확인) |
| 도봉·은평 | **날짜 없음(의도적)** | 아래 참조 |

### ⚠️ 실제로 낸 실수와 고친 방법 — 회귀 테스트로 고정함

도봉(2014년 회차)·은평(2018년 회차)은 게시판에서 회차를 찾긴 했지만 **2022년
회차가 없었다.** 그 값을 그대로 `lastBid`에 넣었더니 `addYears`가 term을 **한 번만**
더해 도봉은 2018년, 은평은 2022년 — **둘 다 이미 지난 날짜**가 나왔다. 결과적으로
"곧 만료"가 아니라 **"이미 늦었다"는 잘못된 빨간불**이 뜨는 것이었는데, 25개 구가
전부 2023~2026 약정(§⑧ COMMON)인 걸 이미 알고 있으므로 명백히 틀린 값이었다.

**규칙 위반이었다** — 중간 회차를 못 찾았으면 `lastBid`를 비워야 하는데, "그래도
회차 하나는 찾았으니까"로 오래된 값을 넣은 것이 원인이다. 두 구 모두 `lastBid`를
제거하고 사실은 `sources`에만 남기는 것으로 고쳤다.

**재발 방지**: `institutions_data.test.js`에 회귀 테스트를 추가했다 — `lastBid+term`으로
유도한 날짜가 조사 시점(2026-08-10)보다 **1년 넘게 과거로 나오면 실패**한다. 이 테스트가
없었다면 이 오류는 지도가 실제로 렌더링될 때까지, 혹은 사용자가 "왜 도봉이 이미
늦었다고 뜨지?"라고 묻기 전까지 발견되지 않았을 것이다.

### 음성 결과(지정 공고 없음) — 14/25구

종로·중구·용산·광진·성동(§⑧에서 확인) · 중랑·마포·양천·구로·영등포·동작·관악·서초·강동.
전부 `BOARD_CHECKED`에 조회 근거를 남겼다 — 다음 세션이 같은 구를 다시 조회하지
않도록. "게시판이 당해연도만 노출"(아직 공고 전으로 읽힘) vs "게시판에 과거분이
있는데도 없음"(다른 곳에 게시)을 구분해서 적었다.

### 미완 2곳 — 노원·성북

- **노원**: eminwon 호스트를 못 찾았다(다른 시스템으로 추정, DNS 후보 3개 전부 실패).
- **성북**: 자체 게시판(`www.sb.go.kr/www/selectEminwonList.do`)은 찾았으나 **제목 검색
  파라미터가 안 먹는다** — `searchKrwd`+`searchCnd` 조합을 시도했지만 최신순 전체
  목록(30,000건대)이 그대로 나온다. 페이징 없이는 비효율적이라 이번 회차는 보류.
  다음에 이어받으면: 검색 폼의 정확한 파라미터명을 다시 확인하거나, 최신순 목록을
  날짜 역순으로 페이징하며 "금고" 포함 행만 걸러야 한다.

### 최종 상태 (2026-08-10)

- 총 44건(광역 17 + 서울 25 + 갱신 2건은 기존 레코드 수정).
  ~~날짜 확보~~ **13 → 서울 9곳 추가 = 광역 9 + 서울 9 = 18곳.**
- **2026년 진행 중 공고 확정 2건** — 강남·동대문. 예상대로 8~9월에 공고가 뜨고 있다.
- node 227 → **228 passed**(회귀 테스트 1건 추가).

### 남은 일 (⑦·§⑧의 목록에 추가)

7. **노원·성북 경로 확보** — 위 미완 사유 참조.
8. **음성 14구를 9월 중순에 재조회** — §⑧에서 예측한 대로 "당해연도만 노출"인 구들은
   아직 공고 전일 뿐이므로, 접수철인 9월에 같은 절차를 반복하면 더 많이 잡힐 것이다.
9. **경기 31개 시군** — 다음 순서. 사용자 확정: 일반구로 쪼개진 6개 시(수원·성남·안양·
   안산·고양·용인)는 **일반구 폴리곤 전부에 같은 시 값을 붙인다**(ⓐ안). 같은 eminwon
   방식이 경기 시청에도 통하는지가 착수 시 첫 확인 사항.

---

## ⑪ 경기도 31개 시군 (2026-08-10, 사용자 승인 후 야간 자율 작업)

사용자 지시 순서(서울→경기→부산) 중 경기분. **웹 검색만으로 진행**했다 —
이유는 시간대 제약: 사용자가 잠든 사이 진행하는 작업이라, 서울처럼 브라우저
확장 도메인 허용을 실시간으로 요청할 수 없었다(승인해 줄 사람이 없다).
eminwon 직접조회(§⑨)는 다음 세션에서 브라우저 자동화로 이어갈 수 있는 자리로 남긴다.

### 경기는 서울과 근본적으로 다른 전제였다

서울 25구는 **전부 2023~2026 공통 약정**이라 "만료연도는 같다"는 사실만으로도
값어치가 있었다. 경기 31개 시군은 그렇지 않다 — 수원 2023~2026, 안성 2025~2028,
부천 ~2029 등 **시마다 독립적으로 주기가 어긋나 있다.** 그래서 "공통 사실"을
COMMON 블록으로 묶을 수 없고, 시마다 개별로 확인해야 했다.

### 결과 — 날짜 확보 3/31 시, 42개 폴리곤 레코드 중 6곳(수원 4구)이 확정 날짜

| 시 | 값 | 근거 | 비고 |
|---|---|---|---|
| **수원시** | `lastBid` 2022-08-19, term 4 | 기업은행 선정 보도 | ⚠️ 기준일=보도일(공고일 아님) |
| **부천시** | `lastBid` 2025-07-08, term 4 | 1금고 농협·2금고 기업은행 확정 보도 | ⚠️ 기준일=보도일 |
| **안성시** | `lastBid` 2024-05-10, term 4 | **약정체결식** 날짜(기사로 확인) | 약정 2025-01-01~2028-12-31 |
| 파주시 | term 4만 확정 | 시 조례(§③ ⓑ 요건 충족) | lastBid 없어 날짜는 안 뜬다 |
| 시흥시 | 날짜 없음 | 현재 금고=농협중앙회만 확인 | |
| 나머지 26곳 | **전부 날짜 없음** | 검색 자체가 안 걸림 | 근거 없이 채우지 않음 |

⚠️ **수원·부천은 "공고일"이 아니라 "보도일"을 기준일로 썼다.** §⑧에서 광역에
적용한 예외(공고일 없으면 지정일)를 한 단계 더 넓힌 것이다 — 보도에도 정확한
지정일이 안 나오고 기사 작성일만 확인되는 경우, 그 날짜를 근사치로 쓰되
`sources`에 **"기준일=보도일…근사치로 사용"** 이라고 명시했다. 안성은 다르다 —
"약정체결식 날짜"라는 구체적 이벤트가 있어 더 신뢰도가 높다(다만 이것도 공고일보다
늦은 시점이라고 밝혀 뒀다).

### ⓐ안(일반구 분할 도시) 실제 적용 결과

수원(4)·성남(3)·안양(2)·안산(2)·고양(3)·용인(3) = 17개 폴리곤이 6개 시 값을
반복해 담는다. **브라우저로 실측 확인**: 수원 4개 구가 지도에서 전부 붉게
칠해지고, 랭킹 패널에도 "수원시청"이 4번 연속 뜬다. ⚠️ **부수 효과 하나 발견**:
임박 TOP5 티커가 같은 시 이름으로 채워질 수 있다(수원 4개 구가 전부 상위권에
들어가면 TOP5 중 2자리를 수원이 차지) — 데이터 오류는 아니고 ⓐ안의 당연한
결과이지만, 다른 시가 랭킹에서 밀려나는 부작용이 있다. **고치지 않고 남겨둔다**
— 랭킹 중복제거는 데이터가 아니라 화면 로직 변경이라 이번 범위(기본 데이터
채우기) 밖이고, 사용자가 실제로 화면을 보고 판단할 문제다.

### 회귀 테스트 3건 추가/수정

기존 테스트 2개가 **경기 데이터를 넣자 잘못된 가정으로 드러나 고쳤다** —
"버그를 찾았다"가 아니라 "서울만 보고 세운 가정이 깨졌다"는 것이 정확한 표현이다.

1. **subRegion이 region 코드로 시작한다는 가정이 틀렸다.** 서울은 region '11' +
   subRegion '11XXX'로 우연히 접두사가 같았지만, 경기는 region '41' + subRegion
   '31XXX'(gyeonggi.js 자체 코드 체계)로 **다르다.** `render.municipalityForFeature`가
   실제로 쓰는 방식(그 region의 geo 파일에 있는 코드인지)으로 테스트를 다시 짰다.
2. **"모든 레코드는 URL이 있어야 한다"가 너무 강했다.** 서울까지는 "못 찾았다"고
   해도 조회 근거 URL(게시판 주소)이 항상 있었지만, 경기는 순수 웹 검색이라
   진짜 아무것도 안 나온 시가 26곳이다. URL을 강제하면 무관한 URL을 억지로
   채워 넣는 역효과가 생기므로, **날짜를 주장하는 레코드만 URL을 강제**하고
   날짜 없는 레코드는 "찾아봤다"는 문구(검색/조사/확인 등)로 대체하게 완화했다.
3. 경기 42개 폴리곤 코드 전량이 gyeonggi.js와 일치하는지 확인하는 커버리지
   테스트를 서울과 같은 패턴으로 추가했다.

### 최종 상태 (2026-08-10 야간)

- 총 **84개 레코드**(광역 17 + 서울 25 + 경기 42).
- node 228 → **229 passed**.
- 브라우저 실측: 전국 지도에서 경기가 붉게 칠해짐(수원의 임박도가 그 지역
  최임박값이라 면 전체 색을 끌어올림 — `regionUrgencyColor`가 지역 내 최임박
  지자체를 대표색으로 쓰는 기존 로직 그대로), 드릴인 시 수원 4구·성남 3구
  일관되게 렌더링 확인. 콘솔 오류 0건.

### 남은 일

10. **경기 eminwon 직접조회** — §⑨ 방식이 경기 시청에도 통하는지 확인 후
    26개 미확보 시를 이어서 채운다. 사용자가 깨어 있을 때(도메인 허용 필요) 진행.
11. **부산 16개 구·군** — ⚠️ `geo/busan.js`가 여전히 없다(§⑦-6에서 이미 지적).
    데이터를 넣어도 드릴인 화면은 안 생기고 전국 지도 면 색·랭킹에만 반영된다.
12. **랭킹 TOP5 중복 문제 검토** — 위 "부수 효과" 참조. 사용자가 실제로 보고
    거슬리면 그때 `sortByUrgency`/티커 로직에 시-단위 중복제거를 추가할지 결정.
