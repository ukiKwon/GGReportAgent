# 계획 C1 — 워크플로 현황판·배점표 매핑·overlay 재설계 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 오케스트레이터가 일하는 현장을 웹에서 본다 — 신규 "워크플로" 탭에 9단계 스테퍼·에이전트/담당자 카드·지시·보고 로그·run/결재 버튼과 배점표 매핑 뷰를 붙이고, B에서 드러난 편집 overlay 설계 결함(F4)을 바로잡는다.

**Architecture:** 스펙 `docs/superpowers/specs/2026-07-31-multi-agent-collab-system-design.md` §⑦의 화면 6종 중 ①상황판(기존)·③현황판·④배점표 매핑을 담당(C2가 ②대화창·⑤쪽지함·⑥지식탭). **기존 지도·탭은 건드리지 않는다** — `index.html`에 탭 버튼 1개와 `<section id="tab-workflow">` 1개를 추가하고, 로직은 신규 `dashboard/js/workflow.js`에 격리한다(render.js 무수정). 순수 로직(스테퍼 모델·카드 행·매핑 행)과 DOM 렌더를 한 파일 안에서 분리해 순수부만 `node --test`로 고정한다. 서버 모드 전용 — `file://` 폴백에서는 탭 자체를 숨긴다.

**Tech Stack:** vanilla JS(IIFE + `module.exports` 이중 노출), FastAPI, pytest, node:test.

## Global Constraints

- **무빌드·무의존** — npm 패키지 금지. 기존 IIFE 패턴(`dashboard/js/store.js:56`) 그대로.
- **지도·기존 탭 무수정** — `dashboard/js/render.js`는 한 줄도 고치지 않는다. `index.html`은 **추가만**(탭 버튼 1, 섹션 1, 스크립트 태그 1, 스타일 블록 1).
- **file:// 폴백 불변** — 서버 모드가 아니면 워크플로 탭 버튼·섹션을 숨기고, 기존 40건 node 테스트가 무수정 통과.
- 기존 API 계약(변경 금지): `GET /institutions/{id}/status` → `{stage, running, pending_gate, failed, tasks:[{team,status,progress_pct,assignee}], notifications_unread}`; `POST /institutions/{id}/run` → 202; `POST /institutions/{id}/checkpoint` body `{approved, comment, by}` + `X-User-Id` 헤더(**ASCII만** — 한글 이름은 body의 `by`로).
- 결재 게이트 3곳: `기획승인`(5)·`이관결재`(7)·`최종결재`(8). 되물음은 결재가 아니다.
- `coverage_map.json` 형태(A2 산출): `{ "<배점항목>": {team, covered, gap_note, pii_count} }`.
- 주석·커밋 한국어(끝에 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`), UTF-8, `py -3` 런처, TDD.
- 전체 `py -3 -m pytest agent backend collector -q` 기준선 **346 passed** 유지 + 신규, `node --test dashboard/test/*.test.js` **40** + 신규 통과.

---

### Task 1: F4 재설계 — 편집 overlay를 로컬 전용 필드로 한정

**Files:**
- Modify: `dashboard/js/logic.js`(상수 1개 추가), `dashboard/js/store.js`(`applyEdits`·`setEdit`)
- Test: `dashboard/test/store.test.js`(추가), `dashboard/test/logic.test.js`(상수 확인은 store 쪽에서만 해도 됨)

**Interfaces:**
- 문제(B 최종리뷰 F4): `store.setEdit`이 `logic.ALL_FIELDS` 전체를 localStorage overlay에 넣고 `applyEdits`가 서버 값 **위에** 덮어써서, 한 번 편집한 행은 이후 서버측 변경(타 사용자 CSV 반입 등)이 화면에 영영 반영되지 않는다 — "서버가 authoritative"와 충돌.
- Produces:
  - `logic.LOCAL_ONLY_FIELDS = ['subRegion','confirmed','lng','lat','sources','updatedAt']` — 서버 `Institution`에 대응 필드가 없는 것만(서버 필드는 `name·type·region·term·lastBid·contractEnd`).
  - `store.applyEdits(list)` — **서버 모드일 때만** overlay를 로컬 전용 필드로 필터링해서 적용. 폴백 모드는 기존 동작(전체 적용) 그대로 — file://에서는 overlay가 유일한 저장소라 한정하면 편집이 사라진다.
  - `store.setEdit(name, partial)` — 시그니처 불변(저장은 그대로, 적용 시점에 필터). 저장을 필터링하지 않는 이유: 서버↔폴백을 오가도 로컬 편집 이력이 보존돼야 하고, 폴백 모드에서 그 값이 여전히 유일한 진실이기 때문.

- [ ] **Step 1: Write the failing tests**

`dashboard/test/store.test.js` 하단에 추가(기존 파일의 store 로딩 관행을 먼저 읽고 동일하게 할 것 — `freshStore()` 류 헬퍼가 있으면 그것을 쓴다):

```js
test('applyEdits: 서버 모드에서는 서버 필드 overlay를 무시하고 로컬 전용 필드만 덮는다', function () {
  const store = freshStore();
  store.setEdit('도봉구', { contractEnd: '2020-01-01', sources: ['수기'] });
  store.setServerData([{ name: '도봉구', contractEnd: '2026-12-31' }]);

  const out = store.applyEdits(store.loadData());
  assert.strictEqual(out[0].contractEnd, '2026-12-31');      // 서버 값이 이긴다
  assert.deepStrictEqual(out[0].sources, ['수기']);           // 로컬 전용 필드는 유지
});

test('applyEdits: 폴백 모드에서는 기존대로 전체 overlay가 적용된다', function () {
  const store = freshStore();
  store.setEdit('도봉구', { contractEnd: '2020-01-01', sources: ['수기'] });

  const out = store.applyEdits([{ name: '도봉구', contractEnd: '2026-12-31' }]);
  assert.strictEqual(out[0].contractEnd, '2020-01-01');      // file://에선 overlay가 진실
  assert.deepStrictEqual(out[0].sources, ['수기']);
});
```

- [ ] **Step 2: Run to verify fail** — `node --test dashboard/test/store.test.js` → 첫 테스트 실패(overlay가 서버를 덮음).

- [ ] **Step 3: Implement**

`dashboard/js/logic.js`(ALL_FIELDS 선언 근처):

```js
  // 서버 registry에 대응 필드가 없는 것들 — 서버 모드에서도 localStorage overlay가
  // 유일한 저장처다. 서버 필드(name·type·region·term·lastBid·contractEnd)를 overlay가
  // 덮으면 타 사용자의 서버 변경이 화면에 영영 안 나온다(계획 B 최종리뷰 F4).
  logic.LOCAL_ONLY_FIELDS = ['subRegion','confirmed','lng','lat','sources','updatedAt'];
```

`dashboard/js/store.js`의 `applyEdits`:

```js
  store.applyEdits = function (list) {
    const e = store.loadEdits();
    const localOnly = (typeof logic !== 'undefined' && logic.LOCAL_ONLY_FIELDS) ||
      (typeof require !== 'undefined' ? require('./logic.js').LOCAL_ONLY_FIELDS : null);
    const serverMode = store.isServerMode();
    return list.map(function (r) {
      const patch = e[r.name];
      if (!patch) return r;
      if (!serverMode || !localOnly) return Object.assign({}, r, patch);
      const filtered = {};
      localOnly.forEach(function (f) {
        if (Object.prototype.hasOwnProperty.call(patch, f)) filtered[f] = patch[f];
      });
      return Object.assign({}, r, filtered);
    });
  };
```

⚠️ store.js는 브라우저에서 전역 `logic`을, node 테스트에서 `require`를 쓰게 된다 — 기존 파일이 다른 모듈을 참조하지 않는 구조라면, **의존을 만들지 말고** `LOCAL_ONLY_FIELDS`를 store.js 자체 상수로 두는 편이 낫다. 구현자는 기존 파일 구조를 보고 둘 중 단순한 쪽을 택하고 그 사유를 보고서에 적을 것(테스트가 규정하는 동작만 지키면 된다).

- [ ] **Step 4: Run to verify pass** — 신규 2 + 기존 전체 `node --test dashboard/test/*.test.js` 통과.
- [ ] **Step 5: Commit** — `fix(dashboard): 편집 overlay를 로컬 전용 필드로 한정 — 서버 값이 가려지던 F4 (C1 Task 1)`

---

### Task 2: `GET /institutions/{id}/coverage-map` — 배점표 매핑 데이터

**Files:**
- Modify: `backend/routers/institutions.py`(엔드포인트 1개)
- Test: `backend/tests/test_api_coverage_map.py`

**Interfaces:**
- Consumes: A2가 쓰는 `{output_root}/{name_ko}/coverage_map.json`, `{output_root}/{name_ko}/rfp_scoring.json`(배점표 원본 — 항목·배점·분류).
- Produces: `GET /institutions/{institution_id}/coverage-map` → `{"criteria": [{"category","item","score","team","covered","gap_note","pii_count"}], "total_score": int}`. 배점표가 없으면 `{"criteria": [], "total_score": 0}`(200 — 아직 3단계 전인 정상 상태). coverage_map이 없으면 team/covered는 null·false로 채운다. 기관 없으면 404.

- [ ] **Step 1: Write the failing tests**

`backend/tests/test_api_coverage_map.py`:

```python
import json

from fastapi.testclient import TestClient

from backend.db import get_connection
from backend.main import create_app


def _app(tmp_path):
    app = create_app(str(tmp_path / "r.db"), output_root=str(tmp_path / "out"),
                     graph_db_path=str(tmp_path / "g.db"))
    conn = get_connection(str(tmp_path / "r.db"))
    conn.execute("INSERT INTO institutions (institution_id, name_ko, stage) VALUES ('nowon','노원구',6)")
    conn.commit(); conn.close()
    return app


def _write(tmp_path, scoring=None, coverage=None):
    out = tmp_path / "out" / "노원구"
    out.mkdir(parents=True, exist_ok=True)
    if scoring is not None:
        (out / "rfp_scoring.json").write_text(json.dumps(scoring, ensure_ascii=False), encoding="utf-8")
    if coverage is not None:
        (out / "coverage_map.json").write_text(json.dumps(coverage, ensure_ascii=False), encoding="utf-8")


def test_merges_scoring_with_coverage(tmp_path):
    _write(tmp_path,
           scoring={"rfp_title": "공고", "total_score": 30, "criteria": [
               {"category": "사업", "item": "전산 시스템", "score": 20, "description": None},
               {"category": "기타", "item": "지역 기여", "score": 10, "description": None}]},
           coverage={"전산 시스템": {"team": "전산", "covered": True, "gap_note": None, "pii_count": 0}})
    client = TestClient(_app(tmp_path))

    body = client.get("/institutions/nowon/coverage-map").json()
    assert body["total_score"] == 30
    first, second = body["criteria"]
    assert first["item"] == "전산 시스템" and first["team"] == "전산" and first["covered"] is True
    assert second["item"] == "지역 기여" and second["team"] is None and second["covered"] is False


def test_no_scoring_returns_empty(tmp_path):
    client = TestClient(_app(tmp_path))
    body = client.get("/institutions/nowon/coverage-map").json()
    assert body == {"criteria": [], "total_score": 0}


def test_unknown_institution_404(tmp_path):
    client = TestClient(_app(tmp_path))
    assert client.get("/institutions/ghost/coverage-map").status_code == 404
```

- [ ] **Step 2: Run to verify fail** — 404/AttributeError.

- [ ] **Step 3: Implement** — `backend/routers/institutions.py`(기존 artifacts 엔드포인트 아래, 같은 `_conn`/`get_institution` 관행):

```python
@router.get("/{institution_id}/coverage-map")
def get_coverage_map(institution_id: str, request: Request):
    """배점표 항목 ↔ 팀 작성물 커버리지 병합 — 배점표 매핑 뷰(계획 C1)의 데이터원."""
    conn = get_connection(request.app.state.db_path)
    try:
        inst = get_institution(conn, institution_id)
    finally:
        conn.close()
    if inst is None:
        raise HTTPException(status_code=404, detail="institution not found")

    out_dir = os.path.join(request.app.state.output_root, inst.name_ko)
    scoring_path = os.path.join(out_dir, "rfp_scoring.json")
    if not os.path.isfile(scoring_path):
        # 아직 3단계(배점표 추출) 전 — 빈 상태가 정상이다.
        return {"criteria": [], "total_score": 0}
    with open(scoring_path, encoding="utf-8") as f:
        scoring = json.load(f)

    coverage = {}
    coverage_path = os.path.join(out_dir, "coverage_map.json")
    if os.path.isfile(coverage_path):
        with open(coverage_path, encoding="utf-8") as f:
            coverage = json.load(f)

    criteria = []
    for c in scoring.get("criteria", []):
        cov = coverage.get(c["item"], {})
        criteria.append({
            "category": c.get("category"), "item": c["item"], "score": c.get("score"),
            "team": cov.get("team"), "covered": bool(cov.get("covered", False)),
            "gap_note": cov.get("gap_note"), "pii_count": cov.get("pii_count", 0),
        })
    return {"criteria": criteria, "total_score": scoring.get("total_score", 0)}
```

(`json`·`os` import가 이미 있는지 확인하고 없으면 추가.)

- [ ] **Step 4: Run to verify pass** — 3 passed + `py -3 -m pytest backend -q` 무회귀.
- [ ] **Step 5: Commit** — `feat(backend): GET /institutions/{id}/coverage-map — 배점표↔작성물 매핑 (C1 Task 2)`

---

### Task 3: `workflow.js` 순수 로직 — 스테퍼·카드·매핑 행 모델

**Files:**
- Create: `dashboard/js/workflow.js`
- Test: `dashboard/test/workflow.test.js`

**Interfaces:**
- Produces(전역 `root.workflow` + `module.exports`):
  - `STAGES` — 9개 `{no, label, gate}`; gate는 5·7·8만 각각 `'기획승인'|'이관결재'|'최종결재'`, 나머지 null.
  - `stepperModel(status)` — `status`(= `GET /status` 응답)를 받아 `[{no,label,gate,state}]`. `state`는 `'done'`(no < stage) / `'current'`(no === stage) / `'todo'`. 단 `status.pending_gate`가 있고 그 게이트가 그 단계면 `'gate'`.
  - `runnable(status)` → bool — `!running && !pending_gate && stage < 9`.
  - `teamCards(status)` → `[{team, status, progress_pct, assignee, label}]` — `label`은 `assignee ? team + ' · ' + assignee : team + ' · 미배정'`. tasks 없으면 빈 배열.
  - `coverageRows(payload)` — coverage-map 응답을 `[{item, category, score, team, covered, gapNote, piiCount, state}]`로. `state`는 `covered ? 'ok' : (team ? 'gap' : 'none')`.
  - `coverageSummary(payload)` → `{total, covered, coveredScore, totalScore, piiTotal}`.

- [ ] **Step 1: Write the failing tests**

`dashboard/test/workflow.test.js`:

```js
const test = require('node:test');
const assert = require('node:assert');
const wf = require('../js/workflow.js');

test('STAGES: 9단계이고 결재 게이트는 5·7·8뿐', function () {
  assert.strictEqual(wf.STAGES.length, 9);
  const gates = wf.STAGES.filter(function (s) { return s.gate; }).map(function (s) { return [s.no, s.gate]; });
  assert.deepStrictEqual(gates, [[5, '기획승인'], [7, '이관결재'], [8, '최종결재']]);
});

test('stepperModel: 완료/현재/대기 구분', function () {
  const model = wf.stepperModel({ stage: 4, running: false, pending_gate: null, tasks: [] });
  assert.strictEqual(model[2].state, 'done');     // 3단계
  assert.strictEqual(model[3].state, 'current');  // 4단계
  assert.strictEqual(model[4].state, 'todo');     // 5단계
});

test('stepperModel: 결재 대기 중이면 해당 단계가 gate 상태', function () {
  const model = wf.stepperModel({ stage: 5, running: false, pending_gate: '기획승인', tasks: [] });
  assert.strictEqual(model[4].state, 'gate');
});

test('runnable: 실행 중·결재 대기·완료 상태에서는 실행 불가', function () {
  assert.strictEqual(wf.runnable({ stage: 2, running: false, pending_gate: null }), true);
  assert.strictEqual(wf.runnable({ stage: 2, running: true, pending_gate: null }), false);
  assert.strictEqual(wf.runnable({ stage: 5, running: false, pending_gate: '기획승인' }), false);
  assert.strictEqual(wf.runnable({ stage: 9, running: false, pending_gate: null }), false);
});

test('teamCards: 담당자 미배정 표시', function () {
  const cards = wf.teamCards({ tasks: [
    { team: '영업', status: '작성중', progress_pct: 30, assignee: null },
    { team: '전산', status: '1차완료', progress_pct: 100, assignee: 'it-user' },
  ] });
  assert.strictEqual(cards[0].label, '영업 · 미배정');
  assert.strictEqual(cards[1].label, '전산 · it-user');
});

test('coverageRows/Summary: 상태 분류와 합계', function () {
  const payload = { total_score: 30, criteria: [
    { category: '사업', item: '전산 시스템', score: 20, team: '전산', covered: true, gap_note: null, pii_count: 1 },
    { category: '기타', item: '지역 기여', score: 10, team: null, covered: false, gap_note: null, pii_count: 0 },
  ] };
  const rows = wf.coverageRows(payload);
  assert.strictEqual(rows[0].state, 'ok');
  assert.strictEqual(rows[1].state, 'none');

  const sum = wf.coverageSummary(payload);
  assert.deepStrictEqual(sum, { total: 2, covered: 1, coveredScore: 20, totalScore: 30, piiTotal: 1 });
});
```

- [ ] **Step 2: Run to verify fail** — MODULE_NOT_FOUND.

- [ ] **Step 3: Implement `dashboard/js/workflow.js`**(순수부만 이 태스크 — 렌더 함수는 Task 4에서 같은 파일에 추가):

```js
(function (root) {
  'use strict';
  // 워크플로 현황판의 순수 모델 (계획 C1). DOM·fetch는 아래 태스크에서 별도 함수로.
  const workflow = {};

  workflow.STAGES = [
    { no: 1, label: '입찰현황 파악', gate: null },
    { no: 2, label: '입찰상황 발생', gate: null },
    { no: 3, label: 'RFI 공시', gate: null },
    { no: 4, label: 'RFI 분석', gate: null },
    { no: 5, label: '제안서 기획', gate: '기획승인' },
    { no: 6, label: '세부기획(3팀)', gate: null },
    { no: 7, label: '취합', gate: '이관결재' },
    { no: 8, label: '검토', gate: '최종결재' },
    { no: 9, label: '제출', gate: null },
  ];

  workflow.stepperModel = function (status) {
    const stage = Number(status && status.stage) || 1;
    const pending = status && status.pending_gate;
    return workflow.STAGES.map(function (s) {
      let state = s.no < stage ? 'done' : (s.no === stage ? 'current' : 'todo');
      if (pending && s.gate === pending) state = 'gate';
      return { no: s.no, label: s.label, gate: s.gate, state: state };
    });
  };

  workflow.runnable = function (status) {
    if (!status) return false;
    return !status.running && !status.pending_gate && Number(status.stage) < 9;
  };

  workflow.teamCards = function (status) {
    const tasks = (status && status.tasks) || [];
    return tasks.map(function (t) {
      return {
        team: t.team, status: t.status, progress_pct: t.progress_pct,
        assignee: t.assignee || null,
        label: t.team + ' · ' + (t.assignee || '미배정'),
      };
    });
  };

  workflow.coverageRows = function (payload) {
    return ((payload && payload.criteria) || []).map(function (c) {
      return {
        item: c.item, category: c.category, score: c.score, team: c.team || null,
        covered: !!c.covered, gapNote: c.gap_note || null, piiCount: c.pii_count || 0,
        state: c.covered ? 'ok' : (c.team ? 'gap' : 'none'),
      };
    });
  };

  workflow.coverageSummary = function (payload) {
    const rows = workflow.coverageRows(payload);
    let covered = 0, coveredScore = 0, piiTotal = 0;
    rows.forEach(function (r) {
      if (r.covered) { covered += 1; coveredScore += Number(r.score) || 0; }
      piiTotal += r.piiCount;
    });
    return {
      total: rows.length, covered: covered, coveredScore: coveredScore,
      totalScore: (payload && payload.total_score) || 0, piiTotal: piiTotal,
    };
  };

  if (typeof module !== 'undefined' && module.exports) module.exports = workflow;
  else root.workflow = workflow;
})(typeof self !== 'undefined' ? self : this);
```

- [ ] **Step 4: Run to verify pass** — 신규 6 + 기존 전체 통과.
- [ ] **Step 5: Commit** — `feat(dashboard): workflow 순수 모델 — 스테퍼·팀카드·배점표 매핑 행 (C1 Task 3)`

---

### Task 4: 워크플로 탭 UI — 스테퍼·카드·로그·액션 배선

**Files:**
- Modify: `dashboard/index.html`(탭 버튼·섹션·스타일·스크립트 태그 추가만), `dashboard/js/workflow.js`(렌더·배선 추가), `dashboard/js/app.js`(탭 진입 훅 1줄)
- Test: 순수부는 Task 3이 커버 — 이 태스크는 배선. 새 순수 함수가 생기면 `dashboard/test/workflow.test.js`에 추가.

**Interfaces:**
- Consumes: Task 3의 모델, `GET /institutions`(서버 모드 목록 — `store.loadData()`에 이미 병합돼 있음), `GET /institutions/{id}/status`, `POST …/run`, `POST …/checkpoint`, `GET /tasks/{task_id}`(로그 — `TaskDetail.messages`).
- Produces:
  - `workflow.renderPanel(containerEl, status, coverage)` — 스테퍼 + 팀카드 + 요약을 DOM에 그린다(순수 문자열 조립 후 innerHTML; 값은 `logic.esc`로 이스케이프).
  - `workflow.mount()` — 워크플로 탭 초기화: 기관 선택 `<select>`(서버 모드 목록에서 `institutionId` 있는 행만), 선택 시 status·coverage-map 병렬 fetch → 렌더, `[▶ 실행]`·`[승인]`·`[반려]` 버튼 배선, **2초 폴링**(탭이 활성이고 `running`이거나 방금 액션한 뒤에만 — 비활성 탭에서는 멈춘다).
  - `app.onTabChange`에 `if (tab === 'workflow' && root.workflow) root.workflow.mount();` 한 줄 추가.
  - 서버 모드가 아니면 탭 버튼·섹션에 `style.display='none'` (부트스트랩 후 판정).

- [ ] **Step 1: index.html에 자리 추가**

탭 버튼(기존 2개 뒤): `<button class="tab-btn" data-tab="workflow" id="tab-btn-workflow">워크플로</button>`
섹션(기존 `#tab-regions` 뒤): `<section id="tab-workflow" class="tab-view"><div id="wf-controls"></div><div id="wf-panel"></div><div id="wf-coverage"></div><div id="wf-log"></div></section>`
스크립트 태그: `serverdata.js` 다음 줄에 `<script src="js/workflow.js"></script>`
스타일: 기존 `<style>` 블록 끝에 스테퍼·카드·매핑 표 클래스(`.wf-step`, `.wf-step.done/.current/.gate/.todo`, `.wf-card`, `.wf-cov tr.ok/.gap/.none`) — 기존 CSS 변수(`--panel`,`--line`,`--fg`,`--muted`,`--accent-color`) 사용, 새 색상 하드코딩 금지.

- [ ] **Step 2: `workflow.js`에 렌더·배선 추가**

핵심 골격(구현자는 기존 render.js의 innerHTML 조립·이스케이프 관행을 따를 것):

```js
  // ── 렌더 ────────────────────────────────────────────────────────────
  workflow.renderPanel = function (el, status, coverage) {
    const esc = root.logic ? root.logic.esc : function (s) { return String(s); };
    const steps = workflow.stepperModel(status).map(function (s) {
      return '<div class="wf-step ' + s.state + '"><b>' + s.no + '</b>' +
        esc(s.label) + (s.gate ? '<i class="wf-gate">🛑 ' + esc(s.gate) + '</i>' : '') + '</div>';
    }).join('');
    const cards = workflow.teamCards(status).map(function (c) {
      return '<div class="wf-card"><b>' + esc(c.label) + '</b>' +
        '<span>' + esc(c.status) + ' · ' + (c.progress_pct || 0) + '%</span></div>';
    }).join('') || '<p class="wf-empty">아직 배정된 작업이 없습니다.</p>';
    const sum = workflow.coverageSummary(coverage || {});
    el.innerHTML =
      '<div class="wf-stepper">' + steps + '</div>' +
      '<div class="wf-state">' + (status.running ? '⏳ 실행 중' :
        status.pending_gate ? '🛑 ' + esc(status.pending_gate) + ' 대기' :
        status.failed ? '⚠️ 실패 — 실행을 다시 시도하세요' : '대기') + '</div>' +
      '<div class="wf-cards">' + cards + '</div>' +
      '<div class="wf-sum">배점표 ' + sum.covered + '/' + sum.total + '항목 · ' +
        sum.coveredScore + '/' + sum.totalScore + '점' +
        (sum.piiTotal ? ' · ⚠️ 개인정보 ' + sum.piiTotal + '건' : '') + '</div>';
  };
```

액션: 실행은 `fetch('/institutions/'+id+'/run', {method:'POST'})`, 결재는 `fetch('/institutions/'+id+'/checkpoint', {method:'POST', headers:{'Content-Type':'application/json','X-User-Id':'web-user'}, body: JSON.stringify({approved: true|false, comment: 입력값 || null, by: 이름입력 || null})})`. **모든 fetch는 `r.ok` 검사 + `.catch`로 실패 alert**(계획 B 최종리뷰에서 지적된 관행). 액션 성공 후 즉시 갱신 + 폴링 시작.

로그: 선택 기관의 status.tasks에서 `task_id`가 없으므로(status는 team·status·progress만 준다) — **로그 패널은 `GET /tasks/{id}`가 필요**하다. status 응답에 task_id가 없으면 로그 영역에 "작업 로그는 팀 카드 선택 시(C2에서 연결)"로 두지 말고, **Task 5에서 status 응답에 task_id를 추가**한다(아래 참조). 이 태스크에서는 `#wf-log`를 빈 컨테이너로 두고 Task 5가 채운다.

- [ ] **Step 3: 서버 모드 게이팅 + 탭 훅** — `app.bootstrapServer` 성공 경로 끝에서 워크플로 탭 표시, 실패(폴백)면 숨김. `app.onTabChange`에 mount 호출 한 줄.

- [ ] **Step 4: Run** — `node --test dashboard/test/*.test.js` 전체 통과(순수부 무회귀), `py -3 -m pytest backend -q` 무회귀.
- [ ] **Step 5: Commit** — `feat(dashboard): 워크플로 탭 — 스테퍼·팀카드·실행/결재 배선 (C1 Task 4)`

---

### Task 5: 지시·보고 로그 — status에 task_id 노출 + 로그 패널

**Files:**
- Modify: `backend/routers/workflow.py`(status 응답에 `task_id` 추가), `dashboard/js/workflow.js`(로그 렌더)
- Test: `backend/tests/test_api_workflow.py`(단언 추가), `dashboard/test/workflow.test.js`(로그 행 모델)

**Interfaces:**
- Produces:
  - `GET /status`의 `tasks[]`에 `task_id` 필드 추가(기존 필드 유지 — 프런트가 로그를 열려면 필요).
  - `workflow.logRows(taskDetail)` — `GET /tasks/{id}` 응답을 `[{role, content, at}]`로(오래된 순). `role`은 `orchestrator|agent|user|human` 그대로.
  - 팀 카드 클릭 → 해당 task의 메시지를 `#wf-log`에 표시.

- [ ] **Step 1: Write the failing tests**

`backend/tests/test_api_workflow.py`에 추가(기존 status 테스트 근처):

```python
def test_status_tasks_expose_task_id(tmp_path):
    """프런트가 지시·보고 로그(GET /tasks/{id})를 열려면 task_id가 필요하다."""
    app = _app(tmp_path)
    conn = get_connection(str(tmp_path / "registry.db"))
    conn.execute("INSERT INTO tasks (task_id, bid_case_id, team) VALUES ('task-x','bc-1','영업')")
    conn.commit(); conn.close()

    body = TestClient(app).get("/institutions/nowon/status").json()
    assert body["tasks"][0]["task_id"] == "task-x"
```

`dashboard/test/workflow.test.js`에 추가:

```js
test('logRows: 메시지를 시간순 행으로 변환', function () {
  const rows = wf.logRows({ task_id: 't', messages: [
    { role: 'orchestrator', content: '지시', created_at: '2026-07-31T00:00:00' },
    { role: 'agent', content: '보고', created_at: '2026-07-31T00:01:00' },
  ] });
  assert.deepStrictEqual(rows.map(function (r) { return r.role; }), ['orchestrator', 'agent']);
  assert.strictEqual(rows[1].content, '보고');
});

test('logRows: 메시지 없으면 빈 배열', function () {
  assert.deepStrictEqual(wf.logRows({}), []);
});
```

- [ ] **Step 2: Run to verify fail.**

- [ ] **Step 3: Implement** — status의 tasks SELECT에 `t.task_id` 추가(`backend/routers/workflow.py`의 쿼리). `workflow.js`:

```js
  workflow.logRows = function (detail) {
    return ((detail && detail.messages) || []).map(function (m) {
      return { role: m.role, content: m.content, at: m.created_at };
    });
  };
```

렌더: 팀 카드에 `data-task-id`를 실어 클릭 시 `GET /tasks/{id}` → `logRows` → `#wf-log`에 `역할 · 시각 / 내용` 목록(이스케이프 필수).

- [ ] **Step 4: Run** — 신규 pytest 1 + node 2, 전체 통과.
- [ ] **Step 5: Commit** — `feat: 워크플로 지시·보고 로그 — status task_id 노출 + 로그 패널 (C1 Task 5)`

---

### Task 6: 배점표 매핑 뷰 + B 이월 마감 + 가이드 §9

**Files:**
- Modify: `dashboard/js/workflow.js`(매핑 표 렌더), `backend/main.py`(static_dir 가드), `dashboard/js/store.js`(+`clearServerData`), `dashboard/test/store.test.js`(정리), `docs/실행가이드_backend-agent.md`(§9)
- Test: `backend/tests/test_static_serving.py`(가드 테스트 추가)

**Interfaces:**
- Produces:
  - `#wf-coverage`에 매핑 표: 항목·분류·배점·담당팀·상태(ok/gap/none 색)·PII 건수, 상단에 `coverageSummary` 요약. 데이터는 Task 2의 API.
  - `backend/main.py`: `static_dir`이 지정됐지만 디렉터리가 없으면 **import 실패 대신 경고 로그 후 마운트 생략**(B 최종리뷰 이월 — 현재는 repo 루트 밖에서 `uvicorn backend.main:app` 기동이 RuntimeError로 죽는다).
  - `store.clearServerData()` — 테스트 격리용(B 이월). 신규 store 테스트에서 사용.

- [ ] **Step 1: static_dir 가드 테스트**

`backend/tests/test_static_serving.py`에 추가:

```python
def test_missing_static_dir_does_not_crash_app(tmp_path):
    """정적 디렉터리가 없어도 API 서버는 떠야 한다 — repo 루트 밖 기동 시 import 실패 방지."""
    app = create_app(str(tmp_path / "r.db"), output_root=str(tmp_path / "out"),
                     graph_db_path=str(tmp_path / "g.db"),
                     static_dir=str(tmp_path / "does-not-exist"))
    client = TestClient(app)
    assert client.get("/institutions").status_code == 200
    assert client.get("/").status_code == 404
```

- [ ] **Step 2: Run to verify fail** — RuntimeError(Directory does not exist).

- [ ] **Step 3: Implement**

`backend/main.py`:

```python
    if static_dir:
        if os.path.isdir(static_dir):
            # 라우터 등록 뒤에 마운트해야 API 경로가 정적보다 우선한다.
            app.mount("/", StaticFiles(directory=static_dir, html=True), name="static")
        else:
            # repo 루트 밖에서 기동하면 dashboard/가 안 보인다 — API까지 죽이지는 않는다.
            print(f"[warn] 정적 디렉터리를 찾지 못해 마운트를 건너뜁니다: {static_dir}")
```

`store.js`: `store.clearServerData = function () { serverList = null; };`
매핑 표 렌더 + `mount()`에서 coverage-map fetch 결과 반영.
가이드 §9: 워크플로 탭 사용법(기관 선택→실행→결재 3회→제출), 배점표 매핑 읽는 법(ok/gap/none·PII), 서버 모드 전용이라는 점, 폴링 주기.

- [ ] **Step 4: Run** — 전체 `py -3 -m pytest agent backend collector -q` + `node --test dashboard/test/*.test.js` 통과.
- [ ] **Step 5: Commit** — `feat+fix: 배점표 매핑 뷰 + static_dir 가드·clearServerData + 가이드 §9 (C1 Task 6)`

---

## Self-Review 결과

- **범위 coverage**: 현황판(스테퍼·카드·상태·액션)=T4, 로그=T5, 배점표 매핑=T2+T6, F4 재설계=T1, B 이월(static_dir·clearServerData)=T6. C2 몫(대화창·쪽지함·지식탭)은 손대지 않는다.
- **Placeholder scan**: 없음. T1의 `LOCAL_ONLY_FIELDS` 배치와 T4의 스타일 클래스는 "기존 구조를 보고 단순한 쪽을 택하고 사유를 보고"로 명시(추측 코드 금지).
- **Type consistency**: `stepperModel/runnable/teamCards/coverageRows/coverageSummary/logRows`(T3·T5 정의 = T4·T6 사용), coverage-map 응답 키(T2 정의 = T3 테스트·T6 렌더), status의 `task_id`(T5 정의 = T5 사용) 일치.
