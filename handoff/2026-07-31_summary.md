# 2026-07-31 작업 요약

## Session 02:51

### 1. sub-project 5 재정의 — "기관인텔리" 멀티에이전트 협업 시스템 (사용자 주도 방향 전환)

- 브레인스토밍 중 사용자가 중심 컨셉 제시: **orchestrator(상위 agent)가 subagent에게
  일을 뿌리는 멀티에이전트 웹서비스** — 단, 사용자 시나리오(도봉 질의·노원 확정 이야기)
  대조 결과 "AI가 다 쓰는" 게 아니라 **사람 팀이 일하는 사이트에서 에이전트가
  분석·검사·연결을 맡는 협업 시스템**이 목표. 9단계 불변.
- 결정: LangGraph 도입(2026-07-30의 "순수 Python" 편차 롤백), vanilla JS 확장(Next.js
  미채택), 정적 서빙+file:// 폴백, 재구성 ⑦ 개명 제외, 결재 3지점(5·7·8).
- 산출: 스펙 `docs/superpowers/specs/2026-07-31-multi-agent-collab-system-design.md`
  (시나리오 19항 체크리스트 포함, 커밋 `3c6bea1`) + 상위 E2E 스펙 §⑤·⑧ 재정의 표기.
  설계도 artifact(4회 반복 승인) 원본을 `docs/architecture/2026-07-31_기관인텔리_멀티에이전트_설계도.html`로
  보존(`e3519fc`). 계획 3분할: A(오케스트레이터+대화 코어, A1/A2로 재분할) → B(store→API) → C(협업 UI).

### 2. 계획 A1 구현 완료 — 오케스트레이터 그래프 코어 (main `ac4fc90`, push됨)

- 계획 `docs/superpowers/plans/2026-07-31-orchestrator-graph-core.md`(7 tasks)를
  **subagent-driven-development**로 실행: 태스크별 구현→리뷰→픽스 라운드, 최종 전체
  브랜치 리뷰(opus) + 픽스 웨이브 1회 + 재리뷰 클린. 12커밋 `--no-ff` 머지.
- 구현물:
  - `agent/orchestrator/` — `ports.py`(Recorder 프로토콜/NullRecorder), `pii.py`(휴대폰·
    주민번호·이메일 마스킹 검출), `state.py`(merge_sections reducer — None=리셋 신호),
    `subagents.py`(rfi_agent/draft_team/packager/verifier — 기존 노드 재사용),
    `graph.py`(supervisor 그래프: rfi→3팀 Send 팬아웃→🛑기획승인→🛑이관결재→packager→
    verifier→🛑최종결재→finish, 반려 경로 3종).
  - `backend/` — notifications 테이블·리포지토리, `orchestrator_recorder.py`(DbRecorder),
    `orchestrator_service.py`(기관당 스레드 1개+Lock, SqliteSaver 체크포인터),
    `routers/workflow.py`(`POST run` 202 백그라운드 / `POST checkpoint` 결재 재개 /
    `GET status` — stage·running·pending_gate·failed·tasks·unread).
  - 의존성 추가: `langgraph`(1.2.10 확인), `langgraph-checkpoint-sqlite`.
  - 실행가이드 §6 추가(run→폴링→결재 3회 curl 흐름 + Ollama 실측 절차).
- 리뷰가 잡아낸 주요 버그(전부 수정·재검증됨): ①PII 이중검출로 주민번호 뒷자리 노출
  (1월생 케이스) ②기획반려 후 sections 채널 누적 → PPTX 중복 슬라이드 ③반려 사유
  (revision_note)가 재작성 프롬프트에 미전달 ④sections 팬아웃 순서 비결정 ⑤실패 상태
  미노출(폴링 클라이언트 무한대기) ⑥한글 X-User-Id 헤더 monkeypatch가 브라우저 제약 은폐.
- 테스트: 기준선 284 → **313 passed** + dashboard 36/36. 병합 후 main에서 재확인.
- **Ollama 실측은 미수행** — 이 PC에 Ollama가 현재 미기동/미설치(curl 연결 거부,
  명령어 없음). 절차는 실행가이드 §6에 완비 — NEXT.md 항목 5와 같은 하드웨어 종속 건.

### 3. 최종 리뷰에서 이월된 후속 항목 (A2 착수 시 반영)

F4 실물 subagent 통합테스트 0회(미정의 state 채널이 조용히 유실되는 구조) ·
F5 run의 400 조건이 수기 반입 산출물 미고려 · F6 archive_dir 채널 부재로 과거 제안
참고 슬라이드 미생성 · F7 게이트 도달 시 "결재요청" notification 미생성 · F8 PII가
content만 검사(title·sources 제외) · F9 resume 락 재진입 주석 · F10 X-User-Id 헤더의
한글 모지바케(감사 기록 관점 — body 필드 이동 권장) · Task2 TOCTOU(_ensure_task) ·
pending_gate 커넥션 캐싱/close.

### 남은 것

- **A2**(대화창 consult·notifications 라우터·업로드 즉시검사·완료 아카이브) — 계획 미작성.
- **B**(store→API 전환), **C**(협업 UI) — 설계는 스펙에 확정, 계획 미작성.
