# 2026-08-30 작업 요약

## Session 23:49 — 문의서 내부 회신 5건 반영, 그리고 브랜치를 뒤늦게 찾은 세션

### ⚠️ 먼저 — 이 세션이 낭비한 것과 그 원인 (다음 세션이 꼭 볼 것)

**세션의 절반을 낡은 정보 위에서 일했다.** 원인은 하나다:

> **`main`의 `NEXT.md`는 낡았고, 진짜 작업지는 `weblogic-java-migration` 브랜치다.**
> 그런데 SessionStart 훅은 **체크아웃된 브랜치의 `NEXT.md`를 주입**한다. `main`에서
> 세션을 시작하면 **58커밋 뒤처진 세계관**을 사실로 믿고 출발하게 된다.

그 결과 실제로 벌어진 일:

| 이 세션이 "발견"한 것 | 실제 |
|---|---|
| 이관 문서가 `backend/`·`dashboard/` 낡은 경로를 가리킨다 | 브랜치는 `dashboard/`를 이미 정리함(2곳만 잔존) |
| 스트리밍을 "SSE"로 규정한 사실오류 | **8/28에 이미 수정됨** (`bde657b`) |
| 계획서에 `inbox`·`llm_status` 배정 구멍 | 브랜치엔 이미 Task 5B.5·5B.6으로 존재 |
| Java 구현 미착수 | **단계 1·2·5 완료, 4 대부분** — `kgi-ggreport-web/` 28,222줄 |
| 문의서 7건, 발송 전 점검 완료 | **10건**으로 개정 + 부서별 3통 발송본 완비 |
| eGovFrame 4.x 준수 필수 | **폐기됨** (`557caac`, 8/25) |

**사용자가 "다른 브랜치에서 하고 있는 걸로 아는데"라고 물어봐서야 발견했다.**
사용자가 묻지 않았다면 그대로 틀린 작업을 계속했을 것이다.

**⇒ 다음 세션이 할 일: 이관 관련 작업이면 `git branch -r`부터 확인하고
`weblogic-java-migration`으로 전환한 뒤 그 브랜치의 `NEXT.md`를 읽을 것.**
`main`의 `NEXT.md`는 이관에 관한 한 신뢰하지 말 것.

- **폐기한 커밋**: 낡은 전제로 `main`에 만든 `fb356f4`(이관 문서 경로·라우터
  배정 수정)를 `git reset --hard HEAD~1`로 되돌렸다. 내용의 대부분이 브랜치에
  이미 있어 나중에 병합 충돌만 만들 것이었기 때문이다. 미push 상태였고
  `main`은 `origin/main`과 `0 0` 동기화를 확인했다. (reflog에는 남아 있다.)
- **살아남은 것 1건**: 브랜치 계획서에도 `backend/seed.py`·`backend/consistency.py`
  **2곳**이 낡은 경로로 남아 있다. 이번엔 손대지 않았다 — 아래 "안 한 것" 참조.

### 한 일 — 문의서 10건 중 5건을 내부 회신으로 채웠다

**발송하지 않았다.** 사용자가 곧 내부 담당자여서, 물어보는 것으로 회신을
대체했다. 문항은 지우지 않고 "답변" 칸만 채웠다(과거 3회 개정과 같은 규약).
대상: `docs/이관문의서_WebLogic_확인사항.md`.

| # | 회신 | 여파 |
|---|---|---|
| **1** | **사내 공용 LLM API가 없다** | 🔴 **설계 전제 부정** — 자체 호스팅으로 전환 |
| **2** | 사내 임베딩 API도 없다 | 🟢 그런데 **최악 시나리오는 회피** — 아래 |
| **4** | **Oracle 11g** | `VARCHAR2` 4000바이트 상한 → CLOB이 필수가 됨 |
| 5 | WebLogic 12 버전대(소버전 미확인) | 기존 가정 유지, 비차단 |
| **7** | **앞단 웹서버 없음(web-was 일체형)** | 사용자 유입 경로의 프록시 문제 소멸 |

**신설 1-1(서버 사양)** 을 추가했다 — 자체 호스팅 전환으로 **새로 생긴 최우선
확인 항목**이다. 별도 추론 서버가 없어 **WebLogic 서버 한 대에 모델을 얹어야**
하는데, 사양에 따라 **아예 못 올릴 수도 있다.**

### 판단 3가지 (근거와 함께)

1. **전제 변경의 코드 비용은 0이다.** `LlmClient`·`TokenProvider`가 인터페이스만
   동결돼 있고 구현은 `NotYetMigratedLlmClient`가 일부러 실패시킨다 —
   확인하고 나서 문서만 고치기로 했다. 바뀌는 건 "Task 4.4가 앞으로 무엇을
   구현하느냐"뿐이다.
2. **문의 2의 "의미 검색 포기"는 일어나지 않는다.** 임베딩도 자체 호스팅하면
   되고, 현 Python이 쓰는 `bge-m3`(1.2GB·1024차원)를 그대로 올리면 같은 벡터가
   나온다. **계획 Task 3.3 존치**, 품질 저하 합의 불필요.
3. **`TokenProvider`를 지우지 않았다.** 문의 6 ⓒ("우리 시스템이 공개되는 쪽의
   토큰 재검증")는 살아 있고, 그게 "필요"로 오면 이 시스템에 **인증이 전혀 없으므로**
   통째로 새 과제가 된다.

### ⚠️ 충돌을 지우지 않고 남긴 것

**2026-08-25 기록은 "경유지를 반드시 거쳐 사내 LLM API를 호출하는 구조임이
확인됨"이다.** 오늘 회신과 정면으로 충돌한다. 사용자 지시로 **오늘 답을 기준**으로
삼았으나, 문의서와 NEXT.md 양쪽에 **"재확인 대상"으로 명시**했다 — 뒤집히면
1·2·6번이 되살아난다.

7번도 비슷하다. "경유지"와 "앞단 웹서버"는 **층위가 다를 수 있다**
(경유지=나갈 때 거치는 관문 / 앞단 웹서버=들어올 때). 그 구분을 문서에 적어 뒀다.

### 유보한 결정 — LLM 기능 범위

CPU 추론이 **1건당 249초·CPU 100%**(8코어 실측, `qwen3.5:9b`)라, WAS와 한 서버를
쓰면 그동안 화면 응답성이 함께 나빠진다. ⓐ전부 유지 / ⓑ배치만(대화 탭 제외) /
ⓒLLM 없이 를 제시했고 사용자가 **ⓑ로 기울었다가 "미루자"로 유보**했다 —
서버 사양(1-1)이 나와야 판단이 선다.

⚠️ **그 과정에서 정정한 것**: 처음에 "배치면 괜찮다"는 식으로 말했는데
**틀렸다. 배치도 CPU를 100% 쓴다.** 차이는 사용량이 아니라 **통제 가능성**
(동시 1건 큐 · 코어 제한 · 시간대)이다. **코어를 묶었을 때의 소요 시간은 아직
실측한 적이 없다** — 이 PC(8코어)에서 바로 재볼 수 있는데 안 했다.

### 함께 커밋한 남의 작업

**오늘 앞선 세션이 `NEXT.md`에 35줄을 써 두고 커밋하지 않았다.** 유실 위험이
있어 이번 커밋에 함께 담았다. 내용은 값지다:
- **Oracle 11g 확인**(오늘 회신과 독립적으로 일치) + **12c 전용 문법 수정 위치를
  파일·줄 단위로 특정** — `GENERATED BY DEFAULT AS IDENTITY` 6곳,
  `FETCH FIRST` 1곳(+ 그것을 단언하는 테스트 `DynamicSqlTest.java:71`).
  `uploader/`는 영향 없음.
- **로컬 검증 환경 조사** — `uploader` WAR 빌드 성공(38/38, Java 8 바이트코드
  확인) · WebLogic은 JDK 8 인증이 **14.1.1이 유일**(단 Premier Support가
  2026-12-31 종료) · DB 무료 개발본은 **XE 11g R2**가 유일 경로(Oracle Text 포함) ·
  `ojdbc8 21.7`은 11.2.0.4와 인증되나 로컬 XE(11.2.0.2)엔 19.x가 안전.

### 안 한 것 (다음 세션의 첫 작업)

**문의서만 고쳤고 설계·계획서는 옛 전제 그대로다 — 두 문서가 갈라져 있다.**
사용자가 "마무리"를 택해 의도적으로 멈췄다. 맞춰야 할 곳은 `NEXT.md`의
문의서 절에 파일·섹션 단위로 적어 뒀다(설계 §2·§3·§6-C·§7, 계획 매핑표·Task 4.4,
발송본 상태, 회신영향 내부메모의 2번 행).

여기에 **브랜치 계획서의 낡은 경로 2곳**(`backend/seed.py`·`backend/consistency.py`)도
같이 고치면 된다.
