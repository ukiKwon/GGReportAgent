# 2026-08-20 작업 요약

## Session 11:20 — 포트폴리오 화면 재캡처 (`portfolio/`)

사용자 요청: "dashboard 관련으로 포트폴리오 만들어 둔 거 있었나?" → 있음을 확인한 뒤
"갱신해주면서, 현재 버전은 브라우저로 다시 보여줘".

### 무엇이 낡아 있었나

`portfolio/index.html`은 2026-08-05 작성분이다. 그 뒤 `dashboard/index.html`이
**13개 커밋**으로 바뀌었다 — 특히 `1a8a2ba`(프리미엄 다크 크롬 + 파스텔 지도 전면
리디자인)와 전국 전수 데이터 확대. **워크플로·대화·지식·쪽지함·작업함·결재함·권한관리가
전부 `dashboard/index.html` 한 파일**이라, 캡처 18장 중 **14장이 통째로 낡았다.**

PART 4 리포트 4장(15~18)은 `corpus/reports/5-report_2.0.html`이 그 뒤 **변경 0건**이라
그대로 유지했다.

### 캡처 재현 (이번엔 스크립트를 리포에 남겼다)

- 8/5 세션은 스크립트를 스크래치패드에만 두고 리포에 남기지 않았다
  (`2026-08-05_summary.md` `## Session 13:39`) → **이번에 처음부터 다시 짰다.**
  같은 일을 반복하지 않도록 **`portfolio/capture.py`로 커밋**했다.
- 조건은 8/5와 동일: **Playwright + msedge 채널, 1600×1000 / DSF 2 → 3200×2000**.
  `playwright`가 어느 파이썬에도 없어 `py -3.14 -m pip install playwright`로 설치했다
  (브라우저는 설치된 Edge를 그대로 쓰므로 별도 내려받기 없음).
- 데모 DB는 **사용자 지시로 재시딩 없이 재사용**(`py -3.14 -m backend.demo --port 8000`).

### 첫 시도에서 빈 화면이 찍혔다 — 실패가 조용히 지나간다

**기관을 고르거나 카드를 눌러야 그려지는 화면**이 있는데 탭만 열고 찍어서, 워크플로·
대화·작업함이 빈 껍데기로 저장됐다. `screenshot`은 예외를 던지지 않으므로
**육안 확인 없이는 못 잡는다.** 선택 단계를 넣어 다시 찍고 18장 전부 눈으로 봤다.

| 화면 | 빠져 있던 단계 |
|---|---|
| 05·07·08 워크플로 | `#wf-inst`에서 `dobong` 선택 → `.wf-step[data-stage='7']` 클릭 → `.wf-card` 클릭 |
| 09 대화 | `#ch-inst`에서 `dobong` 선택(심어둔 2턴 대화가 그제야 뜬다) |
| 19 작업함 | `.dg-item` 클릭(이관 패키지가 오른쪽에 열린다) |

- 탭 버튼 id가 `data-tab`과 다른 곳이 하나 있다 — **작업함만 `#tab-btn-tasks`**
  (`data-tab="designer"`). 스크립트에 `TAB_ID` 매핑으로 박아 뒀다.
- 결재함·권한관리는 아래 여백을 잘랐다(2000 → 853·1225px). 배경에 **그레인 텍스처**가
  깔려 있어 완전일치 비교로는 안 잘린다 → 허용오차 18로 판정한다(`crop_bottom`).
- 작업함은 뷰포트를 1400으로 키워 이관 패키지가 한 장에 들어가게 했다.

### 본문 수정

- 수치 현행화(기준 커밋 `56b0ea3`, 측정 기준은 NEXT.md 항목 10의 고정 기준을 지켰다):
  테스트 **731/204 → 759/244**, 파이썬 **8,036 → 8,226줄**,
  화면 JS **3,086 → 3,851줄**. 화면 JS는 전국 데이터 확대로 `dashboard/data/`가
  커져서, **총계(5,714)와 앱 코드(3,851)를 나눠 적었다** — 데이터 1,861줄을 앱 코드로
  세면 규모를 부풀리게 된다.
- **대화 탭 모델 배지가 바뀌었다** — 8/5에는 `llama3.1:8b (자동 선택 · RAM 15.7GB /
  8 vCPU)`였으나 지금은 기본값 `gpt-oss-120b`(`/llm/status`의 `auto:false`). 캡션을
  현재 화면에 맞추고 `LLM_MODEL=auto` 동작은 따로 적었다. Ollama가 안 떠 있어
  (`installed: []`) auto로 되돌려 찍는 건 불가능했다.
- **③ 기관 정보 변경 캡션은 고치지 않았다.** `339a455`가 더한 `POST /institutions`는
  '기관 추가' 버튼의 별개 경로이고, "기관명은 서버 모드에서 변경 불가"라는 서술은
  지금 화면에도 그대로 있다(재캡처본에서 확인).

### PNG → JPEG 전환 (사용자 결정)

새 디자인의 **그레인 텍스처 때문에 PNG가 안 눌린다** — 8/5 캡처는 평면색이라 고유색이
8.5천 개였는데 새 캡처는 텍스처로 색이 흩어져, 총량이 **6.0MB → 24.5MB**가 됐다.
사용자 지시로 **재캡처 14장만 JPEG q92**로 바꿔 **7.5MB**로 되돌렸다.

**PART 4 4장은 PNG로 남겼다** — 평면색이라 JPEG가 오히려 커진다(18번 239 → 366KB).
손대지 않은 파일을 손실 압축으로 다시 굽지 않으려는 이유도 있다.
`index.html`의 `<img src>` 14곳을 `.jpg`로 고쳤고, 브라우저에서 **18장 전부 로드 확인**
(`document.images` broken 0건).

### 커밋

- **`6b5b882`** `docs(portfolio): 화면 14장 재캡처 + 수치 현행화, PNG→JPEG 전환`
  (30 files changed). 사용자가 커밋만 지시해 **푸시는 하지 않았다.**
- 스테이징은 `portfolio/` 경로만 지정했다 — 이 워크트리에서 **다른 세션이 동시에
  커밋 중**이라(`c0da1c8`·`56b0ea3`·`99abb50`이 작업 도중 얹혔다) `git add .`을 쓰지
  않았다. 8/5 세션과 같은 이유다.

### NEXT.md 정리

`99abb50`(다른 세션)이 만든 **"⚠️ 워킹트리에 미커밋 변경이 있다 (2026-08-20 기준)"**
블록을 **제거했다.** 그 블록이 가리키던 `portfolio/` 16건이 바로 이 세션의 작업분이고,
`6b5b882`로 커밋되면서 경고가 사실과 어긋나게 됐다.

### 남은 것 / 확인 필요

- **푸시 안 함** — `main`이 `origin/main`보다 1 앞서 있다.
- **앱 버그**: 결재함·작업함의 카드 본문이 고정 높이에 잘려 "근거자료: spec/02(1장-4),
  plan FN-1" 줄이 반쯤 짤려 보인다(캡처 20·19에 그대로 찍혀 있다).
  → 사용자 지시로 **다음 세션으로 이월**, `NEXT.md` **항목 17**로 등록했다.
- **impeccable 훅 26건 + side-tab 2건**(`portfolio/index.html`) — 손대지 않았다.
  `DESIGN.md`는 **대시보드**(다크 크롬)의 디자인 시스템이고 포트폴리오는 8/5에 만든
  **라이트 테마 별개 문서**라, 서로 다른 산출물에 같은 팔레트를 댄 결과로 보인다.
  훅 설정을 건드리는 일이라 사용자 판단 없이 예외 등록하지 않았다.
  → 사용자 지시로 **다음 세션으로 이월**, `NEXT.md` **항목 18**로 등록했다.
- **로컬 서버 2개가 떠 있다** — 8000(`backend.demo`), 8765(`http.server`, 포트폴리오
  미리보기). 세션 종료 시 내릴 것.
- **데모 쪽지함에 중복 알림 4건**이 쌓여 있다(같은 "디자이너 작업물 제출 — 결재를
  부탁드립니다"). 반복 시딩의 흔적이라 캡처 12에 그대로 보인다. 거슬리면
  `--reset` 후 재캡처가 필요하다.
