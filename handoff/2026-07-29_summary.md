# 2026-07-29 작업 요약

## Session 00:29 — E2E 구성도 재작성 + 7단계(취합) 연결

### 한 일

**1) `architecture/system-overview.html`을 E2E 흐름 축으로 재작성 (rev.2 → rev.4)**
- 기존 문서는 컴포넌트 나열형이었는데, 사용자 요청("end-to-end 중심으로 그림도 같이")에
  따라 **업무 9단계를 축으로** 재편. 신규 절: `02 E2E 전체 흐름`(9단계 × 실제 자산 매핑
  flowchart), `03 입찰건 하나가 지나가는 길`(사람/시스템 교대 sequenceDiagram),
  `05 데이터 소유 지도`, `06 E2E 단절 지점`.
- 리포 전체를 다시 훑으며 **`dashboard/`를 "기존 자산"이 아니라 ①단계(입찰현황 파악)
  도구로 재분류**. 실제로 E2E의 출발점인데 그동안 흐름 밖 레거시로 그려져 있었음.
- 조사 중 드러난 사실 2건: ① `RFP/수원시 금고 지정 계획 공고문.pdf`가 있는데 수원시는
  `giganlist/`(서울 25구)에 없어 ④단계 이후가 실제로 막혀 있음 ② `python-pptx`가
  `requirements.txt`에 누락돼 `agent/tests` 2개 모듈이 수집 단계에서 실패 중이었음.
- 아티팩트로 발행(같은 URL `d2de5751-78a0-41de-9609-b449ed35dfbe`, rev.2→3→4 재발행).
  커밋 `e462d39`(rev.3), `8e335ad`(rev.4).

**2) 7단계(취합) 연결 — 문서가 지적한 단절을 그 자리에서 해소**
- 그때까지 `finalize`는 `institutions.stage=7`만 세팅하고 끝나, 3팀 `draft_content`를
  사람이 직접 꺼내 문서로 조립해야 했음. 트리거·입력·산출 코드가 전부 이미 있는데
  사이만 비어 있던 구간.
- `superpowers:test-driven-development`로 진행(실패 확인 → 최소 구현):
  - `requirements.txt` ← `python-pptx` 추가. 이것만으로 `agent/tests` **16 → 24 passed**.
  - `backend/assembler.py`(신규 50줄) — 3팀 초안을 **영업 → IT → 예산** 순 슬라이드로
    묶어 PPTX 저장, 경로를 `institutions.pptx_path`에 기록
    (`GET /institutions/{id}/artifacts`로 조회). 슬라이드 생성은
    `agent/nodes/pptx_builder.build_pptx()`를 그대로 재사용(E2E 스펙 §④ "재사용" 준수).
  - `create_app(db_path, output_root="report_new")` — 산출 위치 주입 가능하게 해
    테스트가 리포에 파일을 쓰지 않도록. `.gitignore`에 `report_new/` 추가.
  - `backend/routers/bidcases.py` — **승인(approved=true) 경로에서만** 호출. 반려 시
    생성하지 않는 것을 테스트로 고정.
  - 테스트 `backend/tests/test_assembler.py` 4건 + finalize 산출물 2건 → **backend 66 → 72 passed**.
- 커밋 `1f9bcfe`.

**3) 병합·정리**
- 시스템 지침대로 기본 브랜치를 피해 `feat/stage7-assembler`에서 작업 후, 사용자 승인
  받아 `main`에 **fast-forward 병합**. 브랜치는 아직 남아 있음(삭제 여부 미정).
- 병합 시점에 **다른 세션이 같은 체크아웃에서 커밋 중**이었음(`f1cac01`, `784b496` —
  코퍼스 검증·반입 설계 스펙). 그 스펙이 rev.3 서술과 어긋나 rev.4로 정정(아래 참고).
- 병합 후 전체 재실행: **backend 72 + agent 24 = 96 passed**, dashboard `node --test` 36 passed.

### 주요 결정

- **취합 시점은 "finalize 승인과 동시"** — 별도 `assemble` 엔드포인트를 만들지 않았다.
  `finalize`가 이미 `stage=7` 트리거를 쏘고 있는데 받는 쪽이 비어 있던 것이 단절의
  본질이었으므로, 트리거와 산출을 한 트랜잭션 흐름에 붙이는 쪽을 택했다.
- **슬라이드 생성기는 새로 만들지 않고 `agent/`의 `build_pptx()`를 재사용** — 상위 E2E
  스펙 §④가 7단계를 "재사용: pptx_builder_node"로 규정하고 있어 그 의도를 따랐다.
  결과적으로 `backend/`가 `agent/`를 쓰는 두 번째 지점이 됐다(첫 번째는 `llm.py`).
- **push 보류** — 다른 세션이 같은 체크아웃에서 작업 중이라 사용자가 보류를 선택.

### NEXT.md 항목 처리

- **항목 1(`agent/` RFP 팀 확장 — Task 3 `spec_research_node`부터 재개)을 "폐기"로 종결**.
  같은 날 main에 들어온 `docs/superpowers/specs/2026-07-29-institution-corpus-validation-design.md`가
  ① `spec_research_node` 구상을 명시적으로 폐기(조사는 DMZ에서 사람이 수행, 시스템은
  기계 검증·반입·게이팅만 담당)하고 ② 이어받을 워크트리 브랜치·커밋이 로컬/원격
  어디에도 없음을 실물 확인(dangling 79개 전수 조회 포함)했기 때문. 재개할 대상이
  존재하지 않으므로 "진행 중"으로 남겨둘 수 없다.
- **신규 항목 추가**: `main`의 미push 커밋 5개(다른 세션 작업 종료 후 push 판단).

### 남은 것

- `origin/main`에 push (커밋 5개) — 다른 세션 종료 후.
- ⑧ 검토 연결 — 이제 검토 대상 PPTX가 실제로 생기므로 `verification_node`를 물릴 자리가
  생겼다. 양쪽 조각이 이미 있는, 다음으로 값싼 단절.
- ⑦ 산출물의 서식/템플릿은 python-pptx 기본 레이아웃이고 배점표 슬라이드는
  "(배점표 없음)"으로 나간다 — BidCase에 배점표를 싣는 경로가 아직 없음.
- `feat/stage7-assembler` 브랜치 삭제 여부.

---

## Session 07:59 — 마무리 검토 (push 확인, NEXT.md 정리)

### 한 일

- **`## Session 00:29`의 미push 상태를 해소 확인.** 다른 세션이 그 사이 push를 완료해
  `git rev-list --left-right --count origin/main...main` = `0 0`. 이 세션 커밋
  `1f9bcfe`(7단계 취합 `backend/assembler.py`)·`5b45ac7`(handoff)가 `origin/main`에
  들어갔음을 `git merge-base --is-ancestor`로 개별 검증했다.
- **전체 테스트 재실행**: backend 72 + agent 24 = **96 passed**,
  `node --test dashboard/test/*.test.js` **36 passed**, fail 0.
- **NEXT.md 정리** — 아래 "NEXT.md 항목 처리" 참고.

### 다른 세션 상태 (이 세션이 건드리지 않은 것)

- 워크트리 브랜치 `worktree-corpus-validation`이 활성 상태이고
  `.claude/worktrees/`가 untracked로 남아 있다 → **코퍼스 검증 구현이 진행 중**.
- `2026-07-29_summary.md`에 그 세션의 `## Session` 섹션이 아직 없고, main에는 그쪽
  커밋 `3ef30a1`(구현 계획), `759b655`(리포 구조 재편 설계)가 올라와 있다.
- 즉 **이 파일의 마무리는 아직 끝나지 않았다.** 그 세션이 자기 섹션을 append하고
  NEXT.md에 자기 항목을 추가하는 것이 남아 있다. 이 세션은 그 영역을 손대지 않았다.

### NEXT.md 항목 처리

- **구 항목 1(`main`의 미push 커밋 5개) 해소** — push 완료 확인. 해소 로그로 이동.
- 그 항목에 딸려 있던 로컬 브랜치 `feat/stage7-assembler` 삭제 건은 아직 미결이라
  기존 원격 브랜치 삭제 건(`origin/feature/treasury-bid-dashboard`)과 묶어
  **새 항목 1 "병합 완료된 브랜치 2개 삭제 여부"** 로 통합했다. 둘 다 비차단이고
  사용자 판단만 남았다.

### 남은 것

- 브랜치 2개 삭제 여부 (사용자 판단) — 위 항목 1.
- ⑧ 검토 연결 — 7단계에서 PPTX가 실제로 생기므로 `verification_node`를 물릴 자리가
  생겼다. 양쪽 조각이 이미 있는, 다음으로 값싼 단절(구성도 06절 S4).
- ⑦ 산출물 서식/템플릿은 python-pptx 기본 레이아웃, 배점표 슬라이드는 "(배점표 없음)".
  BidCase에 배점표를 싣는 경로가 아직 없다.

---

## Session (풀 상태 감사 — 다른 PC로 이어가기 전 최종 점검)

> 이 세션은 07-26에 시작된 "E2E 워크플로우 시스템" 브레인스토밍 스레드가 사흘 뒤에
> 재개된 것이다(대화 컨텍스트가 그 사이 여러 번 압축·재개됨). 이 세션 자체는 **새 코드를
> 작성하지 않았다** — 사용자가 "다른 PC에서 이어서 작업할 수 있게 handoff를 잘 남겨달라"고
> 요청해, 그 사이(07-26→07-29) 다른 세션들이 쌓아온 상태 전체를 감사하고 문서를
> 실제 상태와 맞게 정정하는 것이 이 세션의 유일한 작업이다. **감사 도중 다른 세션이
> 같은 체크아웃에서 실시간으로 커밋하는 것을 직접 목격**했다(`cf80a85`가 조사 중간에
> 갑자기 나타남) — 이 리포가 진짜로 여러 세션에 의해 동시에 쓰이고 있다는 뜻이므로,
> 아래 내용은 감사 시점(`git fetch` 후 `origin/main`과 `0 0` 동기화 확인 시점) 기준이다.

### 발견 1 — corpus-validation 워크트리: 구현은 사실 100% 끝났는데 아무 데도 기록이 없었다

`cf80a85`(다른 세션, 07:59 이후 작성)조차 "다른 세션이 코퍼스 검증을 **진행 중**"이라고만
적어뒀는데, 실제로 워크트리를 열어 확인한 결과는 다르다:

- 워크트리 `.claude/worktrees/corpus-validation`, 브랜치 `worktree-corpus-validation`,
  HEAD `8d94f88`, **워킹트리 클린**, `main`(`cf80a85`)에서 분기해 6개 커밋 추가:
  `ed7e509`(Task1 검증기 코어+CLI) → `5ffd252`+`dd609b1`(Task2 내용규칙+베이스라인,
  리뷰 fix 1라운드) → `34cb8c1`(Task3 research_status 게이트) → `f2d3811`+`8d94f88`
  (Task4 코퍼스 validate/register 엔드포인트, 리뷰 fix 1건).
- 계획 `docs/superpowers/plans/2026-07-29-institution-corpus-validation.md`의 **Task
  1~4 전부 완료** — `.superpowers/sdd/2026-07-29-institution-corpus-validation/`
  안의 `task-1~4-brief.md`/`task-1~4-report.md`/`review-*.diff` 전부 존재, 개별 태스크
  리뷰 전부 clean(Task2·Task4는 fix 1라운드 거쳐 clean). **단, `progress.md` 원장은
  Task3까지만 기록돼 있고 Task4 완료 줄이 비어 있다**(세션이 커밋 후 원장 갱신을
  누락한 것으로 보임 — 실제 커밋·리포트는 Task4까지 존재하니 원장 누락일 뿐 작업
  누락이 아니다).
- 워크트리에서 전체 테스트 직접 실행 확인: **backend 124 passed**(main 현재 72 대비
  +52 아니라 +28 순증 — Task4 리포트 기준 "116 baseline + 8 new"이고 baseline 116은
  Task1~3이 main의 72에 44개를 더한 것). agent/는 이 브랜치가 건드리지 않음(24 그대로).
  **병합 시 예상 전체 테스트 수: backend 124 + agent 24 = 148.**
- **이 브랜치는 개별 태스크 리뷰까지만 끝났고, sub-project 0(레지스트리) 때 했던
  "전체 브랜치 최종 리뷰(opus 등급) → main 병합" 단계가 아직 없다.** 즉 실행 가능한
  다음 행동은 "머지"이지 "구현 재개"가 아니다 — 이 사실이 어디에도 적혀 있지 않아서
  다음 세션이 Task 5를 찾아 헤맬 뻔했다.
- **이 세션은 병합을 직접 수행하지 않았다** — "handoff 정리"가 요청 범위이고, 병합은
  `superpowers:finishing-a-development-branch` 절차(최종 리뷰 포함)를 거쳐야 하는
  별도의 실질적 작업 판단이라 다음 세션/사용자 판단으로 넘긴다. 아래 NEXT.md 항목 참고.

### 발견 2 — 리포지토리 재구성 스펙(`759b655`)은 설계만 있고 계획이 없다

`docs/superpowers/specs/2026-07-29-repo-restructure-design.md`(다른 세션, 브레인스토밍
스킬로 작성됨)는 리포 구조 전체(`giganlist/`→`corpus/institutions/`, `registry.db`→`data/`,
`RFP/`·`report/`→`corpus/` 등)를 다루는 상위 설계다. 문서 자체가 "이 문서는 아무것도
옮기지 않는다, 실행은 후속 세션"이라고 명시하고 §⑦에 7단계 마이그레이션 순서를 못박아
뒀다(①무위험 정리 ②`data/` 분리 ③`corpus/` 부분 신설 ④giganlist 이동 ⑤검색 인덱스
⑥collector 스키마 ⑦후일 개명). **④는 명시적으로 "코퍼스 검증 구현이 착지한 뒤"로
게이트돼 있다** — 즉 발견 1의 corpus-validation 브랜치가 머지돼야 ④를 시작할 수 있다.
이 스펙에 대한 **`docs/superpowers/plans/` 계획 파일은 아직 없다** — `writing-plans`
스킬이 아직 안 들어간 상태. 브레인스토밍 스킬 절차상 계획 작성 전에 "사용자가 스펙 파일을
검토했는지" 확인이 필요한데, git 커밋 메시지만으로는 그 확인이 있었는지 알 수 없다.

### 발견 3 — 잔해 정리 후보 (비차단, 정보 제공용)

- `.claude/worktrees/institution-intelligence-agent/` — **`git worktree list`에 안
  잡힘**(등록된 워크트리 아님), 내용물은 빈 `.claude/` 하나뿐. 이 이름의 실제 작업은
  이미 다른 커밋들(`1c72190`~`189bee0` 등, BidCase/Task/Message 레이어)로 main에
  전부 병합·완료돼 있으므로, 이 디렉터리는 그 작업을 위해 만들다 만 워크트리 잔해로
  보인다. `git worktree`가 인식 못 하므로 그냥 폴더 삭제(`rm -rf`)면 되고, 실수로
  지워도 잃을 커밋은 없다(git 객체가 아니라 빈 폴더). **삭제는 하지 않고 다음 세션
  판단에 맡김.**
- `tmp/KB_AI_Lab_교육내용정리.html` — untracked. 재구성 스펙 §⑦ 1단계가 `tmp/` 정리를
  포함하지만 "빈 tmp/ 삭제"라고만 돼 있어 이 파일을 어떻게 할지는 안 정해짐 — 1단계
  진행 시 사용자에게 보존/삭제 여부 확인 필요.

### 이 세션이 한 일 (전부 문서·리뷰, 코드 변경 없음)

- 워크트리/브랜치/커밋 상태를 `git log`·`git worktree list`·`git status`·직접 테스트
  실행으로 재검증(위 발견 1~3).
- `handoff/2026-07-29_summary.md`에 이 섹션 추가.
- `NEXT.md`에 항목 2(corpus-validation 병합 대기)·항목 3(재구성 스펙 계획 필요) 신규
  추가, 항목 3(잔해 정리)은 참고용으로 부기.

### 다음 세션이 바로 이어받을 것 (우선순위 순)

1. **corpus-validation 브랜치 최종 리뷰 → main 병합**(발견 1). sub-project 0 병합 때와
   동일 패턴: `EnterWorktree`로 재진입 → 전체 diff(`main..worktree-corpus-validation`)
   최종 리뷰 → clean이면 `main`으로 로컬 병합 → 테스트 148개 확인 → push.
2. 병합 후 재구성 스펙 §⑦**1단계(무위험 정리)**부터 진행 — corpus-validation과
   무관하게 지금 시작 가능(giganlist 이동인 4단계만 1번 완료를 기다림). 단, 계획
   파일이 없으므로 사용자에게 스펙 승인 여부 확인 후 `writing-plans`부터.
3. 브랜치 삭제 여부(NEXT.md 항목 1, 그대로 대기 중) — 사용자 판단.

### 환경 (변동 없음, 재확인만 함)

`python`/`pip` 맨 명령은 Windows Store 스텁 — `py -3`(3.14.0) 사용. 현재 `main` 기준
전체 테스트: `py -3 -m pytest backend agent -q` → **96 passed**(backend 72 + agent 24),
`node --test dashboard/test/*.test.js` 검증은 이 세션에서 재실행하지 않음(직전 세션에서
36 passed 확인됨, 코드 변경 없어 재검증 불필요 판단).

---

## Session 08:05 — 브랜치 정리

### 한 일

- **사용자 승인 후 병합 완료된 브랜치 2개 삭제** (NEXT.md 구 항목 1 해소):
  - 로컬 `feat/stage7-assembler`(`784b496`) — `git branch -d`. 7단계 취합 작업 브랜치이며
    main에 ff 병합돼 있어 `-d`(안전 삭제)가 그대로 통과했다.
  - 원격 `origin/feature/treasury-bid-dashboard`(`464f906`) — 삭제 전
    `git merge-base --is-ancestor 464f906 origin/main`으로 완전 병합을 검증한 뒤
    `git push origin --delete`.
- **세 번째 원격 브랜치 `origin/subagent-init-archi`(`4d721a9`)를 발견**해 조사했다.
  `agent/` 오케스트레이션 패키지 구현 브랜치로, 2026-07-21에 **PR #1으로 squash merge**
  되어 main에는 `5bafdfa` 한 커밋으로 들어가 있다. squash라 브랜치 커밋 11개가 main의
  조상이 아니어서 ancestry 검사로는 "미병합"으로 나오지만, `git diff origin/main
  origin/subagent-init-archi`는 482파일 62,367줄 **삭제**로 나온다 — 브랜치가 main보다
  뒤처져 있다는 뜻이며 브랜치 고유 산출물은 확인되지 않았다. **자동 안전 판정이 안 되는
  건이라 지우지 않고 NEXT.md 새 항목 1로 이월**했다.

### 다른 세션 상태 (재확인)

- 여전히 진행 중. 워크트리 `.claude/worktrees/corpus-validation`
  (브랜치 `worktree-corpus-validation`, 팁 `8d94f88`)이 활성이고,
  `2026-07-29_summary.md`에 그 세션의 `## Session` 섹션은 아직 없다.
- main 쪽 그 세션 커밋은 `3ef30a1`(구현 계획)·`759b655`(리포 구조 재편 설계)까지.
  이 세션은 그 영역을 손대지 않았다.

### 남은 것

- `origin/subagent-init-archi` 삭제 여부 (사용자 판단) — NEXT.md 항목 1.
- 다른 세션 종료 후 그쪽 summary 섹션·NEXT.md 항목까지 합쳐 한 번 더 훑기.
- ⑧ 검토 연결, ⑦ 산출물 서식/배점표 경로 — `## Session 07:59` 항목과 동일.

---

## Session 08:13 — 두 세션 결과 합쳐 교차검증 (마무리)

사용자가 "다른 세션 push 완료"를 알려와, 이 세션이 약속했던 **합동 점검**을 수행했다.
새로 구현한 것은 없고, 다른 세션의 산출물과 주장(NEXT.md 항목 2)을 직접 재현·검증했다.

### 검증 결과 — 전부 일치

- `main == origin/main`(`0 0`). 원격 브랜치 3개: `main`(`66f1625`),
  `worktree-corpus-validation`(`8d94f88`), `subagent-init-archi`(`4d721a9`).
  항목 2가 주장한 "브랜치 origin push 완료"는 사실 — 다른 PC에서 이어받을 수 있다.
- 워크트리 `.claude/worktrees/corpus-validation`에서 `py -3 -m pytest backend agent -q`
  → **148 passed**(backend 124 + agent 24). 항목 2의 예측치와 정확히 일치, 워킹트리 클린.
- **병합 충돌 0건** — 분기점 `3ef30a1` 이후 main에 쌓인 6커밋이 전부 `handoff/`·`docs/`
  문서라 코드가 겹치지 않는다(`git merge-tree`로 확인). 즉 항목 2에 남은 것은
  **전체 브랜치 최종 리뷰뿐**이고 기술적 장애물은 없다.
- 브랜치 변경분 13파일 833줄: `corpus_validator.py` + 테스트 2종(208·131줄) 신설,
  Task 게이팅(`db.py`/`models.py`/`bidcase_repository.py`), 스펙이 예고한 오타 2건
  (`dongjak`·`gangbuk`의 `bank_ideas_draft.txt`) 수정.
- 이 세션의 기존 섹션 3개(`00:29`/`07:59`/`08:05`)는 무손상 — `git diff cd06db9 HEAD`의
  삭제 라인 0건으로 확인.

### 발견한 규약 위반 (고치지 않음)

- 다른 세션의 섹션 제목이 `## Session (풀 상태 감사 — …)`로 **`HH:mm`이 없고**,
  이 세션의 `## Session 08:05` **앞에 삽입**됐다. CLAUDE.md의 "`## Session HH:mm` 섹션을
  **시간순으로 이어붙인다**"에 어긋나 파일 내 순서가 `00:29 → 07:59 → (무시각) → 08:05`로
  뒤섞였다.
- **의도적으로 수정하지 않았다** — 같은 규약의 "기존 세션 섹션은 절대 고치거나 지우지
  않는다"가 우선하기 때문. 이후 세션은 **파일 맨 끝에 `HH:mm`을 붙여 append**할 것.

### NEXT.md 항목 처리

- 신규 항목 **추가 없음**(사용자 지시: 중첩 금지). 이번 점검에서 나온 사실은 전부
  기존 항목 2에 **상태 갱신으로 흡수**했다 — "병합 사전점검 완료" 불릿을 추가해
  다음 세션이 148 재실행·충돌 확인을 반복하지 않게 했다.
- 열린 항목은 그대로 4개: ①`subagent-init-archi` 삭제 여부 ②corpus-validation 병합(최우선)
  ③리포 재구성 계획 작성 ④빈 워크트리 디렉터리 정리.

### 다음 세션이 바로 할 일

`git diff main...worktree-corpus-validation` 전체 리뷰
(`superpowers:finishing-a-development-branch`) → clean이면 main 병합 → 148 확인 → push.

---

## Session 08:30 — 원격 브랜치 `origin/subagent-init-archi` 기록용 보관 결정

### 한 일

- **NEXT.md 항목 1 해소** — 원격 브랜치 `origin/subagent-init-archi`를 기록용으로 보관하기로
  사용자 결정.
  - **결정 근거**: 파일은 이미 main에 있고, `git diff origin/main origin/subagent-init-archi`에서
    내용 변경이 없음(482개 파일 62,367줄 삭제는 main이 더 최신이라는 뜻). 자동 안전 판정이
    불가능하지만(squash merge라 조상 판정 불가), 사람 확인 결과 main에만 있는 파일들
    (report/5-report_2.0.html, requirements.txt, log/ 등)이므로 branch-only 고유 산출물 없음.
  - **Action**:
    1. NEXT.md에서 "항목 1. 원격 브랜치 `origin/subagent-init-archi` 삭제 여부" 제거 완료.
    2. 다른 항목 번호 재정렬: 기존 항목 2→새 항목 1 (corpus-validation 병합),
       기존 항목 3→새 항목 2 (리포 재구성), 기존 항목 4→새 항목 3 (빈 워크트리).

### 상태

- **NEXT.md 항목 1(브랜치 삭제 여부) 해소 완료 ✅**
- 열린 항목: 이제 3개 (①corpus-validation 병합 ②리포 재구성 계획 ③빈 워크트리 정리).

---

## Session 11:14 — 리포 재구성 ①~③ 실행 + corpus-validation 최종 리뷰·병합

이 세션은 08:30 세션의 연속이다(같은 대화, 세션 재시작 2회 포함).

### 한 일

**1) NEXT.md 구 항목 2(리포 재구성) — 계획 작성 + ①~③단계 실행 완료**
- 브랜치 `restructure-stage1-3`에서 진행(사용자 "안정성 있게 신규 브랜치" 지시).
- 계획 `docs/superpowers/plans/2026-07-29-repo-restructure-stage1-3.md` 작성(`caaa198`).
  사전 조사: `tmp/`·`workflow/`는 **이미 없었음**(NEXT.md가 우려한
  `KB_AI_Lab_교육내용정리.html` 보존 문제 소멸), `RFP/`·`report/` 코드 참조 0건.
- ①`archive/` 신설(`eb0ad5d`): build 스크립트 2·gigan.zip·HTML 3·구청_log·한글화계획·
  `log/` 이동, 전부 100% rename. CLAUDE.md Layout 갱신.
- ②`data/` 분리(`baa1583`): 경로 기본값 4곳(`main.py`×2·`seed.py`·`assembler.py`)을
  `data/registry.db`·`data/report_new`로, `init_db`에 부모 폴더 생성 추가(유일한 로직
  변경), gitignore를 `data/*`+`!data/.gitkeep`로 통합, 실물 registry.db 이동.
- ③`corpus/` 신설(`04f3603`): `RFP/`→`corpus/rfp/`, `report/`→`corpus/reports/`,
  `corpus/inbox/` 생성. 살아있는 문서(CLAUDE.md·rfp-locate 스킬·실행가이드·
  system-overview.html) 경로 갱신 — 스킬의 1·2단계 잔여 참조(구청_log 경로,
  report_new→data/report_new)도 함께 수정. 날짜 박힌 과거 spec/plan·handoff는 기록물이라
  손대지 않음.
- 각 단계 후 96 passed 확인, main 병합(`7b9d7be`) 후 148 passed. 브랜치는 `-d` 삭제.

**2) NEXT.md 구 항목 1(corpus-validation) 해소 — 최종 리뷰 → fix → main 병합**
- 이 PC에 워크트리가 없어 원격 브랜치 기준으로 로컬 브랜치 생성해 진행.
- 전체 diff(13파일 +833줄) 최종 리뷰 → **Minor 2건**: 비UTF-8 파일이 plan/01·plan/05·
  홈페이지확인·spec/00 위치에 있으면 규칙9 보고 대신 예외 크래시(CLI traceback/API 500),
  규칙9 오류가 파일당 2회 중복 보고. `_read_quiet` 도입으로 수정(`a8e14fd`, 148 passed).
- `--no-ff` 병합(`c7ba0c7`) → main에서 **148 passed** (backend 124 + agent 24).
- **§⑦ ④단계(giganlist 이동) 게이트 해제됨.**

**3) 사용자 폴더 제안 3건 검토** (회신 대기 2건)
- `architecture/`→`docs/architecture/`: 찬성, **실행 완료**(`d5010d9`, 코드 참조 0건).
- `dashboard/`→`frontend/`: **보류 권고** — 스펙 결정로그 1(이름 유지)·§⑤(최종형 `web/`)와 충돌.
- `giganlist/`→`data/`: **반대** — `data/`는 gitignore된 시스템 생성물 전용(결정로그 5),
  스펙 목적지는 `corpus/institutions/`(§⑥). 사용자 회신 대기.

### 환경 사건 (다음 세션 필독)

- **Python 3.14가 3.15로 자동 교체돼 있었다** — 3.15는 pydantic-core·watchfiles 휠이
  없어 의존성 설치 불가. python.org에서 3.14.1을 받아 재설치(`py -3.14`로 실행,
  `py -3`은 이제 3.15를 가리킴). **테스트는 `py -3.14 -m pytest`로 실행할 것.**
- **push가 인증 문제로 차단** — 네트워크가 아니라 credential 부재(NEXT.md 새 항목 1).
  main의 병합 커밋들과 `worktree-corpus-validation`의 `a8e14fd`가 미push 상태.
- **다른 세션 동시 작업 목격**: `giganlist/` mapo·seocho spec 파일 3개가 워킹트리에서
  수정 상태(이 세션은 안 건드림), NEXT.md도 이 세션 도중 디스크에서 변경됨.

### NEXT.md 항목 처리

- 구 항목 1(corpus-validation 병합) **해소** → 해소 로그로 이동.
- 구 항목 2(재구성)를 "①~③ 완료, ④는 목적지 결정 대기"로 갱신.
- 신규 항목 1 추가: main 미push 커밋 — 사용자 인증 필요.

---

## Session 13:38 — ④단계(giganlist → corpus/institutions/) 실행·병합

11:14 세션의 직접 연속. 사용자 결정 2건 접수: ④단계 목적지는 스펙대로
`corpus/institutions/`(`data/` 제안 철회), `dashboard/`→`frontend/`는 최종형 개명 때로 보류.

### 한 일

- 브랜치 `restructure-stage4`에서 §⑥ 요구대로 **한 커밋**에 묶어 실행(`acdcd49`):
  - `git mv giganlist corpus/institutions` (25개 기관 폴더, 100% rename)
  - `backend/seed.py`(루트 경로)·`backend/repository.py`(저장값 접두사
    `corpus/institutions/{id}`)·`backend/corpus_validator.py`(docstring)·
    테스트 8개 파일의 경로 리터럴·`agent/pipeline.py` 기본값·`agent/state.py` 주석
  - 살아있는 문서: CLAUDE.md, `institution-corpus-format` 스킬(SKILL.md+샘플 3),
    실행가이드, system-overview.html
  - tmp_path 픽스처의 `"giganlist"` 문자열은 리포 경로가 아니므로 유지
- **sed 부작용 1건 수정**: traversal 테스트의 `giganlist/../..`(리포 밖)이
  `corpus/institutions/../..`(=리포 루트, 안쪽)로 바뀌어 400→422 실패 —
  `../../..`로 수정해 원래 의도 복원.
- `data/registry.db`가 **비어 있던 것을 발견**(기존 registry.db가 언젠가 빈 스키마로
  재생성돼 있었음) → `py -3.14 -m backend.seed`로 25개 기관 재시딩, 새 접두사 확인.
- 루트에 재출현한 `registry.db`(빈 스키마)는 병합 전 브랜치에서 pytest가 옛 기본값으로
  import 시 만든 부산물로 확인 후 삭제.
- main 병합(`--no-ff`) 후 **148 passed**. 브랜치 `-d` 삭제.

### 동시 세션 처리

- 다른 세션의 미커밋 수정(mapo·seocho spec 3파일)이 `git mv`를 따라 새 경로로 이동
  (`RM` 상태) — 커밋에서 의도적으로 제외하고, main 전환이 막히자 **stash → 병합 →
  pop**으로 새 경로에 무손상 복원했다. 그 세션이 옛 경로(`giganlist/...`)에 쓰기를
  시도하면 폴더가 재생성될 수 있으니 다음 세션은 루트에 `giganlist/`가 다시 생겼는지
  확인할 것.

### NEXT.md 항목 처리

- 항목 2(재구성)를 "①~④ 완료, 남은 것 ⑤~⑦"로 갱신. dashboard 보류 결정 기록.
- 항목 1(미push)은 그대로 — 이 세션 커밋들이 쌓여 미push 폭이 더 커졌다(사용자 인증 대기).

### 추기 (13:46) — push 해소

사용자가 세션 안에서 `! git push origin main worktree-corpus-validation`을 직접 실행해
인증과 함께 push 완료. `origin/main...main` = `0 0` 검증. NEXT.md 항목 1(미push) 해소,
항목 번호 재정렬(2→1 재구성, 3→2 빈 워크트리).

## Session 15:25

**재구성 ⑤단계 (agent/retrieval FTS) — 스펙·플랜·구현·병합까지 완결.**

- 스펙 `docs/superpowers/specs/2026-07-29-agent-retrieval-fts-design.md`(`1e29e43`) 작성
  후 사용자가 plan 모드로 실행 플랜 승인 →
  `docs/superpowers/plans/2026-07-29-agent-retrieval-fts.md`(`6fdc349`).
- 브랜치 `agent-retrieval-fts`에서 Task 2~7 구현(태스크당 커밋 1개):
  - `agent/retrieval/` — parsers(.txt 레지스트리)·chunker(문단 800자 병합)·
    indexer(FTS5 trigram, `.tmp` 빌드 후 `os.replace` 원자 교체)·
    `search()` 단일 시그니처(`RetrievedChunk`, `IndexNotBuiltError`)·CLI(build/search).
  - `GET /search`(`backend/routers/search.py`, `create_app(index_db_path=…)` 주입,
    인덱스 부재 시 503).
  - agent_adapter 통합: `giganlist_dir`가 `corpus/institutions/` 접두사 + 인덱스 존재
    + 결과 1건 이상일 때만 검색 경로, 아니면 legacy 통째-읽기 폴백 — 기존 어댑터
    테스트 3개 무수정 통과로 폴백 보존 증명. 인용 형식 `[경로#청크번호]`.
- **구현 중 스펙 수정 1건**: 질의 전체를 단일 구문 리터럴로 감싸는 원안은 채팅
  문장이 코퍼스에 통째로 실재해야만 매치됨을 실측 확인 → **토큰(3자↑)+인접
  토큰쌍 OR 구문**으로 변경(한국어 2자 단어는 인접쌍으로 매치), 스펙 §⑤ 갱신(`209847e`).
- 실코퍼스 스모크: 25개 구 인덱스 빌드(파일 408, 청크 2763). "청년 창업 지원" →
  서대문/중구 plan·송파 bank_ideas 상위, `--institution dobong` 필터 정상.
  CLI가 Windows 콘솔 cp949로 크래시 → stdout/stderr UTF-8 reconfigure로 수정(`f9fbaad`).
- 테스트 **178 passed**(기준선 148+신규 30), dashboard **36/36**. `--no-ff` 병합
  `0af3f55` → main push 완료(브랜치 `agent-retrieval-fts`도 push).
- NEXT.md 항목 1 갱신: ①~⑤ 완료, 남은 것 ⑥(collector/SCHEMA.md)·⑦(최종형 개명).

## Session 15:51

**재구성 ⑥단계 (`collector/SCHEMA.md`) 작성 — 이로써 ①~⑥ 완료, 남은 것은 ⑦뿐.**

- `collector/SCHEMA.md` v1 작성(`5cfc4e6`, main push). 스펙 §⑦-6대로 **문서까지만,
  코드 없음**. 핵심: 망 밖 수집기와 망 안의 유일한 접점을 파일 형식으로 고정.
  - 배치 폴더 1개 = `manifest.json`(권위) + `institutions.csv`(파생) + `files/`.
    폴더명 `YYYY-MM-DD_HHmm_<source_slug>`.
  - dedup 키 `(source.slug, notice_id)`, 배치 불변·나중 배치 우선,
    `schema_version` 정책(선택 필드 추가는 버전 불변, 모르는 필드는 무시).
  - **`institution_id`는 망 밖에서 발급 금지** — 슬러그 발급은 망 안 권한
    (`upsert_institution`이 이름 매칭 실패 시 `new-<hex8>` 발급).
  - 추측 금지 원칙: 공고에 없는 값은 공란(망 안 upsert가 `COALESCE`라 공란은
    무해하지만 틀린 추측은 확정 데이터를 덮어쓴다).
- **조사 중 발견**: CSV 소비자가 둘이고 읽는 열이 다르다 —
  `backend/csv_import.py`는 6열, `dashboard/js/logic.js`는 12열(구시군코드·확정여부·
  경도/위도·출처·수정일 추가). 둘 다 모르는 열을 무시하므로 **12열 상위집합** 하나로
  정하고, 예제 CSV(UTF-8 BOM)를 **두 파서에 실제로 통과시켜 검증**했다
  (backend 6필드 정상 추출, dashboard `confirmed:true`·`sources` 세미콜론 분리).
  manifest 예시도 JSON 파싱 확인. 검증 내역은 SCHEMA.md §⑦에 기록.
- `CLAUDE.md` Layout에 `collector/` 항목 추가(코드 없이 SCHEMA.md만, 이 파일이
  계약이라는 점, `backend/csv_import.py` 헤더맵을 고치면 계약이 깨진다는 경고).
- **⑦(최종형 개명)은 착수하지 않음** — 착수 조건 ⓐ사용자 결정 "최종형 됐을 때"
  ⓑ스펙 §⑤ "backend 작업이 잠잠해진 뒤"가 둘 다 미충족(같은 날 `routers/search.py`
  신설·`create_app` 시그니처 변경). NEXT.md에 "조건부 보류, 착수 금지"로 명시.
- **NEXT.md 항목 2(빈 워크트리 디렉터리) 해소** — 이 PC에 `.claude/worktrees` 자체가
  없고 `git worktree list`도 메인 리포만 반환. 과거 `skill-essential` 잔해와 같이
  자연 정리된 것으로 보여 "해소된 항목"으로 이동. 별도 조치 불필요.
- 테스트 178 passed 유지(문서 작업이라 코드 무변경).

## Session 18:14

**E2E 스펙 sub-project 1(DMZ 수집 서비스) 설계·구현·병합 완결.** 사용자가 "1번(DMZ
FastAPI)"을 선택하며 "나중에 테스트할 때는 같은 IP에서 포트만 바꿔 주고받는 게 필요할
것 같다"는 제약을 함께 줬고, 그 제약이 설계의 핵심 결정이 됐다.

- **설계** `docs/superpowers/specs/2026-07-29-dmz-collector-service-design.md`(`557fe77`).
  핵심 결정 §③ — 테스트 편의를 위해 두 서비스를 직접 연결하면 `collector/SCHEMA.md` §⑧의
  "파일 단방향" 원칙이 깨지고 운영에서 성립하지 않는 코드가 남는다. 그래서 **브리지
  CLI가 사람의 대역**을 맡는 안을 채택: DMZ(8001)와 망 안(8000)은 **서로의 주소를
  모르고**, 주소를 아는 것은 브리지뿐이며 운영에서는 그 자리에 사람+USB가 들어간다.
  기각안 2개(망 안이 pull / DMZ가 push)의 이유도 기록.
- **플랜** `docs/superpowers/plans/2026-07-29-dmz-collector-service.md`, 브랜치
  `dmz-collector-service`에서 Task 2~7을 태스크당 커밋 1개로 구현.
- **구현** (`collector/`, 신규 의존성 0):
  - `sources/base.py`(어댑터 인터페이스·레지스트리, 날짜/필수필드 검증) +
    `sources/fixture.py`(로컬 JSON, 네트워크 불필요) — **실사이트 크롤러는 범위 밖**.
  - `batch.py` — SCHEMA v1 배치 생성. manifest(BOM 없음)/CSV(BOM 있음), 기관당 1행
    합치기(확정 우선, 동률이면 posted_at 최신), 첨부 경로 이탈 거부,
    **자기검사 실패 시 배치를 남기지 않음**.
  - `schema.py` — 배치 검증(표준 라이브러리만). 상위 schema_version은 즉시 거부.
  - `app.py` — FastAPI :8001, health/sources/collect/batches/archive. batch_id
    경로 이탈 방어. 같은 분 재수집은 422(조용히 덮지 않음).
  - `bridge.py` — 반입 대행 CLI. 받아서 → inbox에 풀고 재검증 → `POST
    /institutions/import`. 검증 실패·zip 경로 이탈 시 **inbox에 남기지 않음**.
- **테스트로 강제한 두 가지** (문서로만 두면 언젠가 깨지는 것들):
  - `test_boundary.py` — collector **런타임** 코드의 `backend`/`agent` import 금지.
  - `test_csv_contract.py` — 생성된 CSV를 **망 안 진짜 파서 두 개**에 통과시킨다
    (`backend.csv_import.parse_csv` 6열 + node로 `dashboard/js/logic.js` 12열).
    SCHEMA.md §⑦의 손검증을 자동 테스트로 승격한 것.
- **E2E 실측**: 두 서비스를 실제로 띄워(8001/8000) `POST /collect` → 브리지 →
  망 안에 기관 2건 upsert 확인. 콘솔에 한글이 깨져 보였으나 httpx·sqlite 직접 조회로
  **데이터는 정상**이고 curl→파이프 인코딩 표시 문제였음을 확인.
- 테스트 **233 passed**(기준선 178 + collector 55), dashboard 36/36.
  `--no-ff` 병합 `5fba9f0` → main push 완료(브랜치도 push).
- `docs/실행가이드_backend-agent.md`에 §4(수집 서비스·브리지 사용법) 추가.
- **NEXT.md 갱신**: 신규 항목 1(E2E sub-project 진행 상황) 추가 — sub-project 1 완료,
  남은 2·3·4·5를 각각 무엇이 미구현인지와 함께 적음. 특히 **배치 수신 처리
  (SCHEMA.md §⑥ 4·5·6: bid_cases 일정 갱신, PDF→corpus/rfp/, 배치 치우기)는
  sub-project 2의 몫으로 미구현**임을 명시. 기존 재구성 항목은 2번으로 밀림.
