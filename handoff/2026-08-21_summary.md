# 2026-08-21 작업 요약

## Session 10:05 — NEXT.md 항목 17·18 해소 (카드 본문 잘림 · 포트폴리오 디자인 지적)

세션 시작 시 `NEXT.md` + `2026-08-20_summary.md`가 훅으로 주입됐다. 사용자가 열린
항목 목록에서 **17·18을 먼저** 지정했다. 둘 다 원래 "사용자 시각 결정 대기" 상태라,
코드를 먼저 조사해 선택지를 수치로 만든 뒤 물었고 사용자가 ⓐ안(+ kicker·radius)을
확정했다.

**커밋: `b78e5f2`** (8 files changed, 120 insertions, 11 deletions). 푸시는 하지 않았다
— 사용자가 커밋만 지시했다.

### 항목 17 — 원인이 둘이었고, NEXT.md의 추정은 절반만 맞았다

NEXT.md는 "두 화면 모두 같은 증상이라 **카드 본문 렌더가 공통 원인**"이라고 적어
두었다. 실제로는 다음 둘이다:

1. **신호가 없다** — `.dg-body`는 `max-height:4.5em; overflow:hidden`인데 페이드도
   "더 보기"도 없어서 잘린 줄에서 글이 끝난 줄 알게 된다.
2. **결재함은 아예 못 폈다** — 펼치기 토글이 `designer.js:364`에만 배선돼 있었고
   `approvals.js`에는 없었다. 즉 **작업함은 원래 클릭하면 펴졌다**(신호만 없었다).
   결재함만 진짜로 막혀 있었고, 이쪽이 더 나쁘다 — 부장 화면에는 워크플로가 없어
   그 카드가 맥락의 전부인데 근거가 잘린 채 끝나면 그게 전문인 줄 알고 결재한다.

**사용자 선택 ⓐ안**(클램프 유지 + 신호 추가). 구현:

- `dashboard/index.html` — `.dg-body.clamped`에 하단 페이드 + `⌄ 더 보기`,
  `.clamped.open`에 `⌃ 접기`. **새 색·크기 리터럴 없이** 기존 토큰만 썼다
  (`var(--panel)`·`var(--accent-color)`·11px) → impeccable 훅 검출 0건.
- `dashboard/js/designer.js` — 배선을 `designer.wireBodyToggles(container)` 공통
  함수로 분리. **실제로 넘칠 때만**(`scrollHeight > clientHeight + 1`) `.clamped`를
  붙이고, 안 넘치면 `onclick`도 걸지 않는다 — 다 보이는 글의 "더 보기"는 그 자체가
  거짓말이다. 재렌더로 짧아지면 `clamped`·`open`을 함께 걷는다.
- `dashboard/js/approvals.js` — `paint()`에서 같은 함수를 호출(없던 배선 신설).

**변형본 6개**(`index_2.0_impec.html` 등)의 같은 CSS는 **사용자 지시로 그대로 뒀다**
— 정본 `dashboard/index.html`만 고친다.

### 항목 17 검증 — 실제로 띄워서 쟀다

`py -3.14 -m backend.demo --port 8000` + Playwright(msedge)로 실측:

| 확인 | 결과 |
|---|---|
| 결재함 카드 `clamped` | True |
| 클릭 전 → 후 → 재클릭 높이 | 59px → 89px → 59px |
| `::after` 문구 전환 | `⌄ 더 보기` → `⌃ 접기` |
| NEXT.md가 지목한 `근거자료: spec/02(1장-4), plan FN-1` | 펼치면 온전히 보인다 |
| 작업함 예산 카드(3줄, 안 넘침) | 힌트 **안 뜸** — "넘칠 때만" 규칙 실증 |

테스트: **pytest 759 passed**(기준선 유지), **node 248 passed**(기준선 244 + 신규 4).
`golden/`은 API 스냅샷이라 이 변경과 무관 — 재캡처 불필요.

`dashboard/test/designer.test.js`에 `wireBodyToggles` 테스트 4건 추가. 이 저장소
테스트는 jsdom을 쓰지 않으므로 **최소 DOM 스텁**(`classList`/`scrollHeight`)을
직접 만들었다 — 이 파일에서 DOM 스텁을 쓴 첫 사례다.

### 항목 18 — 훅이 보여준 28건은 실제로 33건이었다

`npx impeccable detect portfolio/index.html` 실측 **33건**. 차이는 훅이
`maxFindings: 5`로 끊어 보고하기 때문이고, CLI에서만 보이던 것이 **radius 1건 ·
kicker 2건**이었다. 사용자가 ⓐ안 + kicker·radius까지 함께 처리하도록 확정했다.

**고친 것**

- side-tab 2건 — `.flow-step`·`.note`의 `border-left:4px` 제거. 막대가 카드마다 같은
  색이라 구분에 기여하지 않으면서 탭처럼만 읽혔다. 강조는 단계 번호(`b`)가 진다.
- kicker 2건 — `OVERVIEW`·`TECH` 라벨 삭제. **`PART 1~4`는 남겼다** — 순서 정보를
  담고 있고 검출에도 걸리지 않는다(라벨 6개 중 2개만 검출된 이유가 이것으로 보인다).
- radius 1건 — 태그 `999px` → `14px`(DESIGN.md `rounded.lg`). 태그 높이가 22px라
  브라우저가 어차피 절반으로 클램프해 **화면상 같은 알약**이다.

**예외로 둔 것** — 색 21건·폰트 6건. `DESIGN.md`는 다크 대시보드의 디자인 시스템이고
`portfolio/index.html`은 8/5에 따로 만든 **라이트 테마 별개 문서**라, 서로 다른
산출물에 같은 팔레트를 댄 결과지 드리프트가 아니다. 사용자 확정으로
**파일 한정 value 예외**만 등록했다:

```
hook-admin.mjs ignore-value design-system-color     "*" --file "portfolio/index.html"
hook-admin.mjs ignore-value design-system-font-size "*" --file "portfolio/index.html"
```

`ignore-file`/`ignore-rule`은 **쓰지 않았다**(훅 정책: 사용자 승인 없이 금지, 그리고
파일 전체를 영구히 침묵시키므로 실제 UI 문서에는 과하다). 결과 **검출 0건**.
`em-dash-overuse` 1건은 advisory(실패 아님)라 한국어 본문을 건드리지 않았다.

### 캡처 19·20 교체

`portfolio/shots/19_작업함_디자이너.jpg`·`20_결재함.jpg`는 **버그가 찍힌 상태**였다
(항목 17이 증거로 인용한 바로 그 화면). `portfolio/capture.py 19 20`으로 같은 조건에
다시 찍고 **JPEG q92**로 변환해 교체했다(8/20 세션과 같은 방식). 크기 568→606KB,
243→256KB. 스테이징 디렉터리 `portfolio/shots_new/`는 정리했다.

> ⚠️ `portfolio/shots_new/`는 `capture.py`가 쓰는 스테이징 경로인데 **gitignore되어
> 있지 않다.** 캡처를 돌린 세션은 커밋 전에 지우거나 스테이징에서 빼야 한다.

### NEXT.md 정리

- **항목 17 제거** — "해소된 항목" 로그로 옮기며 커밋 `b78e5f2`와 위의 "원인이
  둘이었다"는 정정을 함께 남겼다.
- **항목 18 제거** — 같은 방식. 실측 33건·등록한 예외 2건·검출 0건을 남겼다.
- 두 항목이 목록 끝이라 다른 항목 번호에는 영향이 없다.

### 남은 것 / 확인 필요

- **푸시 안 함** — `b78e5f2`가 로컬 main에만 있다.
- **사용자가 새 개선점을 제기했다(미착수)**: 지도에서 지자체를 클릭하면 오른쪽 목록에
  포커스가 가면 좋겠다는 것. 이 세션에서는 조사 전이다.

---

## Session 11:03 — 지도 면 클릭 → 랭킹 목록 포커스 (`dashboard/js/render.js`)

같은 세션의 연속이지만 작업 단위가 달라 섹션을 나눈다. 사용자가 화면을 보다가
직접 제기했다: "지도에 지자체 표기 되어 있는데 지도상에서 눌렀을 때 오른쪽 목록에
focus가 가면 좋을 것 같다."

> ⚠️ 위 `## Session 10:05` 섹션의 "남은 것"에 이 건이 **미착수**로 적혀 있는데,
> 이 섹션에서 **완료**됐다(커밋 `b6e3ba2`). 세션 섹션은 append-only라 그쪽은
> 고치지 않는다 — 이 줄이 그 갱신이다.

**커밋: `b6e3ba2`** (2 files changed, 214 insertions, 6 deletions).

### 조사 — 반대 방향만 구현돼 있었다

| 방향 | 조사 전 상태 |
|---|---|
| 랭킹 카드 클릭 → 지도 | ✅ `selectInstitution` → `_blinkMunicipality`(구 외곽선) + 팝오버 |
| 카드 hover → 지도 | ✅ `highlightMarker` |
| 마커 클릭 → 선택 | ✅ `onMarkerClick` |
| **지자체 면 클릭 → 목록** | ❌ `path.subregion`에 `.on('click')` 자체가 없었다 |

지자체는 좌표가 없어 마커가 그려지지 않는다(`_blinkMunicipality` 주석). 즉
**면이 유일한 클릭 대상인데 핸들러가 없어서 눌러도 아무 일이 없었다.**

사용자 확정 **ⓐ안**: 목록 포커스만 — 마커 클릭과 달리 팝오버는 띄우지 않는다
(팝오버가 방금 끌어온 그 카드를 가릴 수 있다).

### 구현하며 걸린 함정 둘 (다음 세션이 같은 데 빠지지 않게)

1. **document 클릭 핸들러가 선택을 즉시 해제한다.** `render.js`의 전역 핸들러는
   `.rank-card`·`.marker` 밖 클릭을 **전부 '빈 곳'으로 보고** `_selectedName`을
   지운다. 면 클릭도 여기 걸려서, 멈추지 않으면 방금 한 선택이 곧바로 풀린다.
   → 레코드가 **있는** 면일 때만 `stopPropagation()`. 레코드 없는 회색 면은
   **일부러 흘려보내** 예전처럼 '빈 곳'으로 처리되게 뒀다.
2. **`scrollIntoView({block:'nearest'})`가 카드를 가장자리에 걸친다.** 최소 스크롤이라
   목록 밖에 있던 카드가 늘 패널 맨 아래에 붙어 들어와서 "옮겨왔다"는 게 눈에 안
   띈다. 조상 스크롤(페이지 전체)까지 건드리는 문제도 있다.
   → 패널의 `scrollTop`만 직접 옮겨 **가운데**로. 이미 다 보이는 카드는 움직이지
   않는다(목록이 이유 없이 튀면 그게 더 헷갈린다).

### 설계 — 색과 클릭이 어긋날 수 없게 만들었다

면 하나에 지자체 레코드가 여럿 걸린다. 서울에도 **`강동구청`/`강동구`,
`동대문구청`/`동대문구`, `동작구청`/`동작구`, `은평구청`/`은평구`처럼 중복 레코드**가
있다(원래 5개구와 25개구 배치의 명명이 섞인 결과로 보인다 — 이 세션 범위 밖).

처음엔 "면 색을 정한 레코드를 고른다"를 주석으로만 뒀는데, 테스트를 짜면서
그건 언젠가 어긋난다고 판단해 **`render.recordForFeature(feature)`** 를 만들고
`subUrgencyColor`와 `onSubregionClick`이 **같은 함수를 공유**하게 했다. 이제
규칙이 아니라 구조가 보장한다 — 어긋나면 색과 다른 기관이 선택돼 지도를 못 믿는다.

### 테스트 — `render.js`의 첫 테스트 파일

`dashboard/test/render.test.js` 신설(9건). **node 248 → 257 passed.**

`render.js`는 `const logic = root.logic` 방식이라 node에서 require하면
곧바로 터졌다(그래서 여태 테스트가 없었다). `designer.js`·`approvals.js`와 같은
`(typeof require !== 'undefined') ? require(...) : root.x` 패턴으로 헤더만 맞췄다
— 브라우저에는 `require`가 없으므로 동작은 그대로다.

고정한 것은 **면 클릭 규칙뿐**이다(색=클릭 일치 / 회색 면은 전파 안 막음 /
카드가 밖이면 가운데, 보이면 안 움직임). d3 렌더는 여전히 브라우저 실측으로 본다.
jsdom을 쓰지 않으므로 패널·카드는 최소 스텁으로 만들었다(`designer.test.js`와 같은 방식).

### 브라우저 실측 (서울, 랭킹 51건)

| 확인 | 결과 |
|---|---|
| 지자체 면 커서 | `pointer` |
| 목록 28번째 `강동구청` 면 클릭 | scrollTop 0 → **1117**, 카드가 패널 가운데로 |
| 선택 상태 | `.rank-card.hi` + 면 외곽선 강조 동시, 팝오버 `none` |
| 팝오버 열린 채 면 클릭 | 팝오버 닫히고 **선택 유지**(함정 ① 해소 확인) |
| 이미 보이는 `동대문구청` 면 클릭 | scrollTop **0 유지** |

**지도 색 회귀 없음** — `subUrgencyColor`를 건드렸으므로 서울 지역뷰를 재캡처해
기존 캡처와 비교했다: 평균 채널차 **0.011**, 24 초과 픽셀 **515개(0.008%)**.
JPEG 노이즈 수준이다.

### 알려진 한계

- **레코드 없는 회색 면 경로는 실데이터로 확인하지 못했다.** 활성 지역 16곳을
  전부 훑었는데 그런 면이 **하나도 없다**. 코드상의 방어이며 테스트로만 고정했다.
- 위에 적은 **기관명 중복 레코드**(`강동구청`/`강동구` 등)는 손대지 않았다. 랭킹
  목록에 같은 구가 두 줄로 뜨는 원인이라 언젠가 볼 만하지만, 데이터 정리는 이
  작업의 범위가 아니다.

### 커밋 / 푸시

- `b78e5f2` 항목 17·18 수정 → `ae0fe40` handoff 정리 → `b6e3ba2` 지도 면 클릭.
- 사용자 지시로 **세 커밋을 함께 push**했다(이 섹션을 담은 커밋 포함).

---

## Session 14:01 — 서울 25개 구 중복 카드 해소 (`serverdata.mergeUnion`)

사용자 지적: "강동구청/강동구 중복부터 정리해줘." 바로 앞 세션(`## Session 11:03`)이
지도 면 클릭을 만들다가 발견해 **"범위 밖"으로 적어 둔 항목**이다.

> ⚠️ `## Session 11:03`의 "알려진 한계"에 이 건이 *데이터 정리는 이 작업의 범위가
> 아니다*로 적혀 있는데, 이 섹션에서 **해소**됐다(커밋 `74d3112`). 세션 섹션은
> append-only라 그쪽은 고치지 않는다 — 이 줄이 그 갱신이다.

**커밋: `74d3112`** (17 files changed, 90 insertions, 5 deletions).

### 원인은 데이터가 아니라 병합 키였다

| 출처 | 이름 | 생성 위치 |
|---|---|---|
| 지도 번들 | `강동구청` | `dashboard/data/institutions.js:400` — `name: gu + '청'` |
| 서버 registry | `강동구` | `backend/repository.py` `GIGANLIST_DISTRICT_NAMES` |

`serverdata.mergeUnion`이 **원본 이름 문자열**로 맞추다 보니 25개 구가 전부 어긋나,
서버 행이 별도 레코드로 붙었다 — 서울 랭킹 카드 **26 → 51**.

**결정적 단서**: 지도의 `municipalityForFeature`는 이미 `logic.normalizeMuniName`
(`logic.js:31`, 끝의 '청'을 벗긴다)으로 둘을 같은 구로 보고 있었다. 정규화 규칙이
이미 저장소 안에 있었고 **병합만 그것을 안 쓰고 있었다.**

경기 블록(`:605`)도 `name + '청'`이라 같은 문제를 안고 있었고 함께 해결된다.
나머지 13개 지역은 '청'을 붙이지 않는다 — 즉 붙이는 쪽(서울·경기)이 소수다.

### 선택지와 결정

사용자에게 셋을 제시하고 **ⓐ**를 확정받았다.

- **ⓐ(채택) `mergeUnion`을 정규화 이름 기준으로** — `serverdata.js` 한 곳. 화면
  이름은 `강동구청` 유지. 서버·`golden/`·백엔드 테스트 전부 무영향.
- ⓑ 번들에서 `+ '청'` 제거 — 표기가 `강동구`가 되고 포트폴리오·리포트가 흔들린다.
- ⓒ 서버 이름을 `~구청`으로 — `golden/api` 스냅샷과 백엔드 테스트가 이름을 물고
  있어 위험이 가장 크다.

구현 시 **병합해도 이름은 번들 것을 남기도록 명시**했다(`Object.assign(..., {name: b.name})`).
서버 필드가 이름까지 덮으면 표기가 `강동구청`에서 `강동구`로 조용히 바뀐다.

### ⚠️ 중복 제거보다 큰 변화가 딸려 왔다

서울 25개 구는 여태 병합이 안 돼서 **서버·공고 데이터를 한 번도 못 받고 있었다.**
`mergeUnion`의 존재 이유가 "서버가 authoritative store"인데 서울만 그 경로가
끊겨 있던 셈이다. 이제 연결되면서:

- 랭킹 목록이 **전부 `미상`이던 것에서 실제 날짜로** 바뀐다
  (`2026-07-30(확정)`·`2026-05-20(추측)` …). 임박순 정렬이 비로소 의미를 갖는다.
- **지도 면 색이 2곳 바뀐다** — `도봉구`·`노원구`가 회색(미상) → 빨강. 공고일
  `2026-09-30`·`2026-05-20`이 실렸기 때문이다. 나머지 23곳은 그대로.
  색이 틀렸다가 맞아진 게 아니라 **없던 정보가 이제 지도에 도달한 것**이다.

25개 면 전부를 훑어 병합 전/후 임박도를 비교해 위 2곳임을 확인했다(나머지 23곳 동일).

### 테스트 — node 257 → 262

- `serverdata.test.js` **4건**: 구청/구가 한 장으로 병합 · 남는 이름은 번들 것 ·
  서버 전용 기관은 서버 이름 그대로 · **다른 구끼리는 안 합쳐진다**('청'을 벗기는
  정규화가 지나치지 않은지).
- `institutions_data.test.js` **1건**: 이 병합은 지역을 보지 않고 이름만 보므로
  **"서울 구 이름이 다른 지역과 겹치지 않는다"는 전제**에 기댄다. 그 전제를
  고정한다 — 깨지면 지역까지 포함한 키로 바꿔야 한다는 메시지를 assert에 넣었다.
  (현재 번들에서 구 단위는 서울뿐이고, 다른 지역은 시/군이라 충돌이 없다.)
- pytest **759 passed**(기준선 유지). 프런트 전용 변경이라 백엔드 무영향.

> 작성 중 발견: `region: '11'` 지자체는 **26건**이다 — 25개 구 + 광역 레코드
> `서울특별시`(subRegion 없음). 처음 25로 단언했다가 실패해서 `subRegion` 유무로
> 구 단위만 세도록 바로잡았다.

### 포트폴리오 캡처 14장 전면 재촬영

02(서울 지역뷰)·14(기관편집)는 랭킹 목록이 통째로 바뀌었고, **나머지도 상단 임박
티커의 `노원구`가 `노원구청`으로 바뀌어** 전부 한 단어씩 낡았다. 티커는 모든 화면의
공통 크롬이라 14장을 같은 조건(Playwright + msedge, 1600×1000 / DSF 2)으로 다시
찍고 JPEG q92로 교체했다. 총량 6.31 → 6.69MB. PART 4 리포트 4장은 정적 파일이라
그대로 둔다.

캡처 전 **빈 화면 자동 점검**을 넣었다(고유색 수가 50 미만이면 경고) — 8/20 세션이
겪은 "실패가 조용히 지나간다" 문제를 사람 눈에만 맡기지 않기 위해서다. 14장 전부
정상(고유색 5,906~27,090), 크기도 기존과 동일(19: 2173 · 20: 853 · 21: 1225).

### 남은 것

- 이 세션의 커밋 `74d3112`까지 **push 완료**.
- `portfolio/shots_new/`는 여전히 **gitignore되지 않았다**(`## Session 10:05`에서
  적어 둔 것과 동일). 이번에도 커밋 전에 수동으로 지웠다 — 반복되고 있으므로
  `.gitignore`에 넣을지 판단이 필요하다.

---

## Session 16:40 — 리포지토리 재구성 ⑦단계 실행 (`backend/`→`server/`, `dashboard/`→`frontend/`)

사용자가 "남은 일이 뭐야"라고 물어 `NEXT.md` 열린 6건을 정리해 보인 뒤,
**항목 2(디렉터리 개명)** 를 지목했다. 바로 할 수 있는 사실상 유일한 항목이었다 —
5는 하드웨어 대기, 14는 9월 재조회 대기, 6·9·10은 큰 결정이 먼저다.

**커밋: `2fefca0`** (177 files changed, 483 insertions, 473 deletions).
**NEXT.md 항목 2 종결** — 재구성 ①~⑦ 전 단계가 끝났다.

### 실행

스펙 `docs/superpowers/specs/2026-08-20-final-rename-design.md` §⑥의 8단계를
그대로 따랐다. 개명 3건:

```
backend/                        → server/
dashboard/                      → frontend/
docs/실행가이드_backend-agent.md → docs/실행가이드_server-agent.md
```

전부 `git mv`. **git이 rename 149건으로 인식**해 `git log --follow`가 끊기지 않는다.

고친 것: 파이썬 import **243곳** · 모듈 경로 문자열 **49곳**(대부분
`@patch("backend.routers.tasks…")` 류 mock 대상 — 안 고치면 테스트가 깨진다) ·
하드코딩 2곳 · 살아있는 문서·설정 약 130곳.

**스펙 §⑤의 "고친다" 표가 6개 파일만 실측해 두었는데 실제 살아있는 파일은 더
많았다** — `docs/architecture/system-overview.html`(29) · `PRODUCT.md`(6) ·
`DESIGN.md`(4) · `golden/README.md`(2) · `frontend/` 안의 코드 주석·README(17) ·
`portfolio/`(2) 등. 표가 아니라 **원칙**(살아있는 것 / 과거 기록)으로 판단해 포함했다.

안 고친 것(의도): `docs/superpowers/plans/`·`specs/`·`handoff/`·`archive/`의 과거
기록, 그리고 식별자·CLI 플래그(스펙 §⑨).

### `.claude/settings.local.json` — 앞으로 쓸 규칙 10곳만

스펙 §⑤가 "성격이 섞여 있다"고 짚은 그대로였다. 일반 명령 규칙 10곳
(`pytest backend agent -q`, `node --test dashboard/test/*.test.js`,
`py -3.14 -m backend.demo` 등)만 고치고, **옛 `sed` 한 줄·옛 커밋 메시지·일회성
조사 명령은 그대로 뒀다** — 고쳐도 이득이 없고 "이 명령을 승인한 적 있다"는 기록이
거짓이 된다. 최종 잔여 검사에서 걸리는 것은 이 과거 흔적들뿐이다(의도된 상태).

### 검증 — 스펙 §⑦ 전부 통과

| 기준 | 결과 |
|---|---|
| `pytest agent server collector -q` | **759 passed** (기준선 그대로) |
| `node --test frontend/test/*.test.js` | **262 passed** (기준선 그대로) |
| `from backend` / `import backend` 잔여 | **0** |
| **golden 스냅샷 34건 재캡처 diff** | **0** |
| 실기동 `py -3.14 -m server.demo` | index·js·data·geo·vendor·API 전부 200, 마운트 warn 없음 |
| 브라우저 | 지도 폴리곤 16 · 서울 랭킹 26 · 깨진 이미지 0 |

> **착수 전에도 golden을 한 번 떴다** — 그때도 diff 0인 것을 확인해야 사후 diff 0이
> 의미를 갖는다. 이 순서를 건너뛰면 "원래 흔들리는 스냅샷"과 구별할 수 없다.

남는 `favicon.ico` 404는 **개명 이전 서버에서도 동일**함을 대조로 확인했다(기존
문제, 이 작업과 무관). 확인하려고 `git stash`로 작업분을 잠깐 내렸다가 `pop`했는데,
**포트 8009의 옛 서버 프로세스가 남아** 워킹트리가 바뀐 뒤에도 살아 있었다 —
`taskkill`로 정리했다. 같은 대조를 할 세션은 프로세스 정리를 잊지 말 것.

### ⚠️ 함정 3가지 (다시 일괄 개명을 할 세션을 위해)

1. **`args.backend`가 두 번 깨졌다.** `\bbackend\.` 규칙이 argparse 속성
   접근까지 바꿔 `args.server`가 됐다. **테스트로는 안 잡힌다**(CLI 실행 경로).
   `--backend` 플래그는 스펙 §⑨대로 유지하기로 했으므로 되돌리고, 그 자리에
   "플래그에서 나온 속성이라 이름이 그대로다"라는 주석을 남겼다.
2. **`git mv` 뒤에도 `backend/__pycache__`가 남는다** — `.pyc` 42개, 추적 0건.
   지우지 않으면 옛 모듈이 import로 되살아날 여지가 있다. 지운 뒤
   `import backend` → `ModuleNotFoundError`인 것을 실측했다.
3. **mermaid 다이어그램의 `\ndashboard/`** 는 앞 글자가 `n`이라 단어 경계 규칙에
   걸리지 않는다. `system-overview.html` 2곳을 따로 고쳤다.

### 재구성 스펙 §⑤ 갱신

§⑤는 프런트를 `web/`이라 적고 있었다. `frontend/` 확정과 그 근거(§③: `web/`은
통합 프런트를 담는 상위 그릇인데 그 통합은 스펙조차 없는 미래 일)를 갱신 블록으로
남겼다(§⑥-6). 문서끼리 충돌을 남기지 않는다는 재구성 §⑨-4의 원칙 그대로다.

### 곁가지 — 포트폴리오 수치 현행화 (`a83223a`)

`portfolio/index.html`의 프런트 테스트 수치가 **244**(8/20 시점)에 머물러 있었다.
오늘 하루에 18건이 늘어(wireBodyToggles 4 · render 9 · serverdata 4 ·
institutions_data 1) **262**가 맞다. KPI 카드와 기술표 두 곳, 기준 커밋 표기까지
갱신했다. **개명 커밋에 섞지 않고 별도 커밋**으로 뒀다 — 무관한 수치 정정이다.

### 남은 것

- `NEXT.md` 열린 항목은 이제 **5건**(5·6·9·10·14). 항목 2는 "해소된 항목"으로 옮겼다.
- 그중 **바로 착수 가능한 것은 없다** — 5는 dGPU/폐쇄망 엔드포인트 대기,
  14는 9월 재조회 시점 대기, 6·9·10은 사용자 결정이 먼저 필요하다.
