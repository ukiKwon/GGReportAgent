# 2026-07-30 작업 요약

## Session 01:00 (corpus-validation 종결 + sub-project 2 설계)

### 1. corpus-validation 브랜치 종결 — NEXT.md 잔여 정리 항목 해소

세션 시작 시점의 인식("최종 리뷰가 남아 있다")이 **틀렸음을 확인**했다.
`git pull` 후 실제 상태를 조사한 결과:

- 다른 세션이 이미 **최종 리뷰까지 마치고 main에 병합**했다(`c7ba0c7`, `--no-ff`).
- 그 리뷰에서 나온 fix 커밋 `a8e14fd`(비UTF-8 파일이 `plan/01` 등에 있으면
  `validate_corpus`가 규칙 9 보고 대신 크래시 / 규칙 9 중복 보고)도 브랜치에
  얹혀 병합에 포함돼 있었다. 내용을 직접 읽어 정당한 버그픽스임을 확인했다.
- `git merge-base --is-ancestor origin/worktree-corpus-validation main` → **fully merged**.
- 그 위로 재구성 ①~⑥단계와 sub-project 1(DMZ 수집기)까지 이미 쌓여 있었다.

따라서 PR 생성은 불필요했고, `finishing-a-development-branch` 스킬의 정리 단계만
수행했다:

- 로컬 워크트리 `.claude/worktrees/corpus-validation` 제거 + `git worktree prune`
- 로컬 브랜치 `worktree-corpus-validation` 삭제(`git branch -d`, 완전 병합 확인됨)
- 원격 `origin/worktree-corpus-validation` 삭제(`git push origin --delete`)
- 삭제 전 `main` 전체 테스트 **178 passed** 확인

**교훈**: 이 리포는 여러 PC·세션이 같은 브랜치를 밀고 있어서, 세션 시작 시
NEXT.md와 실제 git 상태가 어긋날 수 있다. **작업 착수 전 `git pull` + 실제 상태
조사를 먼저** 하는 것이 맞다(이번엔 그 덕에 불필요한 리뷰·PR 작업을 피했다).

### 2. 남은 일 재점검 — 오늘 범위를 2·4로 좁힘

NEXT.md 항목 1(E2E 워크플로)의 남은 sub-project 2·3·4·5를 코드 기준으로 재평가했다.

| sub-project | 판정 |
|---|---|
| **2 폐쇄망 백엔드 코어** | 경계 명확, 하루 가능 |
| **4 3팀 분화** | `content_writer.py` 73줄 + `role_router_node` 신규. 하루 가능 |
| **3 `rfp_locate_node`** | ⚠️ **2를 하면 존재이유가 상당부분 사라진다** — 배치가 첨부 PDF를 싣고 오고 `rfp_path`까지 기록되므로 "공고문을 찾아온다"는 노드가 할 일이 없어진다. 실사이트 크롤링은 스펙상 범위 밖. **2 완료 후 재정의 필요** |
| **5 통합 프런트** | dashboard JS 1,104줄 이식 + 9단계 워크플로 UI. 게다가 재구성 ⑦(착수 금지 상태)이 딸려옴. **하루짜리 아님** |

사용자 결정: **오늘은 2 → 4**, 3은 2 이후 재정의, 5는 별도 일정.

### 3. sub-project 2 설계 완료 — 커밋 `e8048f8`

`docs/superpowers/specs/2026-07-30-inbox-batch-import-design.md` (313줄).
`collector/SCHEMA.md` §⑧이 후속 스펙으로 미뤄둔 §⑥ 2·4·5·6의 구현 설계다.

**착수 전 중요한 발견**: §⑥ 2단계(배치 검증)는 **이미 구현돼 있다.**
`collector/schema.py`의 `validate_batch()`가 §⑥ 2가 요구하는 5가지
(`schema_version` 지원 여부·필수 필드·날짜 형식·`attachments` 실재·경로 이탈)를
정확히 그대로 수행한다. 문제는 그것이 **망 밖 패키지**에 있다는 것뿐이었다.

**사용자와 확정한 결정 3가지**

1. **검증기를 중립 계약 모듈 `contract/batch_schema.py`로 승격**(`git mv`).
   재구현하면 SCHEMA v2 때 두 곳을 고쳐야 하고(두 개의 진실), `backend`가
   `collector`를 import하면 폐쇄망 배포에 DMZ FastAPI 앱이 딸려온다. 순수 stdlib
   형식 파서 공유는 네트워크 경로를 만들지 않는다. 기존 경계 테스트는 정규식이
   `(backend|agent)`만 보므로 **수정 없이 유효**하다.
2. **`bid_cases`에 `source_slug`/`notice_id`/`title`/`notice_url` 추가** +
   유니크 인덱스. SCHEMA §④가 선언한 유일키 `(source.slug, notice_id)`를 저장할
   곳이 없어서 "나중 배치가 이긴다"를 구현할 수단 자체가 없던 상태였다.
3. **처리된 배치는 `data/batches/`로 이동**. `.gitignore`가 `data/*`를 이미 전부
   제외하므로 수집 원문·PDF가 실수로 커밋되지 않고, `corpus/inbox/`는 "미처리만"이
   문자 그대로 성립한다. 삭제하지 않는 이유는 `evidence.url`·수집 시각이 반입
   근거라 감사에 필요하기 때문.

**API**: `POST /inbox/{batch_id}/validate`(검사만) + `POST /inbox/{batch_id}/import`
— 기존 `POST /institutions/{id}/corpus/validate` + `POST .../corpus` 관례와 대칭.

**설계에서 스스로 잡은 모호함 2건**(자체 검증 단계에서 발견해 스펙에 반영):

- `schedule`에 날짜가 4개인데 어느 것을 `expected_date`/`confirmed_date`로 쓸지
  안 정해져 있었다 → **`deadline_at` 우선, 없으면 `contract_end` 폴백**으로 명시.
  `confidence`가 `확정`이면 `confirmed_date`, `예상`이면 `expected_date`에 넣고
  **반대쪽 컬럼은 건드리지 않는다**(예상→확정 승격 시 이력 보존).
- 경로 안전성을 문자열 차단 목록으로 하려던 것을 **`BATCH_ID_RE` 형식 검사(허용
  목록)** 로 변경 — `/`·`\`·`..`·`:`를 애초에 허용하지 않아 경로 이탈이 구조적으로
  불가능하다.

**커밋 전 실측 검증 2건**(스펙이 기대는 전제라 직접 확인함):

- SQLite 유니크 인덱스는 `NULL`을 서로 다른 값으로 취급 → `NULL` 행 3건 삽입 성공,
  비-`NULL` 중복만 거부됨. **기존 수동/seed `bid_case`는 무영향**.
- `BATCH_ID_RE`가 `../../etc`·`C:/windows`·`a/b`·`..`·빈 문자열을 **전부 거부**.

### 4. 부수적으로 발견한 것 2건 (조치 안 함, NEXT.md로 이월)

- **리포 루트에 stale `registry.db`** — 7/28 22:20자, 재구성으로 `data/`가 표준이
  되기 전의 잔해다. 기관 25건·bid_case 1건·task 3건이 들어 있고 **`.gitignore`에
  안 걸린다**. 이 리포는 세션끼리 서로의 untracked 파일을 커밋에 쓸어담은 이력이
  실제로 있어(2026-07-29 `cd06db9`), 40KB 바이너리 DB가 실수로 커밋될 여지가 있다.
- **`data/registry.db`가 비어 있다** — 0건. NEXT.md에는 재구성 ④단계에서 25개
  기관으로 재시딩했다고 적혀 있는데 현재 0건이다(그 작업이 워크트리에서만 이뤄졌을
  가능성). 서버를 띄우면 빈 레지스트리를 보게 되므로 `py -3 -m backend.seed` 필요.

### 이번 세션에서 해소한 NEXT.md 항목

- **없음** — 항목 1·2 모두 열린 상태 유지. 다만 항목 1의 하위 진행상황(sub-project 2
  설계 완료, 3의 재정의 필요성, 5의 규모 실측)을 갱신했다. 세션 시작 시 인식과 달리
  corpus-validation 관련 항목은 **이미 이전 세션에서 해소돼 있었고**, 이번 세션은
  그 잔여 브랜치/워크트리만 정리했다.

### 다음 세션이 바로 할 일

`docs/superpowers/specs/2026-07-30-inbox-batch-import-design.md`에 대해
`superpowers:writing-plans`로 구현 계획을 작성하고 구현한다. 설계는 사용자 승인
완료 상태이며, 스펙 ⑦(파일 구조 표)과 ⑧(테스트 9건 목록)이 계획의 골격을 그대로
제공한다. 기준선 테스트는 **178 passed**.

---

## Session 11:03 (sub-project 2 구현 + 3 재정의 + LLM 어댑터)

**작업 브랜치 `local-2026-07-30`** — 세션 중 사용자가 "오늘 이 로컬 작업은 새 브랜치로
올려서 다른 PC에서 검토·merge하겠다"고 정해, main 직접 커밋 대신 브랜치에 쌓았다.
커밋 15개 전부 push 완료(`16d1576`~`b5c38ed`).

### 0. 세션 시작 — "push한 게 main에 없나?" 조사

사용자 질문("아침에 push한 것 같은데 못 넘긴 파일이 있나")에 전수 조사한 결과:

- **아침 push는 정상.** `e8048f8`→`6946590`→`92548d3` 모두 main·origin/main에 있었다.
- 미병합 브랜치는 `origin/subagent-init-archi` 하나뿐이었고, tip끼리 직접 비교하니
  **main이 완전 상위집합**이었다. 브랜치에만 있던 줄은 `giganlist/`→`corpus/institutions/`
  개명 **이전의 옛 경로 4줄**뿐. 사용자 승인 후 원격 삭제(복구용 SHA
  `4d721a9a83a39f520318123957e5469143cc3787` 기록).
- **진짜로 main에 없던 것은 이 PC의 커밋 안 된 워킹트리 변경**이었다 —
  코퍼스 3개 파일의 휴대폰 번호 마스킹(`010-9579-9246`→`010-****-****`).
  마스킹 도구가 붙인 UTF-8 BOM(383개 중 이 3개만)은 제거하고 커밋(`16d1576`).
- `.claude/settings.json`의 `enabledPlugins`는 **추적되는 공유 설정**이라 커밋하지 않고
  gitignore된 `.claude/settings.local.json`으로 옮겼다.

**교훈**: 미병합 브랜치를 볼 때 `git log main..branch`(커밋 목록)만 보면 "11개나
남았다"로 보이지만, `git diff main branch`(tip 대 tip)로 보면 건질 내용이 0일 수 있다.

### 1. sub-project 2 — 폐쇄망 배치 반입 (Task 1~8, 커밋 `0f2a067`~`53c72e0`)

스펙 `2026-07-30-inbox-batch-import-design.md` 그대로 구현. `SCHEMA.md` §⑥의 2·4·5·6단계.

- **`contract/batch_schema.py`** — `collector/schema.py`를 `git mv`한 중립 계약 모듈.
  재구현 금지 원칙을 지켰다. `test_contract_depends_on_nobody`를 추가해 contract가
  어느 진영에도 의존하지 않는 것을 **테스트로** 강제(문서로 두면 깨진다).
- **`bid_cases` 4컬럼 + 유니크 인덱스** + `init_db`의 멱등 `ALTER TABLE`.
- **`backend/inbox_import.py`** — DB 커밋 → 파일 이동 순서를 지킨다. 뒤집으면 DB 실패
  시 배치가 이미 사라져 재시도가 불가능하다.
- **API 2개** + `create_app(inbox_root=, rfp_root=, batches_root=)`.
- **브리지 교체** — `_import_csv` → `_import_batch`, `batch_id`만 넘긴다.

**스펙에 없어서 계획 단계에서 보정한 것 2건** (둘 다 실제로 필요했음):
1. 파일시스템 루트 3개를 `create_app` 파라미터로 뺐다 — 안 그러면 테스트가 실제
   `corpus/inbox`·`corpus/rfp`·`data/batches`를 오염시킨다.
2. 스펙 ⑧은 "collector 테스트는 import 1줄 외 무수정"이라 했지만, ⑥이 호출 경로를
   바꾸므로 `test_bridge.py`의 3개 테스트가 함께 바뀌어야 했다. **스펙 ⑧의 그 문장은
   부정확**하다(`test_boundary.py`·`test_batch.py`에 대한 판단은 맞았다).

**구현 중 발견해 고친 것**: 이 PC의 `data/registry.db`에 **`research_status` 컬럼이
아예 없었다**(더 예전 스키마 — 그 컬럼도 과거엔 재시드로 처리했기 때문). 그런 DB에서는
`create_bid_case`의 INSERT가 바로 깨지므로 같은 마이그레이션에 포함했다.

**E2E 실측** (DMZ 8001 → 브리지 → 망 안 8000, 실기관 25건 DB 사본):
기관 2건 upsert → bid_case 신규 2건 → 첨부 1건 `corpus/rfp/` 이동 + `rfp_path` 기록 →
배치 `data/batches/` 보관 → inbox 비워짐. **재수집 시 신규 0 / 갱신 2**(새 bid_case
안 생김). 날짜 규칙도 확인: 마포는 `deadline_at`이 확정이라 `confirmed_date`,
종로는 `deadline_at`이 없어 `contract_end`가 예상이라 `expected_date`.
오류 경로: 없는 배치 404, 형식 위반 400. 잔여물 정리 완료.

### 2. sub-project 3 재정의 + 구현 (Task 1~5, 커밋 `124c4cb`~`9fc502c`)

**재정의 결론 — 신규 노드 3개 중 실제로 만든 것은 1개.**

| 최초 구상 | 결말 |
|---|---|
| `rfp_locate_node` | **`rfp_extract_node`로 재정의.** "찾기"는 sub-project 2가 가져갔고(첨부 PDF가 `corpus/rfp/` + `rfp_path`) 실사이트 크롤링은 범위 밖이라, 남은 일이 `PDF → rfp_text.txt + rfp_scoring.json`뿐이었다. 실행 위치도 DMZ→폐쇄망 |
| `spec_research_node` | 폐기 (2026-07-29 확정) |
| `plan_writer_node` | `content_writer_node`에 흡수 |

상위 스펙 §④에 **재정의 표**로 기록했다 — 스펙에 낡은 이름이 남아 있으면 다음 세션이
그것을 진실로 읽는다.

- **`agent/rfp_text.py`** — 스킬 스크립트의 추출 함수를 옮겼다. 스킬은 얇은 CLI 래퍼가
  되어 같은 함수를 부른다. 사본을 두면 이상 판정 임계값이 갈라져 "사람이 처리했을 때"와
  "자동 처리했을 때"의 판단이 달라진다. `pypdf`를 `requirements.txt`에 추가(없었다).
- **`agent/nodes/rfp_extract.py`** — 이상 PDF는 추측하지 않고 `RfpExtractError`로 멈추고
  `rfp-locate` 스킬로 사람에게 넘긴다. 자동/수동 두 경로가 공존해야 하는 이유다.
- 파이프라인은 **산출물 두 파일이 없고 `rfp_path`가 있을 때만** 추출한다.

**설계 전제가 실물로 확인됨**: 수원시 공고문 추출 텍스트에서 배점표 컬럼 경계가 실제로
무너진다 — `"…안정성소   계 25가.외부기관의…"`, 배점이 `817`처럼 붙어 나온다.
기계적 파싱 불가 → LLM 필요라는 전제가 맞았다.

**부수적으로 고친 실제 버그**: `pptx_builder_node`가 `report_new_dir` 인자를 무시하고
리포 루트에 썼다. 기존 테스트가 `monkeypatch.chdir`만 하고 경로를 단언하지 않아 못
잡고 있었다. 회귀 테스트 2건 추가. 산출물 경로 3중 불일치(`report_new` /
`data/report_new` / 하드코딩)를 `data/report_new`로 통일.

### 3. LLM 백엔드를 운영 모델로 교체 (커밋 `b5c38ed`)

사용자 지시("운영에서 사용하는 모델로. 1순위 gpt-oss-120b, 폴백 llama 4.0")로
스펙 §⑥ 어댑터를 구현했다. 하드코딩 `gpt-4o-mini` + `getpass()` OpenAI 키 입력 제거.

| 환경변수 | 기본값 |
|---|---|
| `LLM_MODEL` | `gpt-oss-120b` |
| `LLM_FALLBACK_MODEL` | `llama-4-scout-17b-16e-instruct` |
| `LLM_BASE_URL` | `http://localhost:11434/v1` |
| `LLM_API_KEY` | `not-needed` |

**스펙에 없던 것 추가 — 폴백 체인.** `with_structured_output` **뒤에** 건다(스키마 강제
방식이 모델마다 달라 1순위가 구조화 단계에서 실패하는 경우까지 받아내야 한다).
호출부는 `structured_llm(스키마)` 하나로 통일. `getpass` 제거의 부수 효과로 **키가
없어도 실행이 멈추지 않아 비대화식 실행이 가능**해졌다.

### 4. LLM 실호출 실측 — Ollama (사용자가 세션 중 설치)

**하드웨어 한계 확인**: 이 PC는 RAM 15.7GB / Intel Iris Xe 내장 GPU라 `gpt-oss:120b`
(~65GB)를 못 돌린다. 그래서 `llama3.1:8b`로 **배선만** 검증했다.

| 검증 | 결과 |
|---|---|
| A. Ollama OpenAI-호환 경로에서 `json_schema` 구조화 출력 | ✅ 13.1s, 타입 검증 통과 |
| B. 1순위 모델 부재 → 폴백이 받아냄 | ✅ 4.4s — 폴백을 넣은 바로 그 상황 |
| C. 둘 다 부재 → 조용히 성공하지 않고 404로 실패 | ✅ |

**A가 통과한 것이 핵심**이다 — 코드만 봐서는 알 수 없던 유일한 위험(자체호스팅
엔드포인트가 `json_schema`를 지원하는가)이 해소됐다. `method="json_mode"` 대안은 불필요.

**⚠️ 배점표 품질은 8B로는 부족하다 (수원시 공고문 실측, 405초)**

- **분류(category) 5개는 정확히 맞췄고** 총점도 100으로 맞췄다.
- **개별 배점은 지어냈다.** 정답 `8/17/21/22/25/7`(6항목) → 산출
  `4/6/0/5/4/6/4/8/7/8/9/8/9/8/5/5`(16항목). 붙어 나온 배점(`817`, `8751`)을 항목에
  올바로 귀속하지 못했다.
- 주석(`※ 경영지표 평가 세부항목별 등급기준은…`)을 **평가 항목으로 오인**해 0점 항목을
  만들었다.
- 프롬프트에 "원문에 없는 배점을 만들지 마라"고 명시했는데도 그랬다.

**이 실측의 의미**: 5·8단계의 🛑 사람 승인 체크포인트가 **장식이 아니라 필수**임이
수치로 확인됐다. `gpt-oss-120b`가 이걸 해내는지는 **여전히 미검증**이고, 그 검증은
GPU 서버가 있어야 가능하다.

### 이번 세션에서 해소한 NEXT.md 항목

- **항목 1의 sub-project 2** — 완료(커밋 `0f2a067`~`53c72e0`).
- **항목 1의 sub-project 3** — 재정의 + 구현 완료(커밋 `124c4cb`~`9fc502c`).
- **항목 3(로컬 DB 파일 2건)** — 이 PC 기준 실측만 기록하고 **열어 둔다**: 루트
  `registry.db`는 이 PC에 없고(항목 ① 해당 없음), `data/registry.db`는 institutions
  25건으로 시딩돼 있다(항목 ② 이 PC에서는 충족). NEXT.md가 "파일이 없으니 해소됨으로
  지우지 말 것"이라 명시했으므로 그 지시를 따랐다.

### 새로 추가한 NEXT.md 항목

- **항목 4** — `institutions.scoring_table`이 아무도 안 쓰는 빈 슬롯.
- **항목 5** — `gpt-oss-120b` 실호출 미검증(이 세션에서 추가).

### 테스트

| 구간 | 시작 | 끝 |
|---|---|---|
| backend + agent | 178 | — |
| backend + agent + collector | 251 | **273 passed** |
| dashboard | 36/36 | 36/36 |

### 다음 세션이 바로 할 일

**sub-project 4 (6단계 3팀 분화)** — `role_router_node` 신규 + `content_writer.py`를
역할 파라미터화 + `ProposalState.sections`를 reducer 필드로. 하루 규모로 추정됨.
그 전에 `local-2026-07-30` 브랜치가 다른 PC에서 검토·merge됐는지 확인할 것.
