# 2026-07-26 작업 요약 (feature/dashboard-enhancements 브랜치)

> 주의: 같은 날 `main`에도 별도 스레드(강남 25개구 배치)의 `2026-07-26_summary.md`가
> 존재한다(브랜치 분기 상태). 본 파일은 **feature/dashboard-enhancements** 브랜치의
> 대시보드 2차 기능 확장 세션 기록이다. 병합 시 두 summary가 충돌하므로 병합 담당이
> 두 내용을 합쳐야 한다.

## Session (대시보드 2차 기능 확장 — 스펙→플랜→SDD Tasks 1~5)

### 한 일

- **오전(지도 복구, main 74e3afb)**: `dashboard/`의 자리표시자 네모 지도를 **실제 행정경계
  GeoJSON**(전국 17시도·서울 25구·경기 42시군, kostat 2018 → 표준코드 remap + DP 간소화)로
  교체. 드릴인/fly-to/오버레이 멈춤/탭2 카드 드릴인 버그 수정. 브라우저(로컬 http)로 검증
  완료. 커밋 `74e3afb`(main에 있음).
- **브레인스토밍 → 스펙**: `docs/superpowers/specs/2026-07-25-treasury-bid-dashboard-enhancements-design.md`
  (커밋 `94f583e`, 개정 `03f3395`). 핵심 결정:
  - 스키마: `term`(입찰주기 1~10년)·`lastBid`(지난입찰일)·`confirmed`(확정여부)·`updatedAt`
    (수정일)·`interest`(관심기관♥) 추가. **confidence는 저장 안 하고 유도**
    (confirmed=true만 확정, 기본 추측, 날짜 없으면 미상). 입찰예상일 표시=`날짜(확정|추측)`.
  - CSV 입력(라이브러리 없음, 한글헤더↔영문키, BOM). 기관 추가 폼. 내보내기 라벨 한글화.
  - 랭킹 개편(반응형·임박순↔관심도순·유형필터·♥·더보기 검색 팝업), 선택 마커 물결(ripple).
  - **③ 입찰정보 확인 Agent는 별도 후속**(요구사항 보존, 폐쇄망).
- **플랜**: `docs/superpowers/plans/2026-07-25-treasury-bid-dashboard-enhancements.md`
  (커밋 `c2835bc`). 12개 TDD 태스크.
- **SDD 실행(subagent-driven, 브랜치 `feature/dashboard-enhancements`)**: **Task 1~5 완료**.
  - T1 effectiveBid/confidence 유도 `8aba368` · T2 검증완화+한글라벨+formatBidDate `f5ff5ef`
    · T3 sortByInterest `9c56e9f` · T4 CSV parseCsv/template `0450831` · T5 store 관심기관/데이터
    저장 `f01b197`(cherry-pick 복구본).
  - T1~4 리뷰 클린(Spec✅/Approved). **T5 리뷰는 미실행**(중단됨).
  - 현재 `feature` 브랜치 tip = `f01b197`, **단위테스트 26/26 통과**. origin에 push됨.

### 사고: 브랜치 뒤섞임(복구 완료)

- 병렬 **강남 배치 워커**가 **같은 워크트리**에서 `main`으로 브랜치를 전환·커밋 → 내 Task 5
  구현자가 `main`에 잘못 커밋(`bef58c7`). Task 1~4는 feature에 안전했음.
- 복구: `feature` 체크아웃 → `bef58c7` cherry-pick(`f01b197`) → 26/26 확인. **유실 없음.**
- main엔 강남 작업 잔존(사용자 승인). **다음 세션부터는 git worktree로 격리**하기로 사용자 합의.

### 남은 것 (NEXT.md 항목 참조)

- **Task 5 리뷰 미실행** → 재개 시 먼저 리뷰(리뷰 패키지 `review-0450831..f01b197.diff` 이미 생성됨).
- **Task 6~12 미착수**: 6 export CSV템플릿 다운로드 · 7 샘플데이터 이관 · 8 index.html
  상단바/모달 · 9 랭킹 개편 · 10 더보기 모달 · 11 물결 · 12 기관추가폼+CSV배선.
- 이후 최종 브랜치 리뷰 → main 병합(강남 작업과의 handoff/데이터 충돌 정리 포함).
- 파킹된 이슈: T4 parseCsv 따옴표 내 개행(멀티라인 셀) 미지원 — known-limitation, 최종 리뷰서 재triage.
