# 2026-07-26 작업 요약

## Session 08:20 (25개 자치구 배치 — 강남구)

세션은 07-25 밤에 시작해 자정을 넘겨 07-26 오전에 종료. NEXT.md 항목 1(25개 자치구 배치
프로젝트) 재개, **강남구(gangnam) 1건** 완료. 이 세션에서 한 작업은 이것뿐이다.

### 사용자 지시로 변경된 제약

- **기존 "반드시 2-parallel" 제약 해제.** 사용자가 토큰 소모량을 고려해 **1개 구씩 순차
  진행**으로 변경 요청. 앞으로 남은 송파·강동도 1건씩 단독 디스패치한다.
- 각 구가 끝날 때마다 마무리(handoff 갱신 + 커밋 + push)하고 다음 작업을 사용자에게 확인.
- **마무리 범위는 이번 세션 작업분으로 한정** — 다른 세션에서 진행 중인 작업은 커밋에
  포함하지 않는다.

### 진행 경과 — 2번 중단 후 재개

단독 디스패치(Agent, subagent_type: general-purpose)했으나 두 번 중단됨. **둘 다 코드/데이터
문제가 아니라 실행 환경 문제**였다.

1. **1차 중단**: 이전 Claude Code 프로세스 종료로 에이전트가 stopped. 디스크 확인 결과
   `giganlist/gangnam/` 폴더 자체가 없었음(조사만 하고 파일 미생성). → 새로 띄우지 않고
   `SendMessage`로 **transcript에서 재개**(웹 검색 컨텍스트 보존 = 토큰 절약). 재개 메시지에
   "조사 확장보다 디스크 쓰기 우선" 지시 추가.
2. **2차 중단**: 세션 한도(`session limit · resets 2:30am`)로 API 에러 종료. 이때는
   `spec/01~08` 8개 파일이 디스크에 남아 있었음(1차 재개 때 넣은 "파일 먼저 쓰기" 지시가
   실제로 효과를 봄). 리셋 시각 경과 확인 후 재개, 남은 `spec/09`, `spec/00`, `plan/` 6개,
   `bank_ideas_draft.txt`만 생성하도록 **디스크 실제 상태를 명시**해서 재조사 방지.

→ **교훈**: 배치 디스패치 프롬프트에 "파일 하나 완성할 때마다 즉시 디스크에 쓰라(중단돼도
진행분 보존)"를 넣으면 중단 복구 비용이 크게 줄어든다. 남은 송파·강동 프롬프트에도 포함할 것.

### 산출물 검증 — 통과

- **spec 10개**(`00_인덱스` ~ `09_민원게시판_2026년정리`), **plan 정확히 6개**,
  **`bank_ideas_draft.txt`(복수형 ✓)**. 총 17개 파일, 3,040줄.
- bank_ideas: 5축(소상공인금융/청년금융/IT협력/복지주택금융/SOC참여), 아이디어 10개,
  3블록(`연계 구청사업/근거`/`구체적 상품/협력 형태`/`은행 기대효과`) 각 10개로 정확히 일치.
- **인용 수치를 spec 원문과 직접 대조 검증**: 예산 확인율 7.1%(4/56), 교차확인
  60.7/30.4/8.9%, 민원 안전교통 47.4%·도시환경 15.2%, 차액 13,308억(89.9%) — 전부 일치.
- 장(章) 구조는 강남구 자체 발표 5개 분야(주민생활/일자리경제/복지보육교육/문화체육/
  도시환경교통)를 그대로 채택.

### 강남구 핵심 수치 — 신뢰도 79/100

- **총예산 1조 4,804억원**(일반회계 14,362 + 특별회계 442 ✓).
- **사업 건수 불일치 병기**: 강남구 발표 57건 vs 조사 확인 56건. 총계는 1건 차이지만 분야별
  구성이 어긋남(복지·보육·교육 +7, 도시환경·교통 −4). 강남구가 **세 개 분류체계**(보도자료
  5분야 / 홈페이지 3탭 / 민원통계 9분야)를 병용하는 것이 원인으로 추정 — 감추지 않고 명시.
- **예산합 ≠ 총액 병기**: 사업별 예산 확인 4건(7.1%)·1,496억 → **차액 13,308억(89.9%) 확인
  불가**. 중소기업육성기금 400억은 기금 융자라 회계 성격이 달라 제외 시 1,096억(7.4%)·
  차액 13,708억(92.6%) — **두 계산 병기**.
- 교차확인: 확인됨 34(60.7%)/부분확인 17(30.4%)/확인안됨 5(8.9%). 분야별로 복지·보육·교육
  81.3% vs **도시환경·교통 25.0%**로 편차 큼.
- 신뢰도 79/100(인용정확도 26/30, 근거자료질 20/25, 중복회피 15/20, 문제진단 13/15,
  한계명시 10/10) — 서초 84, 구로/금천 84보다 낮음. 예산 비공개율이 주 원인.

### 작성 중 발견·정정된 문제 (에이전트 자체 검증 단계)

1. **허위 인용 1건 정정** — plan/01이 "spec/01에 기획예산과 표기 확인됨"이라 썼으나 spec 어디에도
   해당 부서명 없음 → "spec 근거 없는 순수 추정"으로 수정. 담당 컬럼에 확정/추정 구분 표기 도입.
2. **과장 표현 1건 정정** — plan/02 IT-3이 대치선경아파트를 "처리됨"(완료형)으로 서술, spec
   원문은 "교부 예정" → 수정 + 등급 A→B 하향.
3. **민원 통계 컬럼 라벨 오독 발견** — 페이지 표기(2열=전년동월, 3열=증감율)와 달리 산술검증
   결과 3열 = 2열÷1열이 18개 값 전부 일치 → 라벨 오독으로 판단, 1열만 신뢰값 채택.
4. **수치 불일치 병기**: 공동주택 예산(14억 vs 21억), 청년 소득상한(5,000 vs 6,000만원),
   상품권 발행액(680억 vs 650억), 통합돌봄 "50여+22=72", 수서동 도서관 착공일.

### 은행 협업 관점 최대 발견

강남구 중소기업·소상공인 대출이자 지원사업(업체당 3억, 연 2.0~2.5% 이차보전)의 **2026년 공고
협약 금융기관 7개 목록에 KB 미포함**. 공고 원문이 "담보능력이 없으면 대출 불가"를 명시해
창업초기·영세 소상공인이 구조적으로 배제 → FN-1 및 bank_ideas 축1의 근거.
금융상품명은 전부 일반 카테고리로만 표기, 실존 브랜드·지점명 미사용(경쟁 금융기관명은
spec/03(2-14)에 원문 추적 목적으로만 기록, bank_ideas 본문에서는 미사용) — 영등포 세션에서
있었던 실존 지점명 사고가 재발하지 않았음을 확인.

### 주요 확인 불가 항목

57개 사업 전체 원문 목록 / 사업별 예산 52건(92.9%) / 담당부서 34건(5분야는 8건 전부) /
**2026년 개별 민원 원문**(2025.9.24부터 국민신문고 연계로 제3자 열람이 구조적으로 불가) /
민원 다발 세부사유 / 신용보증재단 출연 현황 / 1인가구 실태 / K-테크밸리 실체.

### NEXT.md 갱신

- 항목 1: **17/20 → 18/20**, 다음은 **송파(songpa)**부터 재개. 2-parallel 제약을 사용자
  지시에 따라 "1건씩 순차"로 교체. "파일 즉시 디스크 쓰기" 지시를 재개 방법에 추가.

### git 처리 — 브랜치 착오와 정정 (중요)

세션 시작 시점에 리포가 **`main`이 아니라 `feature/dashboard-enhancements`에 체크아웃돼
있었다**(다른 세션의 dashboard 2차 확장 작업 브랜치, 커밋 4개 `8aba368`~`0450831`).
이를 모르고 커밋해서 강남 산출물이 그 기능 브랜치 위에 얹혔다(`3c756d0`).

- 사용자 선택으로 **main으로 이동**: `main` 체크아웃 → `3c756d0` cherry-pick(`a6d2896`) →
  `git branch -f feature/dashboard-enhancements 0450831`로 그 브랜치에서 내 커밋만 제거해
  **원래 상태로 복원**. dashboard 커밋 4개는 그대로 보존됨.
- **push는 보류**(사용자 선택). 이유: `main`이 `origin/main`보다 **5커밋 앞서** 있는데
  그중 4개가 다른 세션의 dashboard 작업이라, push하면 이번 세션 범위를 벗어난 커밋이
  같이 원격에 올라간다. "마무리는 이번 세션 작업분만" 지시와 충돌하므로 보류.
- 세션 종료 시 HEAD는 **`main`**(강남 파일이 디스크에 존재하는 상태). 다른 세션이
  dashboard 작업을 재개하려면 `feature/dashboard-enhancements`를 다시 체크아웃해야 한다.

→ **교훈**: 배치 작업 커밋 전 `git branch --show-current`로 브랜치를 먼저 확인할 것.
이 리포는 여러 세션이 같은 워킹트리를 공유하므로 HEAD가 바뀌어 있을 수 있다.

### 남은 것

- **송파(songpa)**, **강동(gangdong)** 2건 — 1건씩 순차 디스패치.
- **`main`의 미push 5커밋 처리**(강남 1 + 다른 세션 dashboard 4) — NEXT.md 항목 5 참고.
- 이번 세션에서 커밋한 것은 `giganlist/gangnam/`과 이 handoff 파일뿐. 다른 세션의 작업
  (워크트리 `rfp-agent-team` 브랜치, `feature/dashboard-enhancements`)은 내용 변경 없음.

## Session (대시보드 2차 기능 확장 — 스펙→플랜→SDD 12태스크 완료→main 병합)

> 이 섹션은 위 강남 세션과 **별개 스레드**(feature/dashboard-enhancements 브랜치)의 기록이다.
> 병합 시 두 세션 섹션을 한 파일에 보존함(append-only 규칙).

### 한 일 (요약)

- **오전 지도 복구(74e3afb)**: 자리표시자 네모 지도 → 실제 행정경계 GeoJSON(전국 17시도·서울
  25구·경기 42시군, kostat 2018 표준코드 remap+DP 간소화). 드릴인/fly-to/오버레이/탭2 카드
  버그 수정, 브라우저 검증.
- **스펙**(`2026-07-25-treasury-bid-dashboard-enhancements-design.md`) + **플랜**(12 TDD 태스크)
  브레인스토밍 확정. 핵심: `term`(입찰주기1~10년)·`lastBid`·`confirmed`(확정여부)·`updatedAt`·
  `interest`(♥) 스키마, confidence 유도(confirmed=true만 확정, 기본 추측, 무일자 미상), CSV
  입력(무라이브러리), 랭킹 개편(반응형·임박순↔관심도순·유형필터·♥·더보기 검색), 물결 효과.
- **SDD(subagent-driven) 12태스크 전부 완료** — 태스크별 구현자+리뷰어. 커밋 8aba368(T1)~
  0029556(T12). 단위테스트 27/27.
- **브랜치 뒤섞임 사고 복구**: 병렬 강남 워커와 공유 워크트리 → Task5가 main(bef58c7)에 잘못
  커밋 → feature로 cherry-pick(f01b197) 복구, 유실 없음. (다음 새 작업부터 git worktree 격리 합의.)
- **최종 브랜치 리뷰(opus)** → Fix-before-merge: `effectiveBid` 부분 이관 버그(regionUrgencyColor·
  drawMarkers·drawTicker·renderFallback가 raw contractEnd 사용 → 추측-only 레코드 지도/티커
  오표시) + 팝오버/편집폼 영문키 노출 + 죽은 빗금 조건. **단일 수정 웨이브 `3b9a0ff`**로 전부
  수정, 재리뷰 클린. 브라우저 재검증: 서울=RED·경기/공사=YELLOW·티커 미상 해소·팝오버 전부 한글.
- **main 병합 완료**(이 커밋). NEXT.md의 대시보드 항목 A는 **해소되어 제거**.

### 결정/특이사항

- confidence 저장 안 함(유도). 입찰예상일 표시=`날짜(확정|추측)`/`미상`. 필수=name/type/region,
  sources 필수 아님. ♥관심기관(tbd.interest)과 ★관심지역(tbd.watchRegions) 별개 키.
- SDD 원장 `.superpowers/sdd/2026-07-25-treasury-bid-dashboard-enhancements/progress.md`(git-ignored)에
  태스크별 커밋·리뷰·파킹·이월minor 전체 보존.

### 남은 것 (파킹 — non-blocking, 후속 polish 후보)

- parseCsv 따옴표 내 개행(멀티라인 셀) 미지원(라인 split 방식). CSV 업로드가 데이터셋 전체 교체
  (추가 레코드 유실 가능) + drawNational 시 breadcrumb 잔존. 모두 플랜 설계대로, 실사용 영향 낮음.

## Session (E2E 입찰워크플로우 시스템 — 브레인스토밍→스펙→sub-project 0 플랜)

> 이 섹션도 위 두 세션과 **별개 스레드**(브랜치 `main`, 브랜치 착오 없음 —
> `git branch --show-current`로 세션 시작 시 확인함).

### 한 일 (요약)

사용자가 실제 업무 workflow를 9단계로 정리해 제시(`입찰현황 파악 → 입찰 상황 발생 → RFI 공시
→ RFI 분석 → 제안서 기획 → 제안서 세부기획(영업/전산/예산) → 제안서 취합 → 제안서 검토 →
제안서 제출`) → **"`dashboard`/`report`/`agent` 3개만이 아니라 이 워크플로우 전체가 담긴
시스템을 만들고 싶다"**는 요청으로, `superpowers:brainstorming` 스킬을 정식으로 밟아
한 세션 안에서 스펙 작성까지 완료.

**확정된 핵심 결정** (순서대로 사용자 확인받음):
- 6단계 3팀(영업/전산/예산)은 **AI 에이전트 역할**(사람은 검토만).
- 배포는 **부분 폐쇄망** — 1~3단계(공개데이터)는 **클라우드 DMZ(AWS FastAPI)**,
  4~9단계(민감정보)는 **완전 폐쇄망**. 경계는 정확히 3단계/4단계 사이.
- LLM은 **GPT-OSS 120B**(폐쇄망 LAN 전용, OpenAI-호환 엔드포인트), 모델 교체 대비
  어댑터로 분리(`base_url`/`model` env로만 교체).
- 콘텐츠 생성은 **하이브리드**(코퍼스 조립 + LLM은 문장 다듬기만) — 로컬 오픈소스
  모델 한국어 품질 불확실성 때문에 할루시네이션 리스크 최소화가 목적.
- 아키텍처는 **Approach A**(FastAPI 백엔드가 대시보드+API 서빙, SQLite 통합 레지스트리),
  프론트는 **Next.js**(사용자가 vanilla 유지보다 이쪽 선택, dashboard 정적자산은
  이식 대상으로 보존).
- **Track 1(운영, 완전 로컬 폐쇄망) / Track 2(AWS+Vercel 클라우드 데모·설득용)** 2-트랙,
  코드 분기 없이 env var로만 갈라짐 — 사용자가 Track 2도 **직접 눈으로 확인해보고 싶다**고
  추가 요청, "AWS FastAPI hello-world 배포·접속확인"을 향후 첫 마일스톤으로 못박음.

### 산출물

1. **스펙**: `docs/superpowers/specs/2026-07-26-e2e-bid-workflow-system-design.md`
   (커밋 `1cf9d70`). ①목적/범위 ②아키텍처(DMZ/폐쇄망 다이어그램) ③레지스트리 스키마
   (`institution_id`를 `giganlist/{id}/` 폴더명과 강제일치, localStorage→SQLite 전환,
   DMZ→폐쇄망 CSV 반입게이트는 **기존 dashboard CSV 업로드 기능 재사용**) ④9단계
   상태머신+API 7개 엔드포인트 ⑤6단계 3팀 분화(역할 라우팅 규칙기반, LangGraph 병렬분기
   +`operator.add` reducer로 merge) ⑥LLM 어댑터 ⑦기존 코드 영향범위 ⑧구현 순서(0~6
   sub-project로 분리, 각각 별도 plan). **중요**: 이 스펙은
   `agent/docs/superpowers/specs/2026-07-21-rfp-agent-team-design.md`의 핵심 결정
   #4(Claude Agent SDK 서브에이전트로 스킬 호출)/#5(CLI 단독 실행)를 **명시적으로 대체**함
   — 완전 폐쇄망에서는 Claude Agent SDK 자체가 외부(Anthropic) 네트워크 의존이라 동작 불가.
   아래 "NEXT.md 갱신"에 이 영향 명시.
2. **계획(sub-project 0)**: `docs/superpowers/plans/2026-07-26-registry-institutions-api.md`
   (커밋 `64ad8aa`). 5개 TDD 태스크 — ①SQLite 스키마(`backend/db.py`) ②Pydantic
   모델+repository CRUD(`backend/models.py`, `backend/repository.py`) ③CSV 파싱
   (`backend/csv_import.py`, 기존 dashboard 템플릿 한글헤더 재사용) ④FastAPI
   엔드포인트 4개(`backend/main.py`, `backend/routers/institutions.py` — list/detail/
   import/artifacts만; `advance`/`status`/`checkpoint`는 sub-project 2 몫으로 의도적
   제외) ⑤giganlist 23개구 시딩 CLI(`backend/seed.py`) + 수동 스모크체크.
   **아직 실행 미착수** — 세션 종료 시점에 사용자에게 "Subagent-Driven vs Inline"
   실행방식을 물어본 상태에서 마무리 지시가 들어와 대기 중.

### 환경 확인 (다음 세션이 바로 알아야 할 것)

- 이 머신에서 맨 `python`/`pip`는 **Windows Store 스텁**이라 실행 안 됨(`python was not
  found` 에러). 실제 인터프리터는 **`py -3`**(Python 3.14.0, 설치 경로
  `C:\Users\snrnd\AppData\Local\Python`)로 실행해야 함. `fastapi`/`pydantic`/`pytest`/
  `httpx`/`langchain-openai` **전부 미설치 상태**(pip list 확인함) — plan의 Task 1
  Step 2(`py -3 -m pip install -r requirements.txt`)가 이걸 설치하는 첫 단계.
  `agent/` 기존 pytest 테스트들도 이 환경에서는 아직 실행해본 적 없다는 뜻이므로,
  `agent/` 관련 작업 재개 시에도 같은 의존성 설치가 먼저 필요할 수 있음.

### NEXT.md 갱신

- **신규 항목 6 추가**: E2E 입찰워크플로우 시스템 — 스펙 확정, sub-project 0(레지스트리)
  계획까지 작성 완료, 실행 방식(Subagent-Driven/Inline) 선택 대기.
- **항목 2(agent/ RFP 팀 확장) 상태 갱신**: 이번 세션의 새 스펙이 그 설계의 핵심 결정
  #4/#5를 대체한다는 사실을 명시 추가 — 워크트리 `rfp-agent-team`에 이미 구현된 Task 1
  (`run_subagent`, `claude-agent-sdk` 기반)·Task 2(`rfp_locate_node`, Task1 호출)는
  폐쇄망 운영 전제와 충돌하므로 **그대로 재사용 불가**, 재개 전에 반드시 이 판단을 사용자와
  다시 확인할 것. 항목 자체는 삭제하지 않음(사용자가 "이미 만든 것 재사용 vs 새 설계로
  대체" 중 아직 명시적으로 고르지 않았기 때문).
