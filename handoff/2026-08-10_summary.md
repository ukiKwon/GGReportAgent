# 2026-08-10 작업 요약

## Session 00:18

⚠️ 이 세션은 `2026-08-09_summary.md` `## Session 22:45`에서 **이어진 같은 대화**다
(자정을 넘기며 날짜가 바뀌었다). 8/09 파일에는 항목 1(`POST /institutions`)·항목 10
(보고서 2.0 갱신)·항목 3(로컬 DB 종결)·항목 2(보류 실측)가 기록돼 있다.
이 파일은 그 뒤에 이어진 **항목 5(LLM 실검증)** 만 다룬다.

**커밋**: main `3eefc43` (push됨).

### 배경 — 이 PC에 설치된 모델이 0개였다

항목 5는 원래 "운영 모델 `gpt-oss-120b`(65GB)를 이 PC(RAM 15.6GB)에 못 올린다"는
하드웨어 종속 항목이다. 그런데 조사해보니 그 이전 문제가 있었다 — **`ollama list`가
비어 있었다.** 2026-07-30에 쓰던 `llama3.1:8b`조차 사라져서, `LLM_MODEL=auto`가
고를 수 있는 후보가 **실제로 0개**였다.

### 한 일 1 — 모델 설치 (사용자 확정)

| 모델 | 크기 | 역할 |
|---|---|---|
| `qwen3.5:9b` | 6.6GB | 1순위 후보 |
| `exaone3.5:7.8b` | 4.8GB | 한국어 특화 비교군 (LG AI Research) |
| `qwen3.5:4b` | 3.4GB | 폴백 후보 |
| `bge-m3` | 1.2GB | 검색 임베딩 (별개 용도 — 없으면 지식 탭 인덱스 빌드 불가) |

**검토했으나 제외한 것들** (다시 묻지 않도록 근거를 남긴다):
- **`gpt-oss:20b`** — 운영 모델과 같은 계열이라 가장 가깝지만, 다운로드 14GB에
  ollama 공식 요구가 "최소 16GB 메모리". 이 PC는 **15.6GB로 딱 못 미친다.**
- **Kimi 계열**(`kimi-k3`·`kimi-k2.7-code`·`kimi-k2.6`) — ollama에 올라온 것이 전부
  **`cloud` 태그(클라우드 전용)** 다. ⓐ로컬 추론은 1T MoE라 1비트 양자화로도 약 250GB
  필요 ⓑ**더 중요하게, 프롬프트가 외부 서버로 나간다** — 폐쇄망 전제 및 PII 스캐너를
  둔 이 프로젝트의 설계와 정면으로 충돌한다. **재검토 불필요.**

### 한 일 2 — 코드 변경 (TDD, RED→GREEN 관측)

- `agent/model_select.py`의 `MODEL_TIERS` 맨 위에 **`("qwen3.5:9b", 9.0, 4)`** 추가.
  최소 RAM 9.0은 가중치 6.6GB + 컨텍스트/OS 여유 기준(llama3.1:8b가 4.7GB에 7.0인 것과
  같은 잣대). 더 작은 qwen(4b·2b)은 **설치해서 확인한 뒤에** 넣기로 하고 넣지 않았다 —
  아무도 안 받아본 모델을 티어에 미리 넣으면 추측이 표에 섞인다.
- **함께 발견해 고친 결함**: `agent/llm.py`의 auto 실패 경고가 후보 목록을
  `"(llama3.1:8b / llama3.2:3b / llama3.2:1b)"` 로 **문자열 하드코딩**하고 있었다.
  티어를 늘려도 그 줄만 옛 목록을 계속 말한다. `candidate_hint()`를 신설해
  `MODEL_TIERS`에서 파생시키고, 그 파생을 테스트로 고정했다.
- 테스트 745 → **749 passed**(+4).

### 한 일 3 — 수원시 공고문 배점표 대조 (항목 5의 본 목적)

정답지 `.claude/skills/rfp-locate/references/scoring_schema.json` — 총점 100 / 6항목 /
배점 `8·17·21·22·25·7`. 하네스는 8/06 세션과 같은 방식(Ollama 네이티브 `/api/chat` +
`format`=JSON 스키마 + `think:false`)이고, 프롬프트 **4,348자**로 8/06 기록과 같은
값이 나오는 것을 먼저 확인한 뒤 측정했다.

#### 발견 ① — 기본 설정에서는 측정 자체가 성립하지 않는다

Ollama 기본 temperature(0.8)에서 **같은 프롬프트·같은 모델인데 실행마다 결과가 달랐다.**

| 모델 | 1차 | 2차 |
|---|---|---|
| `qwen3.5:9b` | 2항목 / 합 33 / 총점 103 | 10항목 / 합 135 / 총점 100 |
| `exaone3.5:7.8b` | 15항목 / 합 92 / 총점 100 | 11항목 / 합 182 / 총점 100 |
| `qwen3.5:4b` | 17항목 / 합 90 / 총점 100 | 5항목 / 합 26 / 총점 100 |

`temperature=0` + `seed=0`으로 고정하니 **두 번 호출이 JSON 한 글자까지 동일**했다
(21항목·합 300·총점 100·출력 860토큰, 완전 일치). → **모델 비교는 이 조건에서만.**

⚠️ **이 세션에서 실제로 오판했다.** 1차 결과만 보고 "exaone의 한국어 이해가 확실히
낫다"고 사용자에게 보고했는데, 2차·3차에서 뒤집혔다. **1회 실행으로 모델을 평가하지
말 것.** 같은 이유로 8/04의 `llama3.1:8b` 96점·`qwen3:14b` 108점도 각 1회 측정이라
대표값으로 단정할 수 없다.

#### 발견 ② — `qwen3.5:9b`가 정답 6개를 전부 맞혔다 (2회 재현)

| 모델 (temp 0 / seed 0) | 항목 | 배점합 | 총점 | 정답 6개 적중 | 소요 |
|---|---|---|---|---|---|
| 정답 | 6 | 100 | 100 | — | — |
| `qwen3.5:9b` | 21 | 300 | **100** ✅ | **6/6** ✅ | 249초 |
| `exaone3.5:7.8b` | 17 | 104 | **350** ❌ | 1/6 | 152초 |

**이 항목의 기존 진단("모델이 배점 숫자를 지어낸다")은 `qwen3.5:9b`에는 해당하지
않는다.** 8/04·8/06에 두 모델에서 반복 관측된 현상이라 일반 법칙처럼 적혀 있었는데,
최신 세대 모델에서 깨졌다.

**한국어 특화가 곧 정답은 아니었다.** `exaone3.5:7.8b`는 항목명·분류 표현이 깔끔하고
`주요 경영지표 현황 = 17점`은 맞혔지만, **총점을 350으로 지어냈고** 적중은 1/6이다.
반대로 qwen은 `OCR 센터 운영能力及 계획`처럼 **한자가 섞여 나오는** 약점이 있다.

#### 발견 ③ — 합계 300은 오류가 아니라 계층이다 (→ 신규 항목 13)

`qwen3.5:9b`는 `계(100)` + `대분류 5개(합 100)` + `세부항목(합 100)`을 **한 목록에
평평하게** 담았다. 세부항목도 각자 부모에 정확히 합산된다(8+7+5+1=21, 7+8+7=22,
6+8+8+3=25, 2+5=7). **값은 다 맞는데 구조를 표현할 자리가 없었다.**

→ 진짜 병목은 모델이 아니라 `ScoringCriterion`의 **평면 스키마**다. 그리고
`scoring_consistency()`가 이 정확한 산출물을 **오탐으로 잡는다** — 이 저장소가 못박은
"경고가 한 번이라도 틀리면 아무도 안 읽는다" 원칙에 걸리는 첫 사례다.
**신규 항목 13**으로 NEXT.md에 등록했다(설계 두 갈래 ⓐ스키마에 계층 ⓑ프롬프트로
레벨 고정, 어느 쪽인지 먼저 정해야 함).

### NEXT.md 변경

- **항목 5 갱신** — 위 실측 3건(평가 조건·모델별 결과·계층 발견)을 표째로 기록.
  본체(`gpt-oss-120b` 실호출)는 **여전히 열려 있다**. 다만 *"120b라야 된다"* 는 전제는
  약해졌다고 명기했다 — 9b가 이미 6/6을 맞혔고 남은 문제는 크기가 아니라 스키마였다.
- **항목 13 신설** — 배점표 스키마 계층 문제 + `scoring_consistency()` 오탐.

### 남은 것

- **항목 5 본체**: `gpt-oss-120b` 실호출은 여전히 불가(65GB > RAM 15.6GB).
  dGPU 서버나 폐쇄망 엔드포인트가 있어야 열린다.
- **`qwen3.5:4b`의 temp 0 측정 미실시** — 항목 5 표의 빈칸. 항목 13 설계 판단에
  도움이 되므로 착수 전에 채우면 좋다.
- **폴백 경로 실검증 미실시** — 모델이 2개 이상 깔렸으므로 이제 `LLM_MODEL` 부재 시
  폴백 동작을 실제로 확인할 수 있다(항목 5가 검증 대상으로 꼽은 3가지 중 하나).
- **계측 스크립트는 저장소에 넣지 않았다**(1회성, 8/06 선례). 재현 방법은 NEXT.md
  항목 5에 적어뒀다.

## Session 00:41 — 전국 지자체 금고 입찰 일정 대시보드 기본 데이터 (신규 항목 14)

사용자 요청: "전국 지자체 금고 입찰 일정표를 확인해서 dashboard에 표기해줘." 브레인
스토밍으로 조사 규칙을 확정한 뒤(스펙 참조) **광역 17→서울 25구→경기 31시군** 순으로
착수, 마지막은 사용자가 "경기까지만 하고 자러 간다"며 야간 자율 진행을 지시해
이어서 완료했다.

**커밋 6개, 전부 push됨**: `4235be7`(광역 17) → `dc1e3b5`(서울 25구, 날짜 2/25) →
`02bfea7`(서울 조사 경로 확립, eminwon) → `edb4845`(서울 5구 추가 조회) →
`10893b6`(서울 22구 전수조사 완료, 날짜 9/25) → `90afeac`(경기 31시군, 날짜 3/31).

**스펙**: `docs/superpowers/specs/2026-08-09-nationwide-treasury-schedule-design.md`
(§①~⑪, 전체 이력·근거·남은 일이 전부 여기 있다 — 이 요약은 발췌).

### 조사 규칙 (사용자 확정)

지어내지 않는 것이 핵심. `lastBid`(직전 회차 기준일) + `term`(실제 주기, 4년 일괄
가정 금지 — 예규가 "4년 **이내**"라 실제로 경남은 3년)만 넣으면 화면
(`logic.effectiveBid`)이 스스로 다음 회차를 계산해 `(추측)`으로 표시한다. 근거를
못 찾으면 **날짜를 통째로 비운다** — 지도에 회색 `?`로 남는 것이 의도된 결과다.

### 결과 — 84개 레코드, 날짜 확보 24곳

| 범위 | 레코드 | 날짜 확보 |
|---|---|---|
| 광역 17개 시·도 | 17 | 9곳 |
| 서울 25개 자치구 | 25 | 9곳(2026년 진행 중 공고 확정 2건 — 강남·동대문) |
| 경기 31개 시군(구 분할로 42 레코드) | 42 | 3곳(수원·부천·안성) |

**2026년 진행 중 공고를 실물로 3건 찾았다** — 강남구(2026-07-30)·동대문구
(2026-07-30)·인천시(2026-08-06, 8/9 조사분). 8~9월이 공고철이라는 예측이
실측으로 확인됐다.

### 조사 경로를 하나 새로 뚫었다 (§⑨, 서울 단계)

웹 검색으로는 자치구 공고를 못 얻는다(0건). 대신 **eminwon 고시공고 게시판에
제목 검색 파라미터가 있고, 대상 구로 navigate한 뒤 같은 출처에서 fetch하면 목록이
그대로 나온다.** 23개 구의 eminwon 호스트를 실측해 스펙에 표로 남겼다. 다만 이
방법은 **브라우저 확장의 도메인별 승인이 필요**해서, 사용자가 잠든 경기 단계에서는
쓸 수 없었다 — 경기는 순수 웹 검색으로 진행했고 그래서 날짜 확보율이 낮다
(3/31 vs 서울 9/25).

### 작업 중 실제로 낸 실수 2건과 회귀 테스트

1. **도봉·은평구**: 오래된 회차(2014·2018)를 그대로 `lastBid`에 넣었더니
   `addYears`가 한 번만 더해 **이미 지난 날짜**가 나왔다("곧 만료"가 아니라
   "이미 늦었다"는 잘못된 빨간불). 두 구 모두 날짜를 되돌리고,
   `institutions_data.test.js`에 **"유도 날짜가 조사 시점보다 1년 넘게 과거면
   실패"** 하는 회귀 테스트를 추가했다.
2. **경기 데이터로 서울만 보고 세운 테스트 가정 2개가 깨졌다** — ⓐ subRegion이
   region 코드로 시작한다는 가정(서울은 우연히 '11'/'11XXX', 경기는 '41'/'31XXX'로
   실제로 다른 코드 체계) ⓑ "모든 레코드는 URL 필수"가 너무 강함(순수 웹검색이라
   진짜 못 찾은 26개 시). 둘 다 §⑪에 근거와 함께 고쳤다.

### 화면 변경 (`dashboard/js/render.js`, `logic.js`)

- `activeRegions`를 서울·경기 2곳 → **17개 시·도 전부**로 확대.
- `hasSubGeo(code)` 신설 — 면 색칠(activeRegions)과 드릴인 가능 여부
  (구/시군 GeoJSON 존재)를 분리. 서울·경기 외 15곳은 색은 칠해지되 클릭하면
  "구/시군 경계 준비중"으로 안내(이전엔 오류 배너만 떴다).
- `logic.formatDDay()` 신설 — 지난 날짜가 `D--3`으로 표시되던 버그 수정
  (D+n/D-day로 정리). 인천 진행 중 공고가 실제로 이 버그를 드러냈다.

### 부수 발견 — 고치지 않고 기록만 함

- **전남광주통합특별시**(2026-07-01 출범): 지도는 광주(29)·전남(46)을 여전히
  별개 폴리곤으로 갖고 있는데 실체는 통합됐다. 두 레코드가 같은 통합 일정을
  각각 든 채로 남아 있다 — 폴리곤 병합 여부는 사용자 결정 사항.
- **경기 일반구 분할 도시(ⓐ안)의 랭킹 부작용**: 수원이 4개 구로 쪼개져 임박
  TOP5 티커에 같은 이름이 중복으로 뜬다. 데이터 오류는 아니고 ⓐ안의 당연한
  결과이나, 다른 시가 랭킹에서 밀려나는 부작용이 있다. 화면 로직(랭킹
  중복제거) 변경은 이번 범위 밖.
- **부산은 여전히 `geo/busan.js`가 없다**(§⑦-6에서 이미 지적됨, 재확인만 함).
  부산 조사에 착수해도 드릴인 화면은 안 생기고 전국 면 색·랭킹에만 반영된다.

### 검증

node 211(세션 시작) → **229 passed**. pytest 749 passed(무관 변경 없음, 확인만).
브라우저 실측 2회(서울 단계·경기 단계) — 콘솔 오류 0건, 지도·드릴인·랭킹 패널
정상 렌더링 확인.

### 남은 것 (NEXT.md 신규 항목 14로 등록)

1. 서울 노원·성북 — eminwon 경로 미확보(노원은 시스템 자체 불명, 성북은 검색
   파라미터 안 먹음).
2. 서울 음성 14구 — 9월 접수철에 재조회하면 더 많이 잡힐 것으로 예상(스펙 §⑨·⑩).
3. 경기 eminwon 직접조회로 26개 미확보 시 채우기 — 서울과 같은 방법이 통하는지
   확인이 첫 단계(도메인 허용 필요, 사용자 있을 때 진행).
4. 부산 16개 구·군 — `geo/busan.js` 부재가 선행 조건.
5. 전남광주 통합 폴리곤 처리 — 병합 여부 사용자 결정 필요.
6. 랭킹 TOP5 중복 표시 — 사용자가 화면 보고 거슬리면 그때 판단.

## Session 09:43 — 경기 26개 시 eminwon 전수 조회 (항목 14-③ 해소)

**한 일**: NEXT.md 항목 14의 하위 항목 ③(경기 eminwon 직접조회)을 사용자 승인
("항목 14부터 해보자") 후 완료했다. 상세 이력·방법은 스펙
`docs/superpowers/specs/2026-08-09-nationwide-treasury-schedule-design.md` **§⑫**에
기록했다(상세 페이지 POST 호출법 포함). 요점만:

- **경기 31개 시 전부 `eminwon.{시도메인}.go.kr` 실존** — DNS로 확인 후
  26개 미확보 시를 전수 조회(도메인 허용은 사용자가 실시간 클릭).
- **날짜 확보 24 → 27곳**:
  - **광명 `contractEnd` 2026-08-04 확정(진행 중!)** — 「금고 지정 계획 재공고」
    제2026-1764호, 공고기간 ~08-11. 서울 강남·동대문에 이은 3번째 진행 중 실물.
  - **파주 `lastBid` 2025-05-14** — 지정공고 원문(NH농협·약정 2026-01-01~2029-12-31).
  - **양주 `lastBid` 2022-10-28 + term 4** — 지정공고 원문(약정 ~2026-12-31).
    **다음 회차 2026 하반기 임박.**
- **term만 확보**: 평택·동두천(각 4년 — 최근 회차가 게시판에 없어 lastBid는
  도봉·은평 선례대로 비움). 부천은 2021 지정공고로 4년 주기 실증 보강.
- **음성 20곳** 전부 sources에 조회 근거 기록(재조회 방지). **시흥은 eminwon
  게시판 자체가 없음**(ksign 로그인 전용).
- `institutions_시도금고.csv` 재생성(37행). node **229 passed**.
- ⚠️ 작업 중 `Set-Content`가 institutions.js에 UTF-8 BOM을 붙인 것을 발견하고
  제거했다 — PowerShell로 이 파일을 고칠 때 주의.

**항목 14에서 남은 것**: ①(노원·성북) ②(서울 9월 재조회 — 9월 중순 고정)
④(부산 — `geo/busan.js` 결정 선행) ⑤(전남광주 폴리곤) ⑥(TOP5 중복, 비차단).

## Session 11:28 — 부산 16개 구·군: geo/busan.js 신설 + eminwon 전수 조회 (항목 14-④ 해소)

사용자 결정: 부산은 **ⓐ안(GeoJSON을 구해 드릴인 지도까지)**. 상세는 스펙
`2026-08-09-nationwide-treasury-schedule-design.md` **§⑬**. 요점:

- **geo/busan.js 신설(33.8KB)** — southkorea/southkorea-maps(kostat 2018, MIT)
  원본에서 부산 21XXX 16개 추출, DP 간소화(0.0005) + 5자리 반올림. 섬 폴리곤 유지.
  배선 3곳: `render.REGION_GEO['26']` · index.html script · 테스트 커버리지 +1.
- **부산 16개 구·군 eminwon 전수 조회** (호스트 `eminwon.{bs*}.go.kr` 전부 실존):
  - **사상구 lastBid 2024-10-25 + term 4** (지정공고 원문 + 협력사업비 4년 간격 3회).
  - 서구: 지정공고 2024-11-15를 확인했으나 **약정기간이 첨부에만 있어 필드 보류**
    (sources에 기록, 조례 확인 시 승격 — 스펙 §⑬ 남은 일 13).
  - 사하구 term 4 (2017·2021 2회차). 음성 13곳(해운대는 '게재기간 중만 노출' 유형).
- 총 레코드 84 → **100**, 날짜 확보 **28곳**. CSV 재생성(+16행). node **230 passed**.
- **브라우저 실측**: 부산 드릴인 16개 폴리곤·사상구 색칠·랭킹 정상, 콘솔 오류 0건.
  드릴인 화면에 구 라벨이 없는 것은 경기와 같은 기존 동작(회귀 아님).
- 서울과 이름이 같아지는 6개 구(중·서·동·남·북·강서)는 레코드 이름에 '부산 ' 접두사.

이로써 **사용자 지시 순서(서울→경기→부산)의 마지막 항목이 완료**됐다. 항목 14에
남은 것: ①노원·성북 ②서울 9월 재조회(시점 고정) ⑤전남광주 폴리곤 ⑥TOP5 중복
+ 신규(부산 서구 승격).

## Session 11:43 — 노원·성북 경로 확보 (항목 14-① 해소, 서울 전수 조회 완결)

상세는 스펙 §⑭. 요점:

- **노원**: eminwon이 있었다 — `nowon.eminwon.seoul.kr`(이전 시도는 호스트명이
  달랐을 뿐). https 인증서 문제라 http 진입→리다이렉트. 게시판 활성(227건)이나
  게재기간 중만 노출 유형 — '금고' 0건 = 아직 공고 전. **9월 재조회 대상에 추가.**
- **성북**: 자체 게시판 검색의 열쇠는 숨은 파라미터 `key=6977&searchCnd2=notAncmtSj`.
  **lastBid 2022-10-27 확정** — 지정공고 원문(우리은행 893.92점 단수금고, 약정
  2023-01-01~2026-12-31 = 서울 COMMON과 일치). 2018 회차도 확인(4년 주기 실증).
  2026-02-26 금고 규칙 개정 입법예고 = 2026 회차 준비 신호.
- 날짜 확보 **29곳**(서울 10). 서울 25개 구 **전수 조회 완결(미완 0)**.
  node 230 passed, CSV 2행 갱신.

## Session 13:06 — 부산 서구 승격 완료 (스펙 §⑬-13 해소)

- 조례(제9조)는 "4년 **이내**"라 결정타가 못 됐다(일괄 가정 금지 규칙).
- **결정타 = 지정공고 첨부 hwp**: 사용자 승인 후 다운로드, python olefile로
  PrvText 스트림 추출(hwp 5.0 = OLE 컴파운드) — **약정기간 2025-01-01~2028-12-31
  (4년간)** 원문 명시. 1금고 부산은행 · 2금고 NH농협은행(각 단독 신청·적격).
- → 서구 **lastBid 2024-11-15 + term 4 승격**. 날짜 확보 **30곳**(부산 2).
- eminwon 첨부 다운로드 요령을 스펙 §⑬-13에 기록: FileDown.jsp 인자 3개
  (goDownLoad에서), **UTF-8 percent-encoding 필수**(EUC-KR은 오류 응답).
- node 230 passed, CSV 1행 갱신. 내려받은 hwp는 추출 후 삭제.

## Session 13:39 — 전남광주 폴리곤·레코드 병합 (항목 14-⑤ 해소)

상세는 스펙 §⑮. 요점: 사용자 결정으로 2026-07-01 통합 출범을 지도·데이터에 반영.

- korea.js: 전남(46) 본토에 **광주 모양 구멍이 실제로 뚫려 있었다** — 그 구멍
  제거 + 광주(29) 피처 삭제 = 17→16개, 이름 '전남광주'. 내부 경계 없는 한 덩어리.
- institutions.js: 광주·전남 두 광역 레코드를 「전남광주통합특별시」(region 46)
  1건으로 병합(100→99 레코드). 날짜는 비움(차기 공모 2026-10 예정, 일자 미상) —
  공모가 뜨면 이 한 레코드에 contractEnd만 달면 된다.
- render.js activeRegions·REGION_NAME에서 '29' 제거, '46'→'전남광주'.
  테스트 고정값 17→16. node 230 passed, CSV 재생성(99행).
- 브라우저 실측: '전남광주' 단일 폴리곤·라벨, 콘솔 오류 0건.
