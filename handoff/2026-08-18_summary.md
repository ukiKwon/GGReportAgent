# 2026-08-18 작업 요약

## Session (오전)

NEXT.md **항목 15(대시보드 리디자인 후속 소소 2건) 종결.** 사용자 지시 "15번부터 하자".

### 한 일

- **하위 1(색 리터럴 재도색) — 이미 끝나 있던 것을 실측 확인.** main `2a0848e`
  (2026-08-14, push됨)가 클러스터 배지·geo-retry 배너의 `#2a3550`/`#e6ecff`를
  현행 토큰 `#1a2029`/`#e2e7ee`로 교체했다.
  `grep -rn '2a3550\|e6ecff' dashboard/js dashboard/index.html` → **0건**
  (비교용 `index_*.html`은 제외 — 라이트 계열은 그 색이 의도다).
  - ⚠️ **NEXT.md에 적혀 있던 교체 목표값 `#131a24`/`#dbe4ee`는 틀린 값이었다** —
    HTS 시절 토큰인데, 그 사이 7.1(다크 크롬+파스텔) 정본 승격으로 팔레트가
    바뀌었기 때문. 항목에 적힌 목표값도 시간이 지나면 stale해진다는 사례.
- **하위 2(서버 모드 6탭 육안 확인) — 남아 있던 `권한관리` 1개를 확인해 완료.**
  2026-08-14 Session 16:20이 워크플로·대화·지식·작업함·결재함 **5개만** 확인했고
  (그때 나온 수정이 `53c9cb8` 대화 보내기 버튼 가시성), 6번째가 빠져 있었다.
  - 방법: `py -3.14 -m backend.demo`(:8000) + Chrome. 계정 전환 `<select>`에
    `change` 이벤트를 실제로 발생시켜 **권 차장(전산팀)** 으로 전환 — 전산팀이
    권한관리 메뉴를 가진 역할이다(화면의 권한표 기준).
  - 결과: **다크 크롬·틸 토큰 일관, 깨짐 없음, 콘솔 오류 0건.** 체크박스
    `accent-color`가 정본 틸 `rgb(87,184,173)`로 실제 적용된 것까지 확인.

### 오판할 뻔한 것 (다음 세션 주의)

권한관리 탭이 활성인데 상단 탭바에 안 보여 **"탭바 오버플로우 잘림"으로 의심**했다.
실측하니 `display:none`이었고, 영업팀 팀원 프로필에는 결재함·권한관리가 없는
**권한 규칙대로의 정상 동작**이었다(화면의 권한표와 일치). 숨은 탭을 JS로 강제
클릭한 내 조작이 만든 상태였을 뿐이다. → **권한 있는 역할로 전환해서 볼 것.**

### 새로 발견 → NEXT.md 항목 16으로 등록

**폼 컨트롤이 다크 크롬에서 라이트로 그려진다.** `color-scheme`이 `normal`이라
브라우저 기본 컨트롤이 라이트로 렌더된다 — 체크된 상자는 틸(accent-color 정상)인데
**언체크 상자가 흰색**이라 다크 배경에서 튄다. `<select>`는 CSS로 칠해져 있어 무관.
고치는 건 `:root { color-scheme: dark }` 한 줄이지만 **정본 전역 시각 변경**이라
사용자 확인·DESIGN.md 반영이 필요하고 라이트 비교 파일에는 넣으면 안 된다.
항목 15 범위(6탭 훑기) 밖이라 **손대지 않고 별건으로 분리**했다.

### 상태

- 코드 변경 **0줄** — 이번 세션은 확인과 문서 정리뿐이다.
- `handoff/NEXT.md`: 항목 15를 해소 기록으로 전환(원문은 `<details>`로 보존),
  항목 16 신규 추가.
- 세션 시작 시점 워킹트리 clean, `origin/main`과 동기화 상태였다.
