# 2026-09-08 작업 요약

## Session 08:55 — `main`의 uploader 가져오기 (전체 머지는 포기)

`2026-09-07_summary.md` `## Session 18:32` 의 후속 ① 착수. 원 계획은
**`main` → `weblogic-java-migration` 전체 머지**였으나, 실제로 해 보고 **범위를 줄였다.**

### 전체 머지를 포기한 이유 (시도해서 알아낸 것)

`git merge --no-commit --no-ff main` 을 돌려 본 결과 **충돌 23건**이고,
전부 **`add/add`** 였다 — 두 브랜치가 같은 파일을 **각자 독립적으로 추가**해서
공통 조상이 없다는 뜻이다(PR #2·#3이 `main`에 따로 들어오며 생긴 구조).

**서브트리별로 최신인 쪽이 정반대**라 기계적 해소가 불가능했다:

| 대상 | 충돌 | 최신 | 근거 |
|---|---|---|---|
| `kgi-ggreport-web/` | 16건 | **이 브랜치** | `main`은 `bb4547c`(PR #2) **한 번**만 건드린 초기 스냅샷. 이 브랜치는 merge-base 이후 **30커밋**. 파일 242 vs 216, `InstitutionController` 230 vs 126줄 |
| `uploader/` | 5건 | **`main`** | 전날 확인한 상위집합 |
| `handoff/NEXT.md` · `2026-08-31_summary.md` | 2건 | 수동 | append-only 규약이라 기계적 선택 금지 |
| `docs/tools/md2pdf.py` · `docs/이관문의서_WebLogic_확인사항.md` | 2건 | 개별 확인 | `main`에 실제 개선 `b88be6d` 있음 |

🔴 **`kgi-ggreport-web/`을 `main` 쪽으로 받으면 단계 5-B 작업 30커밋이 통째로 되돌아간다.**
사용자에게 위 근거를 제시하고 **"uploader만 가져오기"** 로 확정했다.
→ 머지는 `git merge --abort` 로 취소했고, 작업 트리는 원상복구됐다.

### 한 일

```
git checkout main -- uploader/     # 26파일 · 2200줄 추가 / 60줄 삭제
cd uploader && mvn -o clean test   # 62건 전부 통과, BUILD SUCCESS
```

`main`이 uploader 파일 집합의 **상위집합**이라(이 브랜치 전용 파일 0건) 남는 찌꺼기가 없다.
가져온 것: `schema-oracle.sql` · Mapper `databaseId` **방언 분기** ·
`TimerManagerScheduler`/`LocalScheduler`/`ReclassificationTrigger` ·
`MyBatisConfig` · `web.xml` · `DEPLOY.md` · `tools/{export,import}_wtp.py` ·
`config-envs`의 `mybatis.*` · 테스트 7종(38 → **62건**).

지워진 60줄은 전부 *"Oracle DDL이 아직 없다"·"스케줄러를 WebLogic에서 쓰면 안 된다"* 같은
**미완성 고지**와 **주석 처리된 Oracle SQL**이었다 — `main`이 그 일을 실제로 해서
경고가 필요 없어진 것이다.

### NEXT.md 반영

- **항목 18**(uploader README §13-① 내부망 설정 누락 + Oracle DDL 부재) — ✅ **종결.**
  Oracle DDL·`mybatis.*` 둘 다 이번에 들어왔고, 함께 적혀 있던 `@Scheduled` 경고도
  CommonJ 경로 교체로 해소됐다(운영에서 **켜는** 것은 항목 25-②로 남는다).
- **항목 25-①** — "전체 머지"에서 **"uploader만 가져옴 + 브랜치 통합은 별도 과제"** 로 갱신.
  다시 통합할 때 쓸 규칙(서브트리별 ours/theirs)을 항목 안에 적어 뒀다 —
  **같은 조사를 반복하지 않도록.**
