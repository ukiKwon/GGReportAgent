# 2026-08-14 작업 요약

## Session 00:10 (2026-08-13 야간 세션의 연속)

대시보드 디자인 스킬 비교 실험 라운드 — 정본(index.html, HTS ship 판정본)은 그대로
두고 **첫 화면 느낌 비교용 변형 4종**을 만들었다. 사용자 평: 기존 3종(HTS·taste·
promax)은 "다 마음에 안 들어" → 추가 2종(brutal·minimal) 제작. **방향은 아직 미확정.**

- **백업**: `dashboard/index_2.0_impec.html` (impeccable ship 판정본 스냅샷).
- **변형 파일** (마크업·JS 공유, 스타일+팔레트 패치만 교체 — 팔레트는 각 파일 하단
  인라인 스크립트가 `render.DEFAULT_THEME`를 덮어쓰고, 라이트 계열은 티커/범례의
  흰색 쪽 color-mix를 어두운 쪽으로 몽키패치):
  - `index_3.0_taste.html` — taste-skill redesign: 다크 프리미엄(틴트 차콜·저채도
    코랄/샌드·글래스 범례·그레인·틸 액센트).
  - `index_4.0_promax.html` — ui-ux-pro-max DB 추천("Data-Dense Dashboard"):
    라이트 BI(화이트+네이비+앰버 티커 밴드). Fira 폰트 추천은 폐쇄망이라 시스템
    폰트로 대체.
  - `index_5.0_brutal.html` — brutalist-skill(Swiss Industrial Print 모드):
    신문지 기질·위험 빨강+잉크 농도 지도·반전 탭·`>>>` 텔레메트리·도면 십자.
  - `index_6.0_minimal.html` — minimalist-skill: 웜 본 캔버스·세리프 제호·뮤티드
    파스텔 지도·파스텔 알약 태그.
- **디자인 훅**: 변형 4종은 정본 DESIGN.md(HTS 체계)와 의도적으로 다른 실험이라,
  사용자 승인("go on") 받아 `.impeccable/config.json` `detector.ignoreFiles`에
  4파일 등록. flat-type-hierarchy 2건은 파일 한정 ignore-value(사유 포함).
- **주의(재발 방지)**: 헤드리스 Chrome은 `--window-size=390`이어도 **최소 창폭
  ~504px를 강제**하고 스크린샷만 390으로 크롭한다 — 모바일 레이아웃 검증은 iframe
  래퍼(`scratchpad/mobile-frame.html` 방식)나 실 브라우저로 할 것.

### 남은 것

- **방향 결정이 열려 있다** — 5종(HTS 정본 포함) 중 어느 것도 사용자를 만족시키지
  못함. 다음 라운드 전에 "무엇이 아쉬운지"를 먼저 물어볼 것 (NEXT 항목 15 갱신).

## Session 16:20

시각 방향 확정·정본 승격 마감 — NEXT 항목 15의 "방향 재논의" 하위 항목 해소.

- **사용자 피드백**: 라이트 계열(minimal·brutal)은 "밝아서 지도가 안 들어오고
  경계만 보인다" → 방향 도출: **다크 크롬 + 파스텔 지도(지도가 주인공)**.
- **하이브리드 3종**(7.0 impeccable / 7.1 taste-redesign / 7.2 ui-ux-pro-max,
  파일명 = 버전+플러그인명+방향 정책) 중 사용자가 **7.1 선택**.
- **승격**: `index.html` ← 7.1 + 타입 강약 미세조정(섹션 제목 15px/700, 지식
  요약행 본문급) + 새 방향 계약 주석. `render.js` DEFAULT_THEME = 파스텔+틸
  #57b8ad 정본(1.0 팔레트 계승, 테마 모달 경로 유지).
- **서버 탭 검증**: backend.demo(:8000)로 워크플로·대화·지식(의미검색 bge-m3)·
  작업함·결재함을 7.1 크롬에서 실확인 — 전부 토큰 일관, 깨짐 없음.
  ⚠️ backend는 py -3.14로 실행해야 한다(py -3=3.15엔 패키지 없음).
- **마감 리뷰**: fix 5건(지도판 폭 캡 완화·모바일 라벨 축소·티커 ▲→SVG·부제
  대비·제호 위계) → 전건 resolved → **ship**. 테스트 244/244.
- DESIGN.md 재기록(documenter), 실험 7.x 파일 삭제(내용은 정본에 승격됨),
  1.0~6.0 비교 파일은 히스토리로 유지. 훅 과도기 검출(구 HTS DESIGN.md 기준
  62건)은 재기록으로 일괄 해소.
